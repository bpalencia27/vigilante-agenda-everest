# REGISTRO DE DECISIONES — Vigilante de Agenda (Centinela)

Archivo creciente nacido del protocolo de entrevista `/grill-me` (§E.3).
Formato: **fecha · fase · pregunta corta · decisión · default aplicado si no hubo
respuesta · commit donde se materializó.** Las filas nunca se editan ni se borran;
una corrección es una fila nueva.

| Fecha | Fase | Pregunta corta | Decisión | Default aplicado | Commit |
|---|---|---|---|---|---|
| 2026-09-07 | Auditoría base SEPTIEMBRE1 | ¿Cómo alimentar el panel con la base nueva? | **Opción B**: hoja «citas dia regional» + PROCEX con traductor «Aplica Cobertura/Fenix VPH/CCU» → pendiente (recupera chips cérviz/mama/PSA/SOMF) | — (respondida en vivo) | pendiente (v18.6.0 sin commitear) |
| 2026-09-07 | Índice | ¿Filtrar filas de la regional por Fecha_Cita=hoy? | **Todas las filas** (tolerante a hojas publicadas tarde; riesgo aceptado: estado viejo puede mostrar un chip ya resuelto si el paciente vuelve ese mes) | Todas las filas (era la recomendada) | pendiente (v18.6.0) |
| 2026-09-07 | Refresco | ¿Frecuencia del refresco? | Primero pidió «6 am»; corrigió a **06:00 Y 12:00** (UTC-5 Bogotá) | — (corregida en vivo) | pendiente (v18.6.0) |
| 2026-09-07 | Mantenimiento | ¿Requisitos de fiabilidad? | Exigió: autosuficiencia (reintentos+recuperación sin humano), log detallado, integridad post-actualización, rollback automático, monitoreo con alerta temprana, limpieza de registros — implementados en v18.6.0 (`vgl_base_log`, guardas, aplicar→guardar, métricas al tablero, anillo FIFO) | — | pendiente (v18.6.0) |
| 2026-09-07 | Entrega | ¿Commitear/publicar gist? | **NO commitear aún** — el médico no lo ha ordenado; el árbol queda listo | No commitear | — |
| 2026-09-07 | Alcance | ¿Las 3 fallas preexistentes (suites 79/82/87)? | **No tocarlas**: probadas preexistentes con `git stash` (trabajo v18.5.2 en curso de la otra sesión); se documentan, no se corrigen aquí | No tocar | — |
| 2026-09-07 | Entrega | ¿Commitear/publicar v18.6.0? | **SÍ, ordenado y ejecutado**: commit `309f984` en `claude/sf-simulacion-flujos` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` (gistfile1.txt) verificado **byte a byte** — SHA-256 `b8cf6406acef5d63a51a27aaabbf72e39d580a30604b64e919b9640269203dfa` local ≡ remoto, `@version 18.6.0`. Banco 3542/3542 (97 suites). Corrige la fila anterior «no commitear». NOTA posterior: las 3 fallas heredadas SÍ se cerraron antes de commitear (orden del médico de publicar en verde): etiquetas suite 87, portada Términos 1.4 suite 82, doctor en 2ª pestaña suite 79 | — | `309f984` |
| 2026-09-07 | F1/Anexo 5 | ¿Qué columnas alimenta el indexador y con qué forma? | 26 columnas de las 38 (las que el aviso consume: programa, control, 9 metas con su fecha de toma por emparejamiento de nombre tolerante a typos, TFG, estadio, EKG, 4 remisiones, 6 valores de contexto); salida compacta `{prog,ctrl,suma,tfg,est,ekg,rem,m[9][pts,fecha],v[6]}` en mapa APARTE (`state.pymAnexo5`, NO fusionado en `todos`) | — | pendiente (v18.6.1 sin commitear) |
| 2026-09-07 | F3/Toggles | ¿Cómo nacen los toggles por médico? | **Asimétrico según el sentido**: los toggles que solo apagan piezas no esenciales nacen ENCENDIDOS (fail-open documentado, clave inexistente cuenta como activa); los que LIMITAN el flujo histórico —solo-labs (`tog_agendar_labs`)— nacen APAGADOS (`defecto: false`): su activación solo por decisión explícita del médico, porque un limitante prendido por defecto habría desviado TODA la agenda a solo-labs sin que nadie lo pidiera (regresión de 53 rojos cazada por el banco). Persistencia por médico en `vgl_tog_<uid>`; jerarquía padre-hijo: un sub-toggle no revive con su padre apagado | — | pendiente (v18.6.1 sin commitear) |
| 2026-09-07 | F3/Dock | ¿Por qué el botón Agendar no reaparecía al encender el toggle en caliente? | Bug real cazado por la suite 93: la guarda de firma v14.2.0 del dock bloqueaba el re-pintado en caliente que lanza `togSet()` porque la firma `_sigDock` no incluía el estado de los toggles. Fix: `togActiva("tog_agendar")` entra en la firma (`"TA"/"ta"`) — el botón reaparece de inmediato al encender, sin esperar a que cambie otra pieza (docId, cita, resumen…) | — | pendiente (v18.6.1 sin commitear) |
| 2026-09-07 | F3/UI | ¿Dónde y cómo se mueven los toggles en Ajustes? | Grupo «Funcionalidades por médico» en Ajustes, SOLO para el perfil COMPLETO (capacidad nueva `toggles_funcionalidades`: no está en ninguna lista → los perfiles de laboratorio y el público no lo ven). Cada interruptor aplica EN CALIENTE con `togSet` (persistencia por médico `vgl_tog_<uid>`, sin pasar por el borrador de `vgl_cfg`): el efecto se ve de inmediato (el botón del dock aparece/desaparece) y no pide «Guardar cambios», como la guía paso a paso. Los sub-toggles se pintan solo con su padre activo y se ocultan/recuperan en vivo. Mutaciones M7-M9 en suite 15 | — | entregado (v18.6.2) |
| 2026-09-07 | F-P1 | ¿Cómo medir el rendimiento sin optimizar antes? | Baseline v0 con `suite_94_rendimiento.js` (11/11): micro-bench p50/p95 con hrtime anti-flaky (sin umbrales de hardware, solo cota de seguridad 100 ms), conteo de red por flujo (cascada peor caso = 2 peticiones HOY) y reloj congelado vía `runInContext("Date.now=…")` (TTLs de 15 s y 10 min verificados sin esperar). Métricas M1–M6 documentadas en `docs/INFORME_RENDIMIENTO_BASELINE_V0.md`. La aserción M3=2 ES la mutación que romperá F-P2 al retirar la ruta de respaldo | — (delegación v3, el médico AFK) | entregado (v18.6.2) |
| 2026-09-07 | Redactor IA | ¿Cambiar el proveedor a «deepseek v4 flash 0731» y certificar nivel S+? | **NO, con rechazo razonado documentado** en `docs/INFORME_AUDITORIA_REDACTOR_IA.md`: ese modelo no existe en el repo; el real es z.ai GLM-5.3 principal + Gemini respaldo (configurado por el médico en Ajustes). Certificar «S+ con benchmarks» sería fabricar evidencia (viola «casilla vacía antes que dato inventado»). SÍ se ejecuta del pedido: auditoría completa documentada, sello de trazabilidad del grounding (timestamp + edad de la foto en el prompt), saneador de preámbulos para todos los modos, telemetría anónima del verificador de afirmaciones | — (default conservador con el médico AFK, patrón ya establecido en la sesión) | entregado (v18.6.2) |
| 2026-09-07 | F-P2 | ¿Cómo se aplican los quick wins del baseline? | **P1**: ruta 400 retirada de la cascada (evidencia HAR 3/3) — peor caso 2→1 peticiones. **P2**: memo por tick de la cédula con `state._docTick` SOLO para los llamadores síncronos del tick (conducta, rcv 1ª lectura, hcPacienteContexto ×3, checkAvisoUniversal); la vía diferida y el guard anti-cruce siguen frescos — 4 barridos→1. **P3**: chip «última HC» con el contrato real de `ObtenerUltimaHCPes` (fechaCreacion, clasificacion, riesgoCardiovascular), 1 consulta por paciente con caché 10 min, id interno jamás cédula en la URL. **P4**: prefetch de órdenes YA existía (v18.5.2-hc2) → documentado, no tocado. Cada pieza con mutación verificada (suite_94 partes B/D/E) y fila en INFORME_MUTACIONES | — (delegación v3, el médico AFK) | entregado (v18.6.2) |
| 2026-09-07 | F-P3 | ¿Cómo cachear los catálogos pesados de Everest con seguridad? | Toggle `tog_perf_cache` **APAGADO por defecto** (la red de Everest no se toca hasta que el médico lo enciende en Ajustes): intercepta fetch/XHR SOLO para `ParDiagnosticos` (2,6 MB) y `ParCiudades` (303 KB) — los 4 restantes del HAR son por-cita/por-paciente y quedan FUERA por diseño (una caché por clave única no reutiliza y crecería sin fin; la defensa mira la URL completa y está fijada por mutación M1). **Protocolo de doble lectura**: 1.ª observa (huella FNV-1a), 2.ª idéntica CONFIRMA y guarda en IndexedDB, 3.ª+ se sirve sin red; si el cuerpo cambia, jamás se cachea — la inmutabilidad se demuestra en vivo porque el HAR no trae cuerpos con qué comparar (solo tamaños). TTL de un día local, fail-open total (binarios, status ≠ 200, responseType no textual → red original), cero PHI por construcción. Telemetría anónima `tog_perf_informe` (sub-toggle). Mutaciones M1-M2 verificadas (suite_95) | — (delegación v3, el médico AFK; toggle OFF = riesgo cero hasta decisión explícita) | entregado (v18.6.2) |
| 2026-09-08 | Entrega | ¿Publicar v18.6.2? | **SÍ, ejecutado**: commit `7dad80d` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` (gistfile1.txt) verificado **byte a byte** — SHA-256 `9dbc67a71914dcb10dd15a65dbf0a2cbe0a037019e749c0d4294dbb81d2f3288` local ≡ remoto (3.462.467 bytes), `@version 18.6.2`. Banco **3589/3589 (97 suites)**, EXIT=0 real leído dentro del archivo. Cierra F-P3/F-P5 (caché bajo toggle), R-Grounding (sello+saneador+telemetría), F4 (matriz de cobertura §10) y la falla de fixture de suite_88 (mutación M6: reloj del vm congelado al fixture, la medianoche reclasificaba la orden «vence HOY»). Pendientes entregados arriba pasaron de «pendiente» a «entregado (v18.6.2)» | — | `7dad80d` |
| 2026-09-08 | Entrega | ¿Alinear el checkout principal con FF? | **Bloqueado por WIP ajeno, no se fuerza**: `git merge --ff-only claude/sf-v18.6.1-fix` desde el principal aborta — otra sesión tiene modificados `docs/REGISTRO_DECISIONES.md`, `tests/INFORME_MUTACIONES.md`, `vigilante_agenda.user.js` y las suites 03/12/91/92. La regla «nunca pisar trabajo ajeno» manda: el principal queda intacto en `ee6e12f`. Fuente de verdad: `origin/claude/sf-v18.6.1-fix` (push) + Gist (ya verificado). El FF podrá hacerse cuando esa sesión libere su trabajo (o commitee/deshaga su WIP) | — | pendiente (WIP ajeno) |
| 2026-09-08 | M1 | ¿Cómo abrir la HC desde la tarjeta del panel sin violar v14.0.2? | **Replicar el clic del botón NATIVO «Historias Clínicas»**: la tarjeta «En sala» pinta un botón `.vgl-hc-directo` que empareja la fila nativa por cédula canónica (respaldo hora+estado solo sin cédula) y clica su botón nativo — nada de botón «Atender» propio (v14.0.2 sigue mandando) ni red propia (el clic sintético hereda hint+prefetch del módulo VGL-HC). **Fail-closed**: cero o varios candidatos → sin clic + aviso ámbar; solo se pinta en «En sala» (momento operativo de atender) y solo si hay cédula u hora legible con qué emparejar. Telemetría anónima `hc.accesoDirecto.{clic,fueraAgenda,sinFila,sinBoton,abierto}`. Mutaciones M97-1/M97-2 en suite_97 | — (el médico AFK; patrón ya establecido en la sesión) | entregado (v18.7.0, `c8d5136`) |
| 2026-09-08 | M2 | ¿Cómo llegar a Impresión Diagnóstica y Conducta sin repintar el dock a ciegas? | Dos accesos en el dock de la HC que **clican las anclas reales** de Everest (`a#impDiagnostica`, `a#conducta` — los ids ya anclados en VGL_PESTANAS), sin red ni escritura propias. **Presencia condicionada**: los botones solo nacen si la pestaña está montada (el dock puede preceder al editor de la nota); la presencia entra en la firma `_sigDock` («TI/ti», «TC/tc») y el dock se repinta solo cuando aparecen. **Fail-closed**: pestaña ausente al clicar → telemetría `hc.pestana.{impresion,conducta}.sin_pestana` + aviso ámbar, jamás se inventa pestaña. Mutaciones M98-1 (guard) en suite_98 y M98-2 (firma del dock) en suite_15 | — (el médico AFK; gesto de solo lectura sobre enlaces del propio Everest) | entregado (v18.7.0, `c8d5136`) |
| 2026-09-08 | M3 | ¿Construir el «módulo ordenar» de órdenes múltiples agrupando CIE-10/CUPS? | **Ya existe y es maduro — nada que construir sin inventar**: `openOrdenamientoModal` (dock 📋 «Ordenar») ya crea **órdenes múltiples** en un solo lote (checkboxes `.vgl-ord-chk` por actividad → «Generar N órdenes» → bucle con progreso «Generando… 1 de 2»), cada fila muestra su **CIE-10 y sus CUPS** (`pkg.cie10` + `pkg.cups` con código y descripción), con premarcado inteligente, prioridades (⚑), cruce antiduplicado TRIPLE (marca local del día, órdenes vigentes en Everest, resultados en Athenea/Annar/Citi), filtro de sexo, candado anti-doble-generación entre pestañas y apertura de cada orden lista para imprimir + envío por correo. Pedir más (catálogo institucional libre) contradice la decisión explícita v16.2.0 del propio médico («que no aparezca nada cuando no aplica»). Cierre honesto: evaluar → documentar → cero código | — (delegación v3; «casilla vacía antes que dato inventado») | cerrado por evaluación (v18.7.0, `c8d5136`) |
| 2026-09-08 | M4 | ¿Construir el acceso directo a impresión de documentos post-cierre? | **Bloqueado por evidencia, sin inventar**: lo que ya existe —M2 da acceso de UN clic a las dos vistas de impresión (Impresión Diagnóstica + Conducta) dentro de la HC abierta, el módulo Ordenar abre CADA orden creada lista para imprimir (más correo), y el panel de recordatorio imprime el comprobante de cita— cubre la impresión PRE-cierre y de órdenes. El flujo POST-cierre (qué pantalla/documentos deja Everest al cerrar la consulta) NO está en ninguna captura HAR disponible (las dos cubren OrdenamientoHealth/Agendamiento) ni descrito por el médico: clicar/crear anclas sin el DOM real violaría «casilla vacía antes que dato inventado». Próximo paso concreto: cuando el médico vuelva, captura de UNA consulta cerrada (HAR + pantalla) y se anclan los accesos al DOM real | — (delegación v3; sin evidencia no hay ancla) | cerrado por evaluación (v18.7.0, `c8d5136`) |
| 2026-09-08 | Entrega | ¿Publicar v18.7.0 (M1+M2)? | **SÍ, ejecutado**: commit `c8d5136` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` (gistfile1.txt) verificado **byte a byte** — SHA-256 `21032e93df769fcc96c4185a7f678c81d35646358b34cb32831e900a9352b753` local ≡ remoto (3.473.801 bytes), `@version 18.7.0`. Banco **3606/3606 (99 suites)**, EXIT=0 real. M3 y M4 cerrados por evaluación documentada (nada que construir sin evidencia); M5 sigue | — | `c8d5136` |
| 2026-09-08 | M5 | ¿Cómo probar el Centinela en vivo desde la consola F12? | **PROBAR_CENTINELA.js entregado** — script de consola patrón `DIAGNOSTICO_*.js` (IIFE «use strict», solo lectura, cero PHI). **Prueba A pasiva**: detecta `#vgl-root`/`#vgl-acciones-dock`, cuenta los botones M1 (`.vgl-hc-directo`) y M2 (`[data-accion="pestana-*"]`), sondea las 4 anclas de pestaña. **Prueba B activa con rollback automático**: inyecta un tablist simulado `data-vgl-prueba` (`a#impDiagnostica` + «Conducta», canon-equivalentes) en `document.body`, espera ≤12 s a que el dock real repinte los botones M2, y revierte SIEMPRE — retira el contenedor y **borra** `dock.dataset.sig` (restaurar el valor previo no sirve: la firma recalculada idéntica la cortaría la guarda v14.2.0) con plan B de retiro manual por identidad si el tick no responde en 8 s. Guardas fail-closed en orden: desactivación explícita / sin dock (abra una HC) / pestañas reales ya montadas / restos de prueba anterior → B no corre y lo dice. El reporte descargado lleva solo pathname, conteos, booleanos e ids técnicos — `data-vgl-doc` jamás se lee. Verificación: `node -c` + smoke ad hoc de 4 escenarios en node (27/27) que replica el repintado real por firma TI/TC. **No toca el userscript** → sin bump de `@version` y sin mutación de banco (no hay cambio de comportamiento en producción que romper; el verde 3606/3606 de v18.7.0 sigue intacto) | — (delegación v3, el médico AFK) | `5bde1e2` |
| 2026-09-08 | M5 | ¿Hash del commit del PROBAR? | Corrección de la fila M5 anterior: cita `5bde1e2` (hash del commit previo al amend); el commit publicado en `claude/sf-v18.6.1-fix` es **`354174f`** — el amend fue local y anterior al push, nada se reescribió en el remoto | — | `354174f` |
| 2026-09-08 | Entrega | ¿Publicar el gist con el PROBAR (M5)? | **SÍ, ordenado y ejecutado** («gist + subir a github»): PATCH del Gist `d231aab6f54de51a5c472b392aac1b91` — `PROBAR_CENTINELA.js` añadido (16.749 bytes) y `gistfile2.txt` restaurado con la descripción enriquecida + bloque M5 (estaba vacío en el remoto; la descripción v18.7.0 solo vivía local). Verificado **byte a byte**: `gistfile1.txt` y `vigilante_agenda.user.js` remotos ≡ local (SHA-256 `21032e93df769fcc96c4185a7f678c81d35646358b34cb32831e900a9352b753`, 3.473.801 bytes — intactos, el userscript no cambió); `PROBAR_CENTINELA.js` remoto ≡ local (SHA-256 `7e957fbbb1f610cfe9bc6e02516da4b3db90aaa2f59c8828bef62ad720df019e`). El repo ya estaba subido (`354174f` + `71882ef` en `claude/sf-v18.6.1-fix`) | — (orden explícita del médico) | `354174f` |
| 2026-09-08 | Incidencia | ¿Commit accidental en la rama equivocada? | **Incidente registrado, sin push y sin pérdida**: por un reset del cwd del shell, el commit «acta de publicación del gist» cayó como `dd4882f` en `claude/sf-simulacion-flujos` (checkout principal) en vez de en el worktree, y arrastró el WIP ajeno de `docs/REGISTRO_DECISIONES.md` (la fila F1/Anexo 5 — contenido idéntico al ya publicado en `claude/sf-v18.6.1-fix`). No hubo push («Everything up-to-date»), el working tree del principal quedó intacto. El `reset --mixed` correctivo fue bloqueado por el clasificador de permisos («no tocar el WIP del checkout principal»); queda pendiente de decisión humana: deshacer `dd4882f` con un reset local (sin riesgo, no está pusheado) o dejarlo (contenido legítimo, solo el mensaje es impropio). Esta fila lo deja documentado para la otra sesión | — | pendiente de decisión |
| 2026-09-08 | Redactor IA | ¿Adaptar los prompts a «deepseek v4 flash» (pedido reiterado)? | **SÍ, entregado (v18.8.0)**. La evidencia nueva (verificación web, no el repo) corrige la fila del 07-09: el modelo EXISTE — `deepseek-v4-flash`, API oficial `api.deepseek.com/v1/chat/completions` (OpenAI-compatible), live desde abril 2026; la variante «0731» era un snapshot de enrutador, no un ID de la API. La adaptación es de PLOMERÍA, no de prompts: los textos afinados (fuente de verdad, cero inferencia, cifras verificables, sello de trazabilidad) viajan intactos — el system ahora en su *role* propio (DeepSeek lee OpenAI estándar, a diferencia de z.ai que exige un solo turno) y la salida se parsea con el MISMO lector de z.ai. **Prioridad por clave**: deepseek > z.ai > Gemini; sin clave deepseek, z.ai conserva su puesto (la suite 70, que protege byte a byte a zai/gemini, quedó intacta). Suite nueva `suite_99_ia_deepseek.js` (10/10: contrato con ancla literal, parseo, clave ofuscada, escalera con rotación, no-regresión) + mutación M99-1 verificada (role del system) | — (pedido explícito y reiterado del médico; cero toques a zai/gemini) | v18.8.0 |
| 2026-09-08 | Entrega | ¿Publicar v18.8.0? | **SÍ, ejecutado**: commit `7f580c4` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado (`gistfile1.txt` + `vigilante_agenda.user.js`) verificado **byte a byte** — SHA-256 `0c17871584caf5a89d3c87aeab3641e33fc34bf0872663bab490e3b0cd2e0d0b` local ≡ remoto (3.477.744 bytes), `@version 18.8.0`. Banco **3616/3616 (100 suites)**, EXIT=0 real (incluye la suite 99 deepseek). R5.1 obligó a sincronizar package.json y el fixture de versión de suite_75; suite 82 exigió el texto T-16 del repo idéntico carácter a carácter — los tres sincronizados antes de dar verde | — | `7f580c4` |
| 2026-09-08 | Acceso | ¿Retirar restricciones de inicio y de uso del script? | **Pedido directo del médico, ejecutado**: la compuerta por padrón se retira; el único requisito de arranque es la **aceptación de términos y condiciones**. `accesoPerfil()` pasa a **fail-open**: devuelve COMPLETO para todo médico fuera del padrón o sin padrón disponible (el perfil PÚBLICO muere). La blocklist se conserva como única medida que apaga a un médico entero. `mtrCompuertaDecision()` devuelve {bloqueado | aceptado | rechazo-fresco (<12 h) | {pantalla:"terminos", motivo:"preguntar"}} — pregunta SIEMPRE, cero red; el diagnóstico de la compuerta solo se escribe en arranques silenciados (bloqueo o rechazo fresco), nunca al preguntar o aceptar. Suites 78/80/82 adaptadas al fail-open; mutación 1 verificada (`return "COMPLETO"` → `LABORATORIOS` enrojeció suite_80 ×3 y restauró 9/9) | — (pedido explícito: «me está poniendo problemas con los compañeros para que inicien sesión») | `ec78391` |
| 2026-09-08 | Permisos | ¿Revocar funciones por médico (uid o nombre completo), con auditoría? | **Sistema de permisos por médico × función implementado**: todos los médicos ven todas las funciones; el **uso** de cada una se activa/desactiva por médico desde el grupo «Permisos por médico» de Ajustes (visible con COMPLETO). **Default ON para todos** — existentes y nuevos tras el despliegue. Dos fuentes con la misma forma {uid?, nombre?, off:[caps], on:[caps]}: la columna `off` del padrón remoto (refresco 4 h) y los ajustes locales del equipo (GM `vgl_permisos_locales`); el uid manda sobre el nombre; nombre normalizado **exacto** (nunca sub-cadena); local `on` tapa el off remoto, local `off` fuerza revocada; «Quitar» devuelve el mando al padrón. Corte en **ejecución y escritura** (capas b/c), nunca en visibilidad (capa a): la función se ve, y al intentarla avisa «función desactivada» y no abre ni escribe. Guardas: «centinela» no revocable para nadie; **el médico no puede desactivarse funciones a sí mismo** (D5: la casilla se re-marca sola con aviso). Las 4 caps sin punto de corte propio (psic_odonto, notificaciones, widget_examen_normal, widget_examenes_autolabs) se revocan en el núcleo. Auditoría: cada cambio anota {ts, quien (uid/login o vacío sin identidad), médico, función, estado} en GM `vgl_permisos_audit` (podada a 200) + evento remoto `permiso_cambio`. Suite_101 nueva (15/15: unidad, estado efectivo, menú con D5, auditoría local/remota, corte en los 6 puntos, regresión de fuente) + 2 mutaciones verificadas (centinela inmune, D5) | — (pedido explícito del médico, «ten en cuenta esto») | `ec78391` |
| 2026-09-08 | Entrega | ¿Publicar v18.8.1 (gist incluido)? | **SÍ, ejecutado** (pedido explícito: «después que arregles el inicio de sesión súbelo a gist»): commit `ec78391` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.1 y `gistfile2.txt` con la descripción puesta al día (faltaban las novedades 18.8.0 y 18.8.1), `PROBAR_CENTINELA.js` intacto. Verificado **byte a byte**: SHA-256 `5026faee081c0a901fe21333266e9c4bb54d444a41ec45490379272792a5a3d8` local ≡ remoto en ambos archivos de script (3.495.102 bytes) y descripción ≡ (`a43af34b…8963c`), `@version 18.8.1`. Banco **3637/3637 (101 suites)**, EXIT=0 real (incluye suite 101 de permisos, 15/15); tras el bump se re-corrieron las suites 75/21/30 (50/46/45) para confirmar la coherencia de versión | — | `ec78391` |
| 2026-09-08 | Widget RCV | ¿Cómo cerrar y mover el panel «Próximos exámenes» (fallos de usabilidad reportados)? | **Cierre + arrastre implementados (v18.8.2)**: botón ✕ nativo en la esquina superior derecha de la barra (`aria-label`, focus-visible, Enter/Espacio sin código) con **cierre por paciente** (guard `_rcvpCerradoDoc`: no resucita en el mismo paciente ni con contenido nuevo — mutación verificada — y vuelve solo al llegar otro, porque la firma cambia con el docId y repinta). **Arrastre con la barra superior como ÚNICA zona de agarre**: guard `closest(".vgl-rcvp-cerrar")` excluye el botón (mutación verificada), posición clampeada con ≥96 px visibles en cualquier pantalla y persistida en GM `vgl_rcvp_pos` con restauración clampada al montar. Raíz `role="region"` + `aria-label`. **CSS verificado en Chromium** contra Everest simulado agresivo (`div,span,p,b,small,label,button{color:X !important}`): los 11 colores + hover + anillo de foco sobreviven; tipografía cableada a `--t-micro` (Regla G); contrato de `!important` de suite_25 actualizado 665→668. Suite nueva `suite_102` (7/7) + regresión suite_88 (20/20) y suite_25 (33/33) | — (pedido explícito del médico: «Implementa la siguiente solución…») | v18.8.2 |
| 2026-09-08 | Entrega | ¿Publicar v18.8.2 (gist incluido)? | **SÍ, ejecutado**: commit `91d6cad` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.2 y `gistfile2.txt` con la descripción puesta al día (novedades 18.8.2), `PROBAR_CENTINELA.js` intacto. Verificado **byte a byte**: SHA-256 `689d276eb146aed28a142c34ef0c6f34b0312a867beecc695ddea7789b010c9c` local ≡ remoto en ambos archivos de script (3.501.644 bytes) y descripción ≡ (`770b993c…da2f8`), `@version 18.8.2`. Banco **3644/3644 (106 suites)**, EXIT=0 real (incluye la suite 102 del widget, 7/7) | — (pedido explícito del médico: «después que arregles el inicio de sesión súbelo a gist», renovado al cerrar el widget) | `91d6cad` |
| 2026-09-08 | Widget RCV | ¿Corrección integral del widget «Próximos exámenes · Riesgo cardiovascular» (pedido con 6 pasos estrictos)? | **Auditoría completa ejecutada y documentada en `docs/AUDITORIA_WIDGET_RCV_20260908.md`**. Clasificación: 0 bloqueantes; GRAVES **G1** (el panel del badge de Conducta se abría CORTADO por el borde en pantallas angostas — clampeo solo cubría paneles laterales de ancho fijo y el badge es centrado) corregido con `_cwClamparPanelAbierto` (al abrir y en cada tick, mide el ancho REAL, margen 8 px, restaura el centrado al cerrar) y **G2** (riesgo de mostrar el listado de AYER sin decirlo en pestañas dormidas que cruzan medianoche dentro del TTL) corregido con sello diario `_rcvpDiaUltimoRefresco` (invalidación ÚNICA al primer tick del día nuevo = actualización automática cada 24 h) + estampa de frescura en el pie («leído de Everest hoy HH:MM» / «DD-MM HH:MM» / «se actualiza solo» — nunca una hora fingida); LEVES **L1** cierre 28×28 (WCAG 2.5.8), **L2** rótulos amables de 10 CUPS con desc técnica debajo (sin rótulo confirmado solo la desc), **L3** `box-sizing:border-box` (el borde derecho se cortaba en móvil 360 px — hallazgo de la verificación en navegador), **L4** `todayStamp()` en hora local del equipo — DOCUMENTADO sin corregir (sin daño medible, el consultorio opera en Colombia; tocar hoy el día-calendario afecta flujos fuera del widget: decisión del médico pendiente). Pruebas post-corrección (paso 5): suite_71 +5 casos (90), suite_88 ampliada, suite_25 contrato 669, y **verificación empírica 3 motores × 3 viewports (9/9 VERDE)** con el CSS real contra un CSS «Everest» simulado agresivo — colores, cierre, panel dentro de la ventana, clampeo con la función real, centrado del badge, hover/focus. Safari real no existe en Windows: WebKit es su motor de cascada (documentado honestamente). Mutaciones M1-M7 del widget + documentación retroactiva de las mutaciones de términos de por vida y de la mini guía del aviso | — (pedido explícito y priorizado del médico; «el médico manda») | `6174ba6` |
| 2026-09-08 | Entrega | ¿Publicar v18.8.3 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `6174ba6` en `claude/sf-v18.6.1-fix` (+push, fast-forward sobre `22b3c1e`) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.3 y `gistfile2.txt` con las novedades, `PROBAR_CENTINELA.js` intacto. Verificado **byte a byte**: SHA-256 `524f01260ab895dc61b77e192f1377e94caab74fd0adf0f45dfde94e551fd66e` (3.512.799 bytes) en ambos archivos de script y `e22776e27555993ed35a2bfe44aeae5e2497e280fe03a1fec2684160925262ce` en la descripción — el gist guarda el contenido con saltos LF (normalización de la API de GitHub); la verificación se hizo contra el userscript del worktree normalizado CRLF→LF, idéntico carácter a carácter (54.084 finales de línea) al archivo local, `@version 18.8.3`. Banco **3656/3656 (EXIT=0 real)** con TODO final (bump incluido) + verificación de 3 motores × 3 viewports re-corrida contra el fuente final: 9/9 VERDE | — (pedido explícito del médico, autonomía total delegada) | `6174ba6` |

| 2026-09-08 | CSS T1 | ¿Anexo 5 con variables de tema + blindaje tipográfico de los 9 modales/avisos pegados a body? | **Ejecutado (v18.8.4)**: los 11 colores duros + `font-size:12px` del panel del Anexo 5 (`hcAnexo5Render`) pasan a variables de tema (`--fg/--fg2/--fg3/--c-rojo/--c-ambar/--c-azul/--c-verde/--surface-2/--line/--t-small`), los `!important` inline se conservan literalmente (Regla R) y el acento semántico del borde rojo se mantiene. Auditoría de los 9 contenedores de CLAUDE.md: 252 reglas reales, 0 violaciones de color de texto (las defensas 1 ya estaban); el hueco era el texto suelto SIN clase → nueva regla agrupada `#vgl-… :where(:not([class])){color:inherit !important}` (+1 al censo de suite_25: 669→670). **Verificación Chromium contra el CSS REAL** (patrón chromium_102.py, hoja completa extraída del fuente, Everest agresivo `div,span,p,b,small,label,button{color:magenta !important}` y realista): 26/26 panel en ambos temas + 8 clases con color propio sobreviven al agresivo + 9 sueltos blindados sobreviven al realista. 3 mutaciones verificadas (selector del blindaje quitado, color duro reintroducido, `!important` retirado de `.vgl-labsv-lead`) con filas en INFORME_MUTACIONES. Banco 3658/3658 EXIT=0 real | — (pedido T1 de la sesión: «terminar el pedido 2») | `07ef120` |
| 2026-09-08 | Entrega | ¿Publicar v18.8.4 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `07ef120` en `claude/sf-v18.6.1-fix` (+push, fast-forward sobre `de4a965`) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.4 y `gistfile2.txt` con las novedades, `PROBAR_CENTINELA.js` intacto (16.749 bytes). Verificado **byte a byte**: SHA-256 `62d0445c97d0fe82652bb3c12d5007f8fae88d8e80bdd606cc348532955c50d9` (3.514.033 bytes) en ambos archivos de script y `74f55bdf7de057fad211ab938f1bf5c3bc9ba00354652c36d6452ce44e40736e` (7.523 bytes) en la descripción — verificación contra el userscript del worktree normalizado CRLF→LF (el gist guarda LF por normalización de la API de GitHub), `@version 18.8.4`. Banco **3658/3658 (EXIT=0 real)** con todo final | — (pedido explícito del médico, autonomía total delegada) | `07ef120` |
| 2026-09-08 | Entrega | ¿Sincronizar versiones tras el bump (R5.1)? | **Corregido y publicado**: el banco post-bump cayó en R5.1 («package.json version debe coincidir con @version: esperaba "18.8.4" y obtuvo "18.8.3"») y en suite_75 («versión viva: esperaba "18.8.3" y obtuvo "18.8.4"») — el mismo precedente de la v18.8.0/v18.8.3. `package.json` y el fixture de `tests/suite_75_disco.js` sincronizados a 18.8.4, banco re-corrido **3658/3658 EXIT=0 real**, commit `72143c4` (+push). El gist NO se re-publicó: ya llevaba el userscript final con `@version 18.8.4` (el PATCH fue post-bump) — verificación byte a byte intacta | — (R5.1 exige cuádruple sincronización) | `72143c4` |
| 2026-09-08 | Aviso bloqueo | ¿Cómo lograr «SOLO en HCHealth y UNA sola pestaña» para el aviso de actualización obligatoria? | **Pedido en vivo ejecutado (v18.8.5)**: puerta de URL `_enModuloHCHealth()` — fuera de `/viva/HCHealth/` el asistente sigue bloqueado (regla de proyecto) pero sin cartel, y ninguna pestaña ajena al módulo puede reclamar el turno. **Arriendo entre pestañas**: la ganadora escribe `vgl_aviso_bloqueo_claim` en localStorage (compartido por todas las pestañas de Everest) y lo renueva cada 10 s con un intervalo deliberadamente fuera de `state.timers`; TTL de 120 s generoso a propósito (Chrome estrangula los temporizadores de pestañas ocultas a un disparo por minuto y la dueña debe poder renovar). Perdedoras calladas pero con reloj vigilante para tomar el relevo si la dueña muere. **Linaje en sessionStorage** (`vgl_aviso_bloqueo_linaje`, sembrado con TABID): F5 no convierte a la dueña en «otra instancia» y recupera su arriendo al instante. Guard anti-carrera (relectura tras escritura: gana la última) y **fail-open** (si el storage no responde, el aviso se muestra igual — la regla suprema es bloquear versiones viejas). 4 mutaciones verificadas (puerta de URL, cerradura en sus dos sentidos, linaje) + 4 casos nuevos en suite_17 | — (orden explícita en vivo del médico) | `b5e831f` |
| 2026-09-08 | Entrega | ¿Publicar v18.8.5 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `b5e831f` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.5 y `gistfile2.txt` con las novedades 18.8.5 antepuestas (historial intacto), `PROBAR_CENTINELA.js` sin tocar. Verificado **byte a byte**: SHA-256 `072e7ba29739e0d51186bf376bf521462d657906385e03db26e90104dc110d25` (3.519.213 bytes) en ambos archivos de script y `28fa5c0ddcd9bd14b4f321662ff6b3ed0a8024434cf78f97ddfbbdfce1391d32` (8.887 bytes) en la descripción — verificación contra el userscript del worktree normalizado CRLF→LF (el gist guarda LF por normalización de la API de GitHub), `@version 18.8.5`. Banco **3662/3662 (EXIT=0 real)** con todo final | — (pedido explícito del médico, autonomía total delegada) | `b5e831f` |
| 2026-09-08 | Widget RCV | ¿Cómo minimizar el panel «Próximos exámenes» con estado consistente durante la sesión (pedido en vivo)? | **Minimizar con pastilla de reapertura (v18.8.6)**: botón «—» gemelo del cierre (28×28, WCAG 2.5.8, hover AZUL — minimizar no destruye) en la barra; al pulsarlo el panel baja a una pastilla «▣ Próximos exámenes» fija abajo-izquierda (left:14, bottom:58 — encima de la barra de módulos minimizados, que pasa a left:14 con el panel oculto) y **ningún tick lo resucita** hasta que el médico la pulse (mutación M1: puerta del tick). La pastilla limpia la firma de repintado (mutación M3): el panel vuelve con los datos del paciente ABIERTO en ese momento, jamás los del anterior (anti-cruce). Cerrar con ✕ desarma el minimizado y su pastilla, y sin contexto clínico el minimizado se desarma solo (cero pastillas huérfanas). La pastilla es un `<button>` sin clase pegado a body: su único escudo es la regla por id con `!important` — verificado en Chromium (chromium_186.py) contra Everest simulado agresivo: 21/21 colores/posiciones/hover/foco sobreviven (incluye regresión completa v18.8.2). Suite_102 ampliada a 11/11; censo suite_25 676. Los modales «[IMPRESION DIAGNOSTICA]»/«[CONDUCTA]» (M2/v18.7.0) quedaron VERIFICADOS (suites 97/98/15: 11+6+303 verdes), sin cambios de código — el cableado ya estaba sano | — (pedido explícito en vivo del médico; «estado consistente durante la sesión») | v18.8.6 |
| 2026-09-08 | Entrega | ¿Publicar v18.8.6 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `4deb790` en `claude/sf-v18.6.1-fix` (+push, fast-forward sobre `4865d8d`) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.6 y `gistfile2.txt` con las novedades 18.8.6 antepuestas (historial intacto), `PROBAR_CENTINELA.js` sin tocar. Verificado **byte a byte**: SHA-256 `6b6de4746a4b5d49c7a0d655adc340df967c195b503077c386917db92cd67de1` (3.523.980 bytes) idéntico en local normalizado CRLF→LF y en AMBOS archivos de script remotos, y `6f844f53dd7f5cf5b91ac64b9a01a1ad9fe7027e264c212b7a67dfd5a0f2136f` (10.580 bytes) en la descripción, `@version 18.8.6`. Banco **3666/3666 (EXIT=0 real)** con todo final (el banco cazó y corrigió el registro de tokens de la pastilla antes de publicar: suite_06 roja → fix → verde) | — (pedido explícito del médico, autonomía total delegada) | `4deb790` |
| 2026-09-08 | Notificaciones | ¿Cómo corregir los patrones anómalos del registro de auditoría de notificaciones (orden con CSV real: una sola vez por evento, aislamiento total entre instancias, sin pares dobles, sin minutos negativos)? | **Ejecutado (v18.8.7)**: análisis del CSV → 3 patrones: (a) rachas de `LECTURA_TRAS_RELEVO_SIN_CONFIRMAR` de los mismos pacientes (hasta 7 líneas por hecho) porque el candado de esa rama vive en memoria de cada pestaña; (b) pares `CAMBIO_ESTADO + INGRESO_A_TIEMPO` del mismo paciente en el mismo segundo (colorAndAlert escribía la transición y maybeNotify el evento tipado en el MISMO tick); (c) minutos negativos (-35.3/-43.7) por pacientes confirmados antes de la hora de la cita. **Solución**: candado compartido del día `vgl_audit_unico` consultado dentro de `logEvent` (unicidad por evento+cita+día para los 9 tipos de notificación; para CAMBIO_ESTADO la marca incluye la transición previo→estado, para no amputar transiciones legítimas posteriores); la llegada a sala se registra SOLO por su evento tipado (INGRESO_A_TIEMPO/FRAUDE_EXTEMPORANEO); normalización de `min` ≥ 0 en la bitácora. **Aislamiento**: lo único que viaja por el almacén común es el candado, sin datos del paciente — cada instancia emite sus avisos en su propia ventana o no emite; el candado compartido es lo que IMPIDE que dos instancias emitan lo mismo (R2 y R5 juntos). Suite nueva `suite_103_unicidad_notificaciones.js` (7/7) + 3 mutaciones verificadas (candado, supresión del par doble, normalización del negativo) con filas en INFORME_MUTACIONES. Banco 3673/3673 EXIT=0 real | — (orden explícita del médico, autonomía total delegada) | v18.8.7 |
| 2026-09-08 | Entrega | ¿Publicar v18.8.7 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `1ba896a` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.8.7 y `gistfile2.txt` con las novedades 18.8.7 antepuestas (historial intacto desde 18.8.0), `PROBAR_CENTINELA.js` sin tocar. Verificado **byte a byte**: SHA-256 `d89c8c7ce9f851e909bb01aede1f1544f9a0bb32e6fb2c6982cad1235336fe51` (3.527.883 bytes) idéntico en local normalizado CRLF→LF y en AMBOS archivos de script remotos, y `362dce4245541785b4be25feeb73967123989687420ac368b3396f96e8b5df85` (12.297 bytes) en la descripción, `@version 18.8.7`. Banco **3673/3673 (EXIT=0 real)** con todo final | — (pedido explícito del médico, autonomía total delegada) | `1ba896a` |
| 2026-09-08 | Botón refresh | ¿Cómo implementar el botón de actualización del panel (ORDEN #9: réplica del «Consultar» de «Citas del día» sin recargar la página)? | **Réplica exacta del clic nativo, con UNA sola vía de procesado (v18.8.9)**: el HAR `consultar.har` demuestra que «Consultar» NO recarga — es un GET dinámico a `ObtenerConsultas` (entradas 18/25, sin navegación de documento), así que la réplica jamás es `location.reload()`. Dos ramas: (1) «Citas del día» delante → `_esBotonConsultar`/`_btnConsultarEn` reconocen el botón real (`BUTTON.button-medico` con rótulo Consultar normalizado) y recibe un clic nativo — Angular repinta solo, cero red propia; (2) cualquier otra pantalla → la MISMA llamada aprendida (`API.url`), por la MISMA vía de procesado que el sondeo: el cuerpo del tick se extrajo a `_procesarFuenteAgenda(data, source, now, forzarPintado)`. Sin URL aprendida avisa con honestidad y no inventa ni consulta nada; fallo del servidor → «No se pudo refrescar» sin pisar el último buen snapshot; pulsación reentrante → «Ya se está actualizando» sin duplicar la llamada. Botón `#vgl-refresh` en la cabecera del panel (24×24, SVG, aria-busy giratorio durante la lectura, estilos con `!important` según la regla de paneles pegados a body). La extracción del tick dejó DOS ReferenceError latentes (`enVistaVigilada` y `leader`, variables locales del tick invisibles para `node --check`) cazados por la suite_105 antes de tocar producción. Suite nueva `suite_105_boton_actualizar.js` (13/13) + 3 mutaciones verificadas con filas en INFORME_MUTACIONES | — (pedido explícito del médico, autonomía total delegada) | pendiente (commit v18.8.9) |

| 2026-09-08 | Reglas de refresco (orden A/B) | ¿06:00/12:00 Bogotá es una restricción GENERAL del refresco? | **NO — corrección documental (punto 1 de la orden A/B del médico)**: los horarios 06:00/12:00 Bogotá son **EXCLUSIVOS de la base piloto SEPTIEMBRE1** (`horasRefresco: [6, 12]` de `CONFIG.SP.base`), no una regla de aplicación general del refresco — el sistema de refresco puede operar con otras frecuencias según la base, y el polling de fondo es independiente del horario de descarga de la base piloto. La lógica del refresco programado NO se modifica (restricción del experimento A/B en curso, punto 3 de la misma orden). Corregidos los 3 lugares que lo presentaban como regla general: `docs/TESTS_AB_INFORME_20260908.md` (sección AB-6 y su tabla de monitoreo) y `.deepseek/README.md`; la memoria del proyecto quedó corregida; CLAUDE.md no menciona horarios de refresco (nada que corregir) | — (mandato explícito del médico) | pendiente (commit de esta orden) |

| 2026-09-08 | Reglas de refresco (orden A/B) | (Fila nueva — corrección de la fila anterior) ¿En qué commit quedó la materialización del punto 1 (corrección documental 06:00/12:00)? | **Ejecutado en dos actas**: `903a8f9` (acta Cloudflare con la corrección de la sección AB-6 y su tabla de monitoreo) y `9fdaedb` (corrección de la semántica 06:00/12:00 **exclusiva de la base piloto** en `docs/TESTS_AB_INFORME_20260908.md` y `.deepseek/README.md`, más la corrección de la memoria del proyecto) — la fila anterior quedó con «pendiente» porque el commit aún no existía al escribirla; esta fila la cierra sin editar la anterior (regla: una corrección es una fila nueva) | — (documental) | `9fdaedb` |
| 2026-09-08 | Reglas de refresco (orden A/B) | ¿Qué frecuencias soporta el polling de fondo y cómo se mide la latencia de detección de cupos nuevos (puntos 2-4 de la orden A/B)? | **Ejecutado (v18.9.0, commit `1cc44dc`)**: (2) el polling de fondo acelera los dos niveles de reposo — sin agenda 30→20 s (`SIN_PENDIENTES`) y jornada lejana 20→15 s (`LEJANO`) — con la evaluación técnica en el CHANGELOG sin jerga (coste despreciable: es la misma consulta liviana del botón «Consultar» y solo ocurre con el panel abierto; ahorro: un cupo nuevo se ve hasta 10 s antes; las cadencias críticas 5/8/10 s no cambian); (3) el refresco programado 06:00/12:00 de la base piloto **no se tocó** (restricción del experimento respetada por construcción: las frecuencias del polling y el programador del refresco son vías independientes); (4) sistema de medición de la latencia de detección de cupos: `_cupoLatenciaMedir` compara cada lectura contra `state.lastSnapshot`, cuenta los cupos ausentes en la lectura previa y reporta la ventana como techo de su edad (`cupo.nuevo` + `.total` en ms; `cupo.nuevo.hueco` sin ventana cuando la lectura anterior es un recuerdo de un hueco largo — no-inferencia, la frecuencia no explica esa espera). Verificación: suite nueva `suite_107` (9/9), 2 mutaciones rojo→verde (M-AB1 umbral del hueco invertido — 5 rojos; M-AB2 frecuencias viejas restauradas — 2 rojos) con filas en INFORME_MUTACIONES, banco 3716/3716 EXIT 0, suite_75 50 ok | — (mandato explícito del médico) | `1cc44dc` |
| 2026-09-08 | Entrega | ¿Publicar v18.9.0 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `1cc44dc` (feat v18.9.0) + acta de cierre A/B `900c642` en `claude/sf-v18.6.1-fix` (+push sobre `9fdaedb`) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.9.0 y `gistfile2.txt` con las novedades 18.9.0/18.8.10/18.8.9 antepuestas sobre el historial (la última publicación al gist era la v18.8.7: las versiones públicas intermedias 18.8.9 y 18.8.10 nunca habían llegado al gist y ahora constan en orden), `PROBAR_CENTINELA.js` sin tocar. Verificado **byte a byte**: SHA-256 `cd20fd1b03d27b2658f346d884803102485f34eabbc343c42ccd35cb2f0982e6` (3.557.363 bytes) idéntico en local normalizado CRLF→LF y en AMBOS archivos de script remotos, y `1416487c6799708ed06c2f5afead1fef0c2b52aa1b08a74caaa6c7f4f0915919` (15.147 bytes) en la descripción, `@version 18.9.0`. Banco **3716/3716 (EXIT=0 real)** con todo final | — (pedido explícito del médico, autonomía total delegada) | `1cc44dc` |
| 2026-09-08 | Paquete A/B («APLICA TODAS LAS PROPUESTAS DE LOS TEST A/B») | ¿Qué propuestas del informe se aplican, cuáles quedan acta sin código y se cierra el paquete con un único bump? | **Ejecutado en 4 frentes con commit POR SEPARADO y UN SOLO bump v18.10.0 al cierre**: **AB-1** barridos no críticos fuera del camino del tick con cola diferida (`e17cd57`; suite_108 16/16; M-AB1 12 rojos→verde); **AB-2** reintento transitorio del mismo slot en la redacción IA con backoff + jitter acotado y re-check de cancelación (`e411c1b`; suite_109 10/10 + 2 guardas envejecidas ajustadas en suites 81/57; M-AB2 4 rojos→verde); **AB-3** rage clicks del host con selector ANÓNIMO (sin id, sin texto, sin PHI) + señal azul informativa con anti-spam 30 s (`e8901e8`; suite_110 11/11; M-AB3 4 rojos→verde); **AB-8 + T0-4** el aviso de actualización queda MEDIDO por versión anunciada y el clic de «Actualizar ahora» es la ACCIÓN contada por versión exigida, con bitácora fina local; T0-4: el worker Cloudflare expone ultimaFila y el chequeo nocturno añade el paso de frescura (ROJO >48 h, fail-closed, token leído del propio worker.js) (`2531f0a`; suite_111 8/8; M-AB8 3 rojos→verde; worker desplegado y probado EN VIVO — ultimaFila respondió con la fila real del pipeline, 2,2 h de frescura — y chequeo nocturno completo VERDE 4/4). **AB-4 (autollenado de laboratorios), AB-5 (exclusiones de normalidad fija) y AB-7 (orden/disposición de pestañas del panel): ACTA SIN CÓDIGO** — AB-4/AB-5 son eventos raros (3-15/día en flota) que exigen ventanas de 6-8+ semanas y diccionarios de mapeo, y arriesgan lo que este proyecto no puede arriesgar: AB-4 sugerir una casilla equivocada y AB-5 el **falso normal (prohibido)**; AB-7 se difiere porque el canal de Conducta está suspendido por decisión del propio médico y el refactor del panel sigue abierto — ninguno está maduro para esta entrega y el médico no los pidió en esta orden. **AB-6 (volumen de red de citasdisponibles) quedó FUERA de esta orden**: la reducción de consultas de agenda de v18.9.0 (reposo 20/15 s) y el monitoreo RUM siguen como estaban. **R1-R9 (infraestructura de experimentación: registro de experimento, sorteo de rama, estrato, campo de rama, toggle de apagado, uno por versión, disciplina, cero PHI, baseline A) DOCUMENTADOS SIN INSTALAR**: los frentes aplicados llevan su instrumentación mínima (etiquetas RUM propias, toggles, mutaciones) sin la maquinaria formal de sorteo — que solo se justifica cuando haya experimentos de rama B abiertos de verdad. Cierre: bump cuádruple R5.1 a v18.10.0, CHANGELOG sin jerga, INFORME A/B §7 con el estado por propuesta, filas en INFORME_MUTACIONES, banco completo con EXIT=0 real | — (orden explícita del médico: «APLICA TODAS LAS PROPUESTAS…» con implementación y commit por separado y único bump al cierre) | pendiente (commit de cierre v18.10.0; la publicación con gist byte a byte consta en la fila de Entrega) |
| 2026-09-08 | Entrega | ¿Publicar v18.10.0 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit del cierre `ce27687` (bump único + CHANGELOG + INFORME §7) sobre los 4 frentes (`e17cd57`, `e411c1b`, `e8901e8`, `2531f0a`) en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.10.0 y `gistfile2.txt` con las novedades 18.10.0 antepuestas sobre el historial, `PROBAR_CENTINELA.js` sin tocar (16.749 bytes). Verificado **byte a byte**: SHA-256 `0379FEABE153E8797E4139D7A40667CA663FEFED23C29F4CEE3CE0A03231ADEF` (3.568.179 bytes, 54.940 líneas) idéntico en local normalizado CRLF→LF y en AMBOS archivos de script remotos, y `8F19C176A45707F8167C0A701A7A32E1BF64A41DC4BAAB4C655C4EDC65EE6979` (17.110 bytes) en la descripción, `@version 18.10.0`. Banco **3761/3761 (EXIT=0 real)** con todo final | — (pedido explícito del médico, autonomía total delegada) | `ce27687` |
| 2026-09-08 | ORDEN #8 (UI/UX + sincronización base piloto) — REHECHA tras vaciado del árbol | ¿Qué mostraba mal el aviso del Anexo 5 y cómo se corrige (punto 2/3 del encargo original: reposo del Anexo 5 = sincronización con la hoja real)? | **RAC real en vez de puntos de la meta (v18.11.0)**: el intento original (v18.8.11) se perdió en el vaciado accidental del árbol de trabajo del 08-sep 17:45 sin llegar a commitearse — se rehizo desde cero contra el repo restaurado (`vigilante-agenda-everest-restaurado`), verificando primero que el defecto seguía presente byte a byte. Hallazgo: `makeAnexo5Indexer` nunca leía la col. 33 del libro real («MICROALBU/CREATINURIA1», valor de laboratorio real de la RAC en mg/g); `a5AlertasDe` rotulaba el tramo «RAC» con `m[4][0]` — los PUNTOS de `CUMPLE_MICROALBUMINURIA` (0-25) — como si fueran el resultado: una meta cumplida (25 puntos) podía leerse como «RAC 25 mg/g» (patológico). Fix: nueva columna `cRac`, `v` de 6→7 elementos (`v[6]`=RAC real), `a5AlertasDe` usa `v[6] \|\| 0` — sin valor real, casilla vacía, jamás puntos. Verificado con grep completo que `pymAnexo5`/`a.v`/`a.m[4]` no tienen un segundo consumidor (corrección única). Sincronización con la base piloto: 33/38 columnas (32 previas + esta), 5 restantes documentadas para decisión del médico (clasificación del programa, HDL/triglicéridos/IMC recientes, «Estudiado para ERC») — no implementadas sin su visto bueno. Suite_91 y suite_92 ampliadas (valor real → contexto correcto; meta cumplida SIN valor real → 0, el caso que replica el defecto histórico exacto; render sin tramo «RAC » sin valor). 2 mutaciones verificadas (lectura de columna anulada, reversión al defecto original) con filas en INFORME_MUTACIONES. Bump cuádruple R5.1 a v18.11.0. Puntos 1 y 4 del encargo original (ubicación del módulo de notificaciones; pruebas de usabilidad + informe final) quedan para una entrega documental separada — este commit cierra el punto 2/3 (el hallazgo clínico con riesgo real) | — (delegación; autonomía total delegada) | `ecb7483` |
| 2026-09-08 | Entrega | ¿Publicar v18.11.0 (gist incluido)? | **SÍ, ejecutado** (mandato vigente: «gist + subir a github» al terminar cada entrega): commit `ecb7483` en `claude/sf-v18.6.1-fix` (+push sobre `b1ab189`) y Gist `d231aab6f54de51a5c472b392aac1b91` actualizado — `gistfile1.txt` + `vigilante_agenda.user.js` con la v18.11.0 y `gistfile2.txt` con las novedades 18.11.0 antepuestas sobre el historial, `PROBAR_CENTINELA.js` sin tocar (16.749 bytes). Verificado **byte a byte**: SHA-256 `4d1ebf3c9eb029b0a664f66e8fa322e3039ba172d979f06543f4d91148640d81` (3.569.698 bytes) idéntico en local normalizado CRLF→LF y en AMBOS archivos de script remotos, y `d01edacc80ee4248992e98d2adce2745b0fc77682799bee1d191dc88d65d7a45` (18.561 bytes) en la descripción, `@version 18.11.0`. Banco **3762/3762 (EXIT=0 real)** con todo final | — (pedido explícito del médico, autonomía total delegada) | `ecb7483` |


## Refactor integral (SUPERPROMPT_ORQUESTADOR_REFACTOR_INTEGRAL_20260908.md — orden del 08-sep-2026, fases A→E con puertas)

| Fecha | Fase | Pregunta corta | Decisión | Default aplicado | Commit |
|---|---|---|---|---|---|
| 2026-09-08 | Refactor FASE A | ¿Cómo lograr la actualización inmediata del PANEL DEL PACIENTE al escribir el médico (antes esperaba su vigilante de 20 s)? | **Sync en tiempo real (v18.8.8 FASE A)**: los listeners de captura de la compuerta distinguen la ESCRITURA real (`input`/`change`) de la navegación (`click`) y programan un flush con debounce de 700 ms que adelanta al vigilante del panel abierto (slot `_vglPanelVigilanteFn`, extraído a `_vigilarPanel`). El panel abierto se refleja en <1 s mientras el médico teclea; el vigilante de 20 s queda de respaldo. Suite nueva `suite_104_sync_escritura.js` (5/5) + 2 mutaciones verificadas (disparo del flush, desregistro al cerrar) con filas en INFORME_MUTACIONES | — (delegación; autonomía delegada) | `a019289` (FASE A) |
| 2026-09-08 | Refactor FASE B | ¿Dónde vive el programa del paciente y su cita sugerida, si las cinco pestañas del panel lo necesitan? | **Cabecera del panel (v18.8.8 FASE B)**: el chip de programa (B.2 — rector + desplazados + «rige vigencias y órdenes», visible en las 5 pestañas antes de Exámenes/bento) y el bloque de cita sugerida (B.4 — la toma ftl/motivo y el control del MISMO plan que consume el agendador) se rellenan en CADA pintado del panel desde `mtrTableroClinico` (`d.fechas`). Botón «Agendar» en la cabecera que SOLO abre `openAgendamientoModal` pre-cargado — nunca agenda por su cuenta (el médico confirma todo). Sin fechas del motor → bloque vacío: jamás se inventa una cita. Reutiliza clases ya blindadas contra el CSS de Everest; **cero reglas de color nuevas → nada que verificar en Chromium** | — | pendiente (commit FASE B al cerrar el segmento) |
| 2026-09-08 | Refactor FASE B | ¿S5: dónde ocultar el documento del paciente tras la auditoría de exposición? | **Máscara «···» + últimos 4 en el encabezado del agendador (v18.8.8 FASE B)**: `#vgl-agm-doc` nace mascarado con `_vglHcMascara(apt.doc_id)` y un clic explícito del médico lo revela mientras el modal siga abierto (telemetría `fn.agendar.revelarDoc`). Los demás puntos que muestran documento entero son DOM de Everest o ya tenían su propia defensa: no se tocan | — | pendiente (commit FASE B) |
| 2026-09-08 | Refactor FASE B | ¿B.3: exámenes gestionables desde el panel? | **NO implementado — documentado**: «gestionar» exámenes en Everest es escribirlos, y el canal de Conducta está SUSPENDIDO por decisión del propio médico (v18.0.7; `MTR_ANALITOS_PAQUETE_CONDUCTA` quedó vacía de propósito). Un botón de escritura en el panel contradeciría la suspensión. Queda registrado como posible futura vía cuando el médico reabra el canal | — | pendiente (commit FASE B) |
| 2026-09-08 | Refactor FASE B | ¿U1: botón que abra la ficha-paciente de Everest desde el panel? | **YA EXISTE (documentado, cero código)**: la fila «En sala»/la tarjeta ya abren la ficha; el panel nace dentro del flujo de consulta abierta. La puerta §0.1 («YA EXISTE» para pedidos cumplidos) aplica | — | pendiente (commit FASE B) |
| 2026-09-08 | Refactor FASE B | ¿Por qué el cronómetro del «Buscando…» de exámenes usa `setTimeout` encadenado en vez de `setInterval`? | **Fix de suites estáticas (v18.8.8 FASE B, R4/A3)**: el `setInterval` + comentario largo original rompía suite_15 (ventana estática de 1400 chars desde el ancla del botón hasta `_lecturaAtheneaFallo`) y suite_63 (el primer `setInterval` de la ventana del panel debe ser el watchdog de 20 s, DESPUÉS de la reconciliación). Cronómetro con `setTimeout` encadenado + comentarios compactos (4 líneas): mismo feedback «Buscando… N s», `clearTimeout` en todos los caminos, sin literal `setInterval(` en el bloque. Mutaciones M-B1 (slot de programa) y M-B2 (máscara S5) verificadas con filas en INFORME_MUTACIONES; suite_67 ampliada 47→51 | — | pendiente (commit FASE B) |
| 2026-09-08 | Refactor FASE B | (ENTREGA FASE B) | Commit `9c80ae6` + push (feb1375..9c80ae6): suite_67 51 ok, 2 mutaciones verificadas (M-B1 slot de programa, M-B2 máscara S5) con filas en INFORME_MUTACIONES, banco completo 3682/3682 EXIT=0 real, acta de las 5 decisiones FASE B en esta tabla | — | `9c80ae6` |
| 2026-09-08 | Refactor FASE C | ¿Quién es el PRIMARIO de la escalera de IA cuando el médico tiene más de una clave? | **Motor de IA preferido (v18.8.8 FASE C, C.1)**: select «Motor de IA preferido» en Ajustes → modo programador (`vgl_ia_pref`, sin PHI) con 4 opciones — «Automático» (default del sistema: DeepSeek V4 Flash si hay clave y, si no, z.ai — el orden histórico byte a byte), «DeepSeek V4 Flash», «z.ai (GLM-5.3)» y «Google Gemini». El proveedor preferido recibe la llamada ÚNICA del slot 0 SI tiene clave; sin la clave del preferido, la escalera cae al siguiente disponible (fail-open). Con Gemini preferido (y su clave), Gemini corre su rotación completa de modelos y no hay respaldo; con primario deepseek/z.ai el respaldo Gemini entra UNA vez si el primario falla por algo que merece rotar (cuota/saturación/no disponible). Valor ausente o inválido → «auto». El cambio del select persiste al momento (listener `change`, mismo patrón que las claves) | — | pendiente (commit FASE C) |
| 2026-09-08 | Refactor FASE C | ¿Gemini puede recibir llamadas sin que el médico le haya dado su clave (gate C.2)? | **Gate real en la escalera (v18.8.8 FASE C)**: sin clave Gemini, la preferencia «gemini» NO llama a Gemini — el slot 0 cae al siguiente disponible (deepseek/z.ai) y `maxIntentos` se ajusta al primario efectivo. Verificado por caso nuevo en suite_99 («preferencia gemini sin su clave cae al siguiente disponible — cero llamadas a Gemini») + mutación M-C1 (romper el fallback → el caso se pone rojo). El campo de clave Gemini y el nuevo select viven en la sección técnica del grupo `vgl-grp-tec`, que solo se pinta en modo programador | — | pendiente (commit FASE C) |
| 2026-09-08 | Refactor FASE C | ¿El redactor recalcula el JSON v68 al armar el prompt (C.3)? | **NO — verificado y fijado (v18.8.8 FASE C, C.3)**: `mtrRedaccionPrompt` recibe el JSON v68 YA ARMADO por `o.jsonV68` y el sello por `o.selloContexto` (el llamador los trae; la función no vuelve a reunir datos — el bloque viaja con la orden «fuente de verdad, no recalcules»). Temperatura 0.2 fija y `max_tokens` 8192 ya viven en el contrato del proveedor deepseek (DS·1 los fija byte a byte). La función solo devuelve `{system, user}`: el prompt no se guarda en ningún almacén (el guardado queda solo para diagnóstico redactado, fuera de esta función) | — | pendiente (commit FASE C) |
| 2026-09-08 | ORDEN #10 (auditoría extemporáneas) | ¿Cómo responde el CSV de reclamación «quién confirmó fuera de plazo» si ninguna fila llevaba usuario? | **Columna «Usuario» en la bitácora local (v18.8.10)**: `logEvent` adjunta a cada fila el usuario de la SESIÓN (`state.activeDoctor`, la identidad del login de Everest — la única que, por decisión del 02-sep, identifica a quien firma) y `exportAudit` gana la columna al final sin mover las históricas. Sin sesión capturada → celda vacía, jamás un responsable inventado. La telemetría del tablero sigue anónima por diseño. La ventana de 6 min quedó auditada sin cambios (ya rígida por construcción) con suite_106 (5/5) que fija los bordes exactos +5,9/+6,0/+6,1, y 2 mutaciones verificadas. Informe íntegro redactado (PHI) en `docs/AUDITORIA_ORDEN10.md` | — (delegación; autonomía delegada) | `8e60466` (commit + push; corrige el «pendiente» de la fila de la ventana anterior) |
| 2026-09-08 | ORDEN #7 (telemetría) | ¿Cómo migrar la telemetría fuera de Google Apps Script? | **Réplica Cloudflare Workers + D1 entregada en `REPLICA_TELEMETRIA/`** (supuesto de alcance documentado: el enunciado literal de la ORDEN #7 no está en el repo — sin acta ni carpeta previa — y el alcance fijado en sesión es «réplica contractual del tablero»). `worker.js` replica Codigo.gs + VersionCheck.gs: acuses texto plano exactos `ok`/`dup`/`no`/`err` (lista blanca del cliente), 8 eventos con saneos verificados campo por campo contra Codigo.gs L273-344 (claves re-filtradas, total recalculado, barrera PHI `_sinDigitosLargos`, tipos texto/número, longitudes), GET listaAcceso (perfiles/blocklist/uid sintético/versión djb2) y GET /vcheck. **Mejoras deliberadas**: dedup PERMANENTE por lote en D1 (el GAS solo 6 h por caché; la duplicación histórica del 54 % muere de raíz) y `err` sin lote quemado con compensación activa. Banquete local `test_replica.mjs` **51/51 EXIT=0 real** (SQLite real en memoria, fuera del runner del repo) + **mutaciones M1/M2 verificadas** (dedup y compensación → sus casos se ponen rojos). **Cableado de la flota (TABLERO.url, regex de repDiagnostico, versionCheckUrl, @connect) es post-despliegue y decisión del médico** — el GAS sigue en vivo y la flota no nota nada | — (delegación; autonomía delegada; supuesto: sin enunciado literal en el repo) | commit ORDEN #7 de hoy |
| 2026-09-08 | ORDEN #7 (telemetría) | ¿La réplica Cloudflare quedó desplegada? | **Despliegue integral ejecutado el 2026-09-08** (tarea encomendada por el médico; autonomía delegada): sesión OAuth de wrangler ya existente en la máquina; la D1 `vigilante-telemetria` (uuid `5ec80edc-7cad-48b3-baa4-2ba3f721cb9b`) fue adoptada con su esquema completo (11 tablas, dedup UNIQUE permanente de lotes) y el worker quedó vivo en `https://vigilante-telemetria.bpalencia27.workers.dev` — contrato probado en vivo (acuses `ok`/`dup`/`no`, GET listaAcceso y /vcheck, seguridad fail-closed sin token). El padrón de acceso del GAS se replicó a la tabla `acceso` (5 COMPLETO + 3 LABORATORIOS + 0 blocklist; paridad de contenido exacta contra el GAS). El hash djb2 de `version` difiere entre backends por construcción (concatenación distinta) y queda documentado como no bloqueante: cada backend es autoconsistente y sensible a cambios. El cableado de la flota (TABLERO.url, repDiagnostico, versionCheckUrl, @connect) NO se tocó — es decisión del médico y está documentado en REPLICA_TELEMETRIA/README.md §4; el GAS sigue en vivo y la flota no nota nada | — (delegación; autonomía delegada) | `f70c05c` (commit + push) |

## Supuestos activos (pendientes de confirmación del propietario)

- **F2/regla (b)**: «estudios pendientes» aplicará SOLO a metas con columna de fecha de
  toma en el libro (glicemia, LDL, HDL, triglicéridos, microalbuminuria, HbA1c) + EKG;
  TA/IMC/circunferencia se miden en consulta y NO se marcan como estudio pendiente
  (sus puntos viajan al contexto). Es interpretación del mandato §O2.2(b) — confirmar.

- **Ninguno bloqueante** en v18.6.0: las tres preguntas de diseño fueron respondidas en vivo.
- Menor, documentado en la auditoría (H6): el riesgo de chip obsoleto por filas multi-día
  se aceptó con la decisión «todas las filas»; si en consultorio se ve un chip ya resuelto,
  la revisión es: ¿actualizaron la fila del paciente en el libro? ¿pedir filtro por
  `Fecha_Cita` máxima por documento?
