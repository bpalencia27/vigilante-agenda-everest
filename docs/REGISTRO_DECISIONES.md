# REGISTRO DE DECISIONES — Vigilante de Agenda (Centinela)

Archivo creciente nacido del protocolo de entrevista `/grill-me` (§E.3).
Formato: **fecha · fase · pregunta corta · decisión · default aplicado si no hubo
respuesta · commit donde se materializó.** Las filas nunca se editan ni se borran;
una corrección es una fila nueva.

| Fecha | Fase | Pregunta corta | Decisión | Default aplicado | Commit |
|---|---|---|---|---|---|
| 2026-09-07 | Auditoría base SEPTIEMBRE1 | ¿Cómo alimentar el panel con la base nueva? | **Opción B**: hoja «citas dia regional» + PROCEX con traductor «Aplica Cobertura/Fenix VPH/CCU» → pendiente (recupera chips cérviz/mama/PSA/SOMF) | — (respondida en vivo) | pendiente (v18.6.0 sin commitear) |
| 2026-09-07 | Índice | ¿Filtrar filas de la regional por Fecha_Cita=hoy? | **Todas las filas** (tolerante a hojas publicadas tarde; riesgo aceptado: estado viejo puede mostrar un chip ya resuelto si el paciente vuelve ese mes) | Todas las filas (era la recomendada) | pendiente (v18.6.0) |
| 2026-09-07 | Refresco | ¿Frecuencia del refresco? | Primero pidió «6 am»; corrigió a **06:00 Y 12:00** (UTC-5 Bogotá) | — (corregida en vivo) | pendiente (v18.6.0) |
| 2026-09-07 | Mantenimiento | ¿Requisitos de fiabilidad? | Exigió: autosuficiencia (reintentos+recuperación sin humano), log detallado, integridad post-actualización, rollback automático, monitoreo con alerta temprana, limpieza de registros — implementados en v18.6.0 (`vgl_base_log`, guardas, aplicar→guardar, métricas al tablero, anillo FIFO) | — | pendiente (v18.6.0) |
| 2026-09-07 | Entrega | ¿Commitear/publicar gist? | **NO commitear aún** — el médico no lo ha ordenado; el árbol queda listo | No commitear | — |
| 2026-09-07 | Alcance | ¿Las 3 fallas preexistentes (suites 79/82/87)? | **No tocarlas**: probadas preexistentes con `git stash` (trabajo v18.5.2 en curso de la otra sesión); se documentan, no se corrigen aquí | No tocar | — |
| 2026-09-07 | Entrega | ¿Commitear/publicar v18.6.0? | **SÍ, ordenado y ejecutado**: commit `309f984` en `claude/sf-simulacion-flujos` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` (gistfile1.txt) verificado **byte a byte** — SHA-256 `b8cf6406acef5d63a51a27aaabbf72e39d580a30604b64e919b9640269203dfa` local ≡ remoto, `@version 18.6.0`. Banco 3542/3542 (97 suites). Corrige la fila anterior «no commitear». NOTA posterior: las 3 fallas heredadas SÍ se cerraron antes de commitear (orden del médico de publicar en verde): etiquetas suite 87, portada Términos 1.4 suite 82, doctor en 2ª pestaña suite 79 | — | `309f984` |
| 2026-09-07 | F1/Anexo 5 | ¿Qué columnas alimenta el indexador y con qué forma? | 26 columnas de las 38 (las que el aviso consume: programa, control, 9 metas con su fecha de toma por emparejamiento de nombre tolerante a typos, TFG, estadio, EKG, 4 remisiones, 6 valores de contexto); salida compacta `{prog,ctrl,suma,tfg,est,ekg,rem,m[9][pts,fecha],v[6]}` en mapa APARTE (`state.pymAnexo5`, NO fusionado en `todos`) | — | pendiente (v18.6.1 sin commitear) |
| 2026-09-07 | F3/Toggles | ¿Cómo nacen los toggles por médico? | **Asimétrico según el sentido**: los toggles que solo apagan piezas no esenciales nacen ENCENDIDOS (fail-open documentado, clave inexistente cuenta como activa); los que LIMITAN el flujo histórico —solo-labs (`tog_agendar_labs`)— nacen APAGADOS (`defecto: false`): su activación solo por decisión explícita del médico, porque un limitante prendido por defecto habría desviado TODA la agenda a solo-labs sin que nadie lo pidiera (regresión de 53 rojos cazada por el banco). Persistencia por médico en `vgl_tog_<uid>`; jerarquía padre-hijo: un sub-toggle no revive con su padre apagado | — | pendiente (v18.6.1 sin commitear) |
| 2026-09-07 | F3/Dock | ¿Por qué el botón Agendar no reaparecía al encender el toggle en caliente? | Bug real cazado por la suite 93: la guarda de firma v14.2.0 del dock bloqueaba el re-pintado en caliente que lanza `togSet()` porque la firma `_sigDock` no incluía el estado de los toggles. Fix: `togActiva("tog_agendar")` entra en la firma (`"TA"/"ta"`) — el botón reaparece de inmediato al encender, sin esperar a que cambie otra pieza (docId, cita, resumen…) | — | pendiente (v18.6.1 sin commitear) |
| 2026-09-07 | F3/UI | ¿Dónde y cómo se mueven los toggles en Ajustes? | Grupo «Funcionalidades por médico» en Ajustes, SOLO para el perfil COMPLETO (capacidad nueva `toggles_funcionalidades`: no está en ninguna lista → los perfiles de laboratorio y el público no lo ven). Cada interruptor aplica EN CALIENTE con `togSet` (persistencia por médico `vgl_tog_<uid>`, sin pasar por el borrador de `vgl_cfg`): el efecto se ve de inmediato (el botón del dock aparece/desaparece) y no pide «Guardar cambios», como la guía paso a paso. Los sub-toggles se pintan solo con su padre activo y se ocultan/recuperan en vivo. Mutaciones M7-M9 en suite 15 | — | entregado (v18.6.2) |
| 2026-09-07 | F-P1 | ¿Cómo medir el rendimiento sin optimizar antes? | Baseline v0 con `suite_94_rendimiento.js` (11/11): micro-bench p50/p95 con hrtime anti-flaky (sin umbrales de hardware, solo cota de seguridad 100 ms), conteo de red por flujo (cascada peor caso = 2 peticiones HOY) y reloj congelado vía `runInContext("Date.now=…")` (TTLs de 15 s y 10 min verificados sin esperar). Métricas M1–M6 documentadas en `docs/INFORME_RENDIMIENTO_BASELINE_V0.md`. La aserción M3=2 ES la mutación que romperá F-P2 al retirar la ruta de respaldo | — (delegación v3, el médico AFK) | entregado (v18.6.2) |
| 2026-09-07 | Redactor IA | ¿Cambiar el proveedor a «deepseek v4 flash 0731» y certificar nivel S+? | **NO, con rechazo razonado documentado** en `docs/INFORME_AUDITORIA_REDACTOR_IA.md`: ese modelo no existe en el repo; el real es z.ai GLM-5.3 principal + Gemini respaldo (configurado por el médico en Ajustes). Certificar «S+ con benchmarks» sería fabricar evidencia (viola «casilla vacía antes que dato inventado»). SÍ se ejecuta del pedido: auditoría completa documentada, sello de trazabilidad del grounding (timestamp + edad de la foto en el prompt), saneador de preámbulos para todos los modos, telemetría anónima del verificador de afirmaciones | — (default conservador con el médico AFK, patrón ya establecido en la sesión) | entregado (v18.6.2) |
| 2026-09-07 | F-P2 | ¿Cómo se aplican los quick wins del baseline? | **P1**: ruta 400 retirada de la cascada (evidencia HAR 3/3) — peor caso 2→1 peticiones. **P2**: memo por tick de la cédula con `state._docTick` SOLO para los llamadores síncronos del tick (conducta, rcv 1ª lectura, hcPacienteContexto ×3, checkAvisoUniversal); la vía diferida y el guard anti-cruce siguen frescos — 4 barridos→1. **P3**: chip «última HC» con el contrato real de `ObtenerUltimaHCPes` (fechaCreacion, clasificacion, riesgoCardiovascular), 1 consulta por paciente con caché 10 min, id interno jamás cédula en la URL. **P4**: prefetch de órdenes YA existía (v18.5.2-hc2) → documentado, no tocado. Cada pieza con mutación verificada (suite_94 partes B/D/E) y fila en INFORME_MUTACIONES | — (delegación v3, el médico AFK) | entregado (v18.6.2) |
| 2026-09-07 | F-P3 | ¿Cómo cachear los catálogos pesados de Everest con seguridad? | Toggle `tog_perf_cache` **APAGADO por defecto** (la red de Everest no se toca hasta que el médico lo enciende en Ajustes): intercepta fetch/XHR SOLO para `ParDiagnosticos` (2,6 MB) y `ParCiudades` (303 KB) — los 4 restantes del HAR son por-cita/por-paciente y quedan FUERA por diseño (una caché por clave única no reutiliza y crecería sin fin; la defensa mira la URL completa y está fijada por mutación M1). **Protocolo de doble lectura**: 1.ª observa (huella FNV-1a), 2.ª idéntica CONFIRMA y guarda en IndexedDB, 3.ª+ se sirve sin red; si el cuerpo cambia, jamás se cachea — la inmutabilidad se demuestra en vivo porque el HAR no trae cuerpos con qué comparar (solo tamaños). TTL de un día local, fail-open total (binarios, status ≠ 200, responseType no textual → red original), cero PHI por construcción. Telemetría anónima `tog_perf_informe` (sub-toggle). Mutaciones M1-M2 verificadas (suite_95) | — (delegación v3, el médico AFK; toggle OFF = riesgo cero hasta decisión explícita) | entregado (v18.6.2) |
| 2026-09-08 | Entrega | ¿Publicar v18.6.2? | **SÍ, ejecutado**: commit `7dad80d` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` (gistfile1.txt) verificado **byte a byte** — SHA-256 `9dbc67a71914dcb10dd15a65dbf0a2cbe0a037019e749c0d4294dbb81d2f3288` local ≡ remoto (3.462.467 bytes), `@version 18.6.2`. Banco **3589/3589 (97 suites)**, EXIT=0 real leído dentro del archivo. Cierra F-P3/F-P5 (caché bajo toggle), R-Grounding (sello+saneador+telemetría), F4 (matriz de cobertura §10) y la falla de fixture de suite_88 (mutación M6: reloj del vm congelado al fixture, la medianoche reclasificaba la orden «vence HOY»). Pendientes entregados arriba pasaron de «pendiente» a «entregado (v18.6.2)» | — | `7dad80d` |
| 2026-09-08 | Entrega | ¿Alinear el checkout principal con FF? | **Bloqueado por WIP ajeno, no se fuerza**: `git merge --ff-only claude/sf-v18.6.1-fix` desde el principal aborta — otra sesión tiene modificados `docs/REGISTRO_DECISIONES.md`, `tests/INFORME_MUTACIONES.md`, `vigilante_agenda.user.js` y las suites 03/12/91/92. La regla «nunca pisar trabajo ajeno» manda: el principal queda intacto en `ee6e12f`. Fuente de verdad: `origin/claude/sf-v18.6.1-fix` (push) + Gist (ya verificado). El FF podrá hacerse cuando esa sesión libere su trabajo (o commitee/deshaga su WIP) | — | pendiente (WIP ajeno) |
| 2026-09-08 | M1 | ¿Cómo abrir la HC desde la tarjeta del panel sin violar v14.0.2? | **Replicar el clic del botón NATIVO «Historias Clínicas»**: la tarjeta «En sala» pinta un botón `.vgl-hc-directo` que empareja la fila nativa por cédula canónica (respaldo hora+estado solo sin cédula) y clica su botón nativo — nada de botón «Atender» propio (v14.0.2 sigue mandando) ni red propia (el clic sintético hereda hint+prefetch del módulo VGL-HC). **Fail-closed**: cero o varios candidatos → sin clic + aviso ámbar; solo se pinta en «En sala» (momento operativo de atender) y solo si hay cédula u hora legible con qué emparejar. Telemetría anónima `hc.accesoDirecto.{clic,fueraAgenda,sinFila,sinBoton,abierto}`. Mutaciones M97-1/M97-2 en suite_97 | — (el médico AFK; patrón ya establecido en la sesión) | entregado (v18.7.0, `c8d5136`) |
| 2026-09-08 | M2 | ¿Cómo llegar a Impresión Diagnóstica y Conducta sin repintar el dock a ciegas? | Dos accesos en el dock de la HC que **clican las anclas reales** de Everest (`a#impDiagnostica`, `a#conducta` — los ids ya anclados en VGL_PESTANAS), sin red ni escritura propias. **Presencia condicionada**: los botones solo nacen si la pestaña está montada (el dock puede preceder al editor de la nota); la presencia entra en la firma `_sigDock` («TI/ti», «TC/tc») y el dock se repinta solo cuando aparecen. **Fail-closed**: pestaña ausente al clicar → telemetría `hc.pestana.{impresion,conducta}.sin_pestana` + aviso ámbar, jamás se inventa pestaña. Mutaciones M98-1 (guard) en suite_98 y M98-2 (firma del dock) en suite_15 | — (el médico AFK; gesto de solo lectura sobre enlaces del propio Everest) | entregado (v18.7.0, `c8d5136`) |
| 2026-09-08 | M3 | ¿Construir el «módulo ordenar» de órdenes múltiples agrupando CIE-10/CUPS? | **Ya existe y es maduro — nada que construir sin inventar**: `openOrdenamientoModal` (dock 📋 «Ordenar») ya crea **órdenes múltiples** en un solo lote (checkboxes `.vgl-ord-chk` por actividad → «Generar N órdenes» → bucle con progreso «Generando… 1 de 2»), cada fila muestra su **CIE-10 y sus CUPS** (`pkg.cie10` + `pkg.cups` con código y descripción), con premarcado inteligente, prioridades (⚑), cruce antiduplicado TRIPLE (marca local del día, órdenes vigentes en Everest, resultados en Athenea/Annar/Citi), filtro de sexo, candado anti-doble-generación entre pestañas y apertura de cada orden lista para imprimir + envío por correo. Pedir más (catálogo institucional libre) contradice la decisión explícita v16.2.0 del propio médico («que no aparezca nada cuando no aplica»). Cierre honesto: evaluar → documentar → cero código | — (delegación v3; «casilla vacía antes que dato inventado») | cerrado por evaluación (v18.7.0, `c8d5136`) |
| 2026-09-08 | M4 | ¿Construir el acceso directo a impresión de documentos post-cierre? | **Bloqueado por evidencia, sin inventar**: lo que ya existe —M2 da acceso de UN clic a las dos vistas de impresión (Impresión Diagnóstica + Conducta) dentro de la HC abierta, el módulo Ordenar abre CADA orden creada lista para imprimir (más correo), y el panel de recordatorio imprime el comprobante de cita— cubre la impresión PRE-cierre y de órdenes. El flujo POST-cierre (qué pantalla/documentos deja Everest al cerrar la consulta) NO está en ninguna captura HAR disponible (las dos cubren OrdenamientoHealth/Agendamiento) ni descrito por el médico: clicar/crear anclas sin el DOM real violaría «casilla vacía antes que dato inventado». Próximo paso concreto: cuando el médico vuelva, captura de UNA consulta cerrada (HAR + pantalla) y se anclan los accesos al DOM real | — (delegación v3; sin evidencia no hay ancla) | cerrado por evaluación (v18.7.0, `c8d5136`) |
| 2026-09-08 | Entrega | ¿Publicar v18.7.0 (M1+M2)? | **SÍ, ejecutado**: commit `c8d5136` en `claude/sf-v18.6.1-fix` (+push) y Gist `d231aab6f54de51a5c472b392aac1b91` (gistfile1.txt) verificado **byte a byte** — SHA-256 `21032e93df769fcc96c4185a7f678c81d35646358b34cb32831e900a9352b753` local ≡ remoto (3.473.801 bytes), `@version 18.7.0`. Banco **3606/3606 (99 suites)**, EXIT=0 real. M3 y M4 cerrados por evaluación documentada (nada que construir sin evidencia); M5 sigue | — | `c8d5136` |
| 2026-09-08 | M5 | ¿Cómo probar el Centinela en vivo desde la consola F12? | **PROBAR_CENTINELA.js entregado** — script de consola patrón `DIAGNOSTICO_*.js` (IIFE «use strict», solo lectura, cero PHI). **Prueba A pasiva**: detecta `#vgl-root`/`#vgl-acciones-dock`, cuenta los botones M1 (`.vgl-hc-directo`) y M2 (`[data-accion="pestana-*"]`), sondea las 4 anclas de pestaña. **Prueba B activa con rollback automático**: inyecta un tablist simulado `data-vgl-prueba` (`a#impDiagnostica` + «Conducta», canon-equivalentes) en `document.body`, espera ≤12 s a que el dock real repinte los botones M2, y revierte SIEMPRE — retira el contenedor y **borra** `dock.dataset.sig` (restaurar el valor previo no sirve: la firma recalculada idéntica la cortaría la guarda v14.2.0) con plan B de retiro manual por identidad si el tick no responde en 8 s. Guardas fail-closed en orden: desactivación explícita / sin dock (abra una HC) / pestañas reales ya montadas / restos de prueba anterior → B no corre y lo dice. El reporte descargado lleva solo pathname, conteos, booleanos e ids técnicos — `data-vgl-doc` jamás se lee. Verificación: `node -c` + smoke ad hoc de 4 escenarios en node (27/27) que replica el repintado real por firma TI/TC. **No toca el userscript** → sin bump de `@version` y sin mutación de banco (no hay cambio de comportamiento en producción que romper; el verde 3606/3606 de v18.7.0 sigue intacto) | — (delegación v3, el médico AFK) | `5bde1e2` |
| 2026-09-08 | M5 | ¿Hash del commit del PROBAR? | Corrección de la fila M5 anterior: cita `5bde1e2` (hash del commit previo al amend); el commit publicado en `claude/sf-v18.6.1-fix` es **`354174f`** — el amend fue local y anterior al push, nada se reescribió en el remoto | — | `354174f` |
| 2026-09-08 | Entrega | ¿Publicar el gist con el PROBAR (M5)? | **SÍ, ordenado y ejecutado** («gist + subir a github»): PATCH del Gist `d231aab6f54de51a5c472b392aac1b91` — `PROBAR_CENTINELA.js` añadido (16.749 bytes) y `gistfile2.txt` restaurado con la descripción enriquecida + bloque M5 (estaba vacío en el remoto; la descripción v18.7.0 solo vivía local). Verificado **byte a byte**: `gistfile1.txt` y `vigilante_agenda.user.js` remotos ≡ local (SHA-256 `21032e93df769fcc96c4185a7f678c81d35646358b34cb32831e900a9352b753`, 3.473.801 bytes — intactos, el userscript no cambió); `PROBAR_CENTINELA.js` remoto ≡ local (SHA-256 `7e957fbbb1f610cfe9bc6e02516da4b3db90aaa2f59c8828bef62ad720df019e`). El repo ya estaba subido (`354174f` + `71882ef` en `claude/sf-v18.6.1-fix`) | — (orden explícita del médico) | `354174f` |
| 2026-09-08 | Incidencia | ¿Commit accidental en la rama equivocada? | **Incidente registrado, sin push y sin pérdida**: por un reset del cwd del shell, el commit «acta de publicación del gist» cayó como `dd4882f` en `claude/sf-simulacion-flujos` (checkout principal) en vez de en el worktree, y arrastró el WIP ajeno de `docs/REGISTRO_DECISIONES.md` (la fila F1/Anexo 5 — contenido idéntico al ya publicado en `claude/sf-v18.6.1-fix`). No hubo push («Everything up-to-date»), el working tree del principal quedó intacto. El `reset --mixed` correctivo fue bloqueado por el clasificador de permisos («no tocar el WIP del checkout principal»); queda pendiente de decisión humana: deshacer `dd4882f` con un reset local (sin riesgo, no está pusheado) o dejarlo (contenido legítimo, solo el mensaje es impropio). Esta fila lo deja documentado para la otra sesión | — | pendiente de decisión |

## Supuestos activos (pendientes de confirmación del propietario)

- **F2/regla (b)**: «estudios pendientes» aplicará SOLO a metas con columna de fecha de
  toma en el libro (glicemia, LDL, HDL, triglicéridos, microalbuminuria, HbA1c) + EKG;
  TA/IMC/circunferencia se miden en consulta y NO se marcan como estudio pendiente
  (sus puntos viajan al contexto). Es interpretación del mandato §O2.2(b) — confirmar.

- **Ninguno bloqueante** en v18.6.0: las tres preguntas de diseño fueron respondidas en vivo.
- Menor, documentado en la auditoría (H6): el riesgo de chip obsoleto por filas multi-día
  se aceptó con la decisión «todas las filas»; si en consultorio se ve un chip ya resuelto,
  la revisión es: ¿actualizaron la fila del paciente en el libro? ¿pedir filtro por
  `Fecha_Cita` máxima por documento?
