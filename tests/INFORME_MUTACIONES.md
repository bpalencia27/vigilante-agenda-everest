# Informe de Auditoría de Mutaciones

Este documento reporta los resultados de la auditoría de mutaciones manual sobre `vigilante_agenda.user.js` usando un banco que muestra 457/457 (456/457 pasan, 1 de Excel falla basal). A continuación se listan 40+ mutaciones aplicadas sobre una copia temporal, indicando cuáles sobrevivieron (revelando aserciones débiles en las suites).

| Línea | Mutación Aplicada | ¿Sobrevivió? | Aserción Faltante (si sobrevivió) |
|---|---|---|---|
| 4335 | Vaciar la función `_instalarCazaErrores` | No | `Telemetría de uso del panel (v12.5) -> _instalarCazaErrores: intercepta errores y rechazos solo del script propio` |
| 932 | `if (!resultDate && !_diagLabFechaLogged) {` &rarr; `if (resultDate && !_diagLabFechaLogged) {` | Sí | Falta asertar que el flag de una-sola-vez `_diagLabFechaLogged` evita desbordar la consola de logs repetidamente. |
| 2478 | `} catch (e) { return false; }` &rarr; `} catch (e) { return true; }` | Sí | Falta aserción que verifique el estado alterado o la acción por defecto tras atrapar un error específico en la API externa/localStorage. |
| 2485 | `} catch (e) {}` &rarr; `} catch (e) { return; }` | Sí | Falta una prueba que cubra la ruta de error/excepción simulando un fallo que obligue a pasar por este `catch` específico y aserte que la función continúa su ejecución sin abortar. |
| 2158 | `} catch (e) { evBuffer = []; }` &rarr; `} catch (e) {}` | Sí | Falta asertar que evBuffer se reinicia a `[]` al fallar el parseo desde localStorage. |
| 2099 | `} catch (e) {}` &rarr; `} catch (e) { return null; }` | Sí | Falta una prueba que cubra la ruta de error/excepción simulando un fallo que obligue a pasar por este `catch` específico y aserte que la función continúa su ejecución sin abortar. |
| 2282 | `} catch (e) {}` &rarr; `} catch (e) { return true; }` | Sí | Falta aserción que verifique el estado alterado o la acción por defecto tras atrapar un error específico en la API externa/localStorage. |
| 83 | `if (!s) return null;` &rarr; `if (s) return null;` | No | - |
| 5957 | `.catch((e) => console.warn("[Vigilante] falló el envío del SMS:", e));` &rarr; `.catch((e) => {});` | Sí | Falta asertar sobre el aviso en consola emitido cuando el Promise del envío del SMS lanza una excepción o es rechazado. |
| 933 | `_diagLabFechaLogged = true;` &rarr; `_diagLabFechaLogged = false;` | Sí | Falta asertar que el flag de una-sola-vez `_diagLabFechaLogged` evita desbordar la consola de logs repetidamente. |
| 934 | `console.warn` &rarr; `// console.warn` | Sí | No hay aserción sobre los llamados a `console.warn` (`console` no está mockeado de forma estricta para asegurar que se producen los avisos de depuración clínica). |
| 925 | `console.warn("[Vigilante] casilla ya escrita, se conserva el primer valor:", matched.key);` &rarr; `// console.warn(...);` | No | - |
| 873 | `if (code && item.codes.includes(code)) return item;` &rarr; `if (code && !item.codes.includes(code)) return item;` | No | - |
| 876 | `if (item.excluye && item.excluye.some((x) => name.includes(x))) continue;` &rarr; `if (item.excluye && !item.excluye.some((x) => name.includes(x))) continue;` | No | - |
| 881 | `return null;` &rarr; `return WHITELIST_13_LABS[0];` | No | - |
| 886 | `for (const id of [primaryId].concat(altIds || [])) {` &rarr; `for (const id of [primaryId]) {` | No | - |
| 887 | `if (!id) continue;` &rarr; `if (id) continue;` | No | - |
| 903 | `let count = 0;` &rarr; `let count = 1;` | No | - |
| 904 | `let pendientes = 0;` &rarr; `let pendientes = 1;` | No | - |
| 907 | `if (!Array.isArray(labsArray)) return { count: 0, pendientes: 0, sinCasilla: [] };` &rarr; `if (!Array.isArray(labsArray)) return {};` | Sí | Falta aserción fuerte que valide los valores de retorno para este caso extremo o error de tipo en el flujo secundario. |
| 911 | `if (!matched) return;` &rarr; `if (matched) return;` | No | - |
| 914 | `if (!resultVal) return;` &rarr; `if (resultVal) return;` | No | - |
| 920 | `if (Number(lab.idEstado) === 1 || String(resultVal).trim().toUpperCase() === "PENDIENTE")` &rarr; `if (Number(lab.idEstado) === 2 || String(resultVal).trim().toUpperCase() === "PENDIENTE")` | Sí | Falta aserción fuerte que valide los valores de retorno para este caso extremo o error de tipo en el flujo secundario. |
| 920 | `toUpperCase() === "PENDIENTE"` &rarr; `toLowerCase() === "pendiente"` | Sí | Falta aserción fuerte que valide los valores de retorno para este caso extremo o error de tipo en el flujo secundario. |
| 925 | `if (yaEscritas.has(matched.resultId)) { console.warn` &rarr; `if (!yaEscritas.has(matched.resultId)) { console.warn` | No | - |
| 941 | `count++;` &rarr; `count += 2;` | No | - |
| 952 | `return { count, pendientes, sinCasilla };` &rarr; `return { count: 0, pendientes: 0, sinCasilla: [] };` | No | - |
| 1247 | `if (!docId) return false;` &rarr; `if (docId) return false;` | No | - |
| 1257 | `if (!docId) return;` &rarr; `if (docId) return;` | No | - |
| 1260 | `if (!p.citas.includes(sDoc)) { p.citas.push(sDoc); writeJSON(PROC_KEY, p); state.lastSignature = ""; repaint(); }` &rarr; `if (p.citas.includes(sDoc)) { p.citas.push(sDoc); writeJSON(PROC_KEY, p); state.lastSignature = ""; repaint(); }` | No | - |
| 1260 | `p.citas.push(sDoc);` &rarr; `p.citas.pop();` | No | - |
| 1260 | `state.lastSignature = "";` &rarr; `state.lastSignature = "dummy";` | No | - |
| 1269 | `if (!docId) return;` &rarr; `if (docId) return;` | No | - |
| 2011 | `} catch (err) {` &rarr; `} catch (err) { return; }` | No | - |
| 2725 | `const ok = await loadPymBase(baseIntentos < 3).catch(() => false);` &rarr; `const ok = await loadPymBase(baseIntentos < 3).catch(() => true);` | Sí | Falta aserción que verifique el estado alterado o la acción por defecto tras atrapar un error específico en la API externa/localStorage. |
| 3581 | `} catch (e) {` &rarr; `} catch (e) { return true;` | No | - |
| 104 | `if (!d || isNaN(d)) return null;` &rarr; `if (!d && isNaN(d)) return null;` | No | - |
| 105 | `if (d > Date.now() + 86400000) return null;` &rarr; `if (d < Date.now() + 86400000) return null;` | No | - |
| 110 | `if (an < 100) an += 2000;` &rarr; `if (an > 100) an += 2000;` | No | - |
| 111 | `if (an < 2000 || an > 2100) return null;` &rarr; `if (an < 2000 || an > 2000) return null;` | No | - |
| 121 | `return null;` &rarr; `return false;` | No | - |

| 10984 | `(a.estado && a.estado.toLowerCase().includes("atendido") ? " atendido" : "")` &rarr; `(a.estado && a.estado.toLowerCase().includes("en sala") ? " atendido" : "")` | No | - |
| 11005 | `const atendidoLeyenda = esAtendido && a.color !== "ROJO";` &rarr; `const atendidoLeyenda = false;` | No | - |
| 11128 | `? (esAtendido || yaAbiertoHoy` &rarr; `? (false || yaAbiertoHoy` | No | - |
| 8822 | `<div class="vgl-labs-uro">…<span class="vgl-labs-uro-i">…</span>…</div>` &rarr; `…join("&lt;br&gt;")` (volver al chorizo previo) | No | - |
| 2844 | `if (actual === "" && guardado !== "") porAplicar.push(...)` &rarr; `if (guardado !== "") porAplicar.push(...)` (rediseño v12.9.0 a plantilla por posición; reemplaza la fila anterior de esta misma pareja de botones) | No | - |
| 8382 | `const franja = tieneDM ? "primera_mitad" : "sin_preferencia";` &rarr; `const franja = tieneDM && !tieneNefro ? "primera_mitad" : "sin_preferencia";` | No | - |
| 6660 | `.vgl-btn-action.vgl-btn-ambar,\n.vgl-btn-action.vgl-btn-ambar:hover{` &rarr; `.vgl-btn-ambar{` (clase suelta, misma especificidad que la regla base — el bug real de T1) | Sí, hasta corregirse | "el ámbar de «falta la toma de muestras» gana a la regla base pase lo que pase (cascada)" — restaurado |
| 11400/11443 | textos "abre la Historia Clínica de.../Historia clínica abierta..." &rarr; versión honesta "Registrar inicio de atención..." (incidente real en consultorio: apiMedicoAbrirHistoria() nunca navega, solo registra un timestamp; el médico creyó haber revisado al paciente sin haberlo hecho) | No | "botón Atender: ningún texto visible promete abrir/mostrar la historia clínica" |
| 7891/7892 | blindaje `:where(...:not([class]))` de `#vgl-labsv-modal`/`#vgl-postcita-panel` &rarr; se quitan ambos del `:where()` y se reintroduce la regla vieja `#vgl-labsv-modal b,#vgl-labsv-modal span,#vgl-labsv-modal div,#vgl-postcita-panel b,#vgl-postcita-panel span,#vgl-postcita-panel div{color:inherit}` (el bug real reportado en consulta: `id+tipo` gana por especificidad a `.vgl-postcita-title`/`.vgl-postcita-sub`, heredando el azul de Everest en vez de verde/gris propios — confirmado con Chromium sobre el CSS real extraído del harness) | Sí, hasta corregirse | "blindaje tipográfico: postcita-panel y labsv-modal usan :not([class]), no div/span/b a pelo" — restaurado |
| 6655 | `.vgl-btn-action.vgl-btn-ambar` &rarr; `.vgl-btn-ambar` y declarada antes de la base. | Sí | Suite 25 (Cascada CSS) detecta dependencia del orden de declaración. |
| 6240 | Añadir `.vgl-d-none{display:none !important}` y usar `el.root.classList.add('vgl-d-none')` antes de `style.display="flex"`. | Sí | Suite 25 (Cascada CSS) detecta conflicto !important vs inline style en `display`. |
| 2929 | botón "🩺 Normalidad fija": `if (actual === "") porAplicar.push(...)` &rarr; `porAplicar.push(...)` sin la condición (deja de respetar la casilla sagrada del médico) | Sí | "Normalidad fija: rellena SOLO las vacías, respeta las que ya tienen texto..." — restaurado |
| 2868 | v12.10.4: se quitaron "💾 Guardar plantilla" / "📋 Aplicar plantilla" a pedido del médico (solo quiere la plantilla fija, sin memoria del último paciente) y se reintrodujo un `if (!confirm(...)) return;` antes de aplicar "Normalidad fija" (el médico pidió explícitamente un solo clic, sin cuadro de confirmación) | Sí | "Normalidad fija: un solo clic rellena SOLO las vacías, sin pedir confirmación..." y "...avisa el desajuste sin dejar de aplicar" — restaurado |
| 6996 | v12.10.5: `.vgl-labsv-lead{color:var(--fg2) !important}` &rarr; `.vgl-labsv-lead{color:var(--fg2)}` sin `!important` (bug real reportado en consulta con captura: el azul de Everest ganaba porque esta regla no tenía forma de competir — a diferencia de `.vgl-labsv-t`/`.vgl-labsv-n`, protegidas por estilo inline) | Sí | "blindaje !important: lead/foot de los avisos flotantes y postcita-title/sub no pueden perder su color contra Everest" — restaurado |
| 8125 | `return FESTIVOS.has(...)` &rarr; `return false;` | No | - |
| 7031 | v12.10.7: `.vgl-labsv-t{...color:var(--c-rojo) !important...}` &rarr; sin `!important` | No | "blindaje !important: título/número de bigAlert, pymAlert, abandonoPESAlert y labsVencidosAlert..." — restaurado |
| 10797 | v12.10.7: `if (lbl2 && lbl2.classList)` &rarr; `if (lbl2.classList)` (sin el `&&`; esta misma línea ya había reaparecido rota una vez en una ronda de Jules) | No | "guard de classList tras closest(\"label\") siempre cortocircuita con &&..." — restaurado |
| 7031 | v12.10.7: `color:var(--c-rojo)` &rarr; `color:var(--c-alerta)` (token inventado por Jules que nunca se definió en ningún lado; causaba herencia del azul de Everest) | No | "no debe reaparecer --c-alerta/--rgb-alerta..." — restaurado |
| 1973 | `_canonNombreLab` (quitar reemplazo de separadores por espacios, `replace(/[\/\-_,.;:()]+/g, " ")`) | No | Falla `_canonNombreLab: convierte separadores especiales a espacios simples` y `_matchLabInWhitelist v12.6.8` |
| 2034 | `_findHbA1cFields` (cambiar el atributo para validación max de 30 a 20 `el.getAttribute("max") === "20"`) | No | Falla `_findHbA1cFields: encuentra el input correcto por type=number y max=30 y asocia la fecha hermana` |
| 11912 | `if (false) return;` | No | `boot() aborta tempranamente si #vgl-root ya existe en el DOM (guard)` |
| 9752 | v12.10.8: `const esSugerida = !!recHorario.sugerida && normalizeHora(horaTxt) === recHorario.sugerida;` &rarr; `const esSugerida = false;` | No | "un paciente diabético ve 'SUGERIDO' en el turno AM temprano..." — restaurado |
| 9547 | v12.10.8: `perfilDelPaciente = perfilPaciente(etiquetasPaciente);` &rarr; `perfilDelPaciente = { franja: "primera_mitad", adicionales: [] };` (perfil fijo, ignora las etiquetas reales del paciente) | No | "un paciente sin etiquetas de riesgo no ve ninguna insignia de sugerido..." — restaurado |
| 11576 | `setNgValue(el, _racGuardia.valor)` &rarr; `_racGuardia.activa = false` (apagar guardia en vez de restaurar) | No | - |
| 2247 | `if (Number(lab.idEstado) === 1 \|\| ...)` &rarr; `=== 2` (un analito PENDIENTE de Athenea deja de descartarse y su valor de arrastre entraría a la casilla como resultado vigente) | No — **ya no**; sobrevivía hasta hoy | "idEstado 1 con un valor numérico viejo en el campo NO se escribe" — restaurado |
| 2247 | `if (Number(lab.idEstado) === 1 \|\| String(resultVal).trim().toUpperCase() === "PENDIENTE")` &rarr; se quita la rama del texto, queda solo `idEstado === 1` | No — **ya no**; sobrevivía hasta hoy | "Resultado 'PENDIENTE' sin idEstado tampoco se escribe (la rama del texto, sola)" — restaurado |
| 10033 | `notify("AMBAR", "🧪 Falta la fecha guardada localmente...` &rarr; `notify("ROJO", "🧪 Falta la fecha mutada...` | No | "openLabSoloModal: paciente sin fecha guardada lanza notify y aborta" — restaurado |
| tests/suite_07:71 | quitar el `await` de un `t.casoAsync` (el bug original que mantuvo 8 pruebas de suite_05 y 2 de suite_07 sin ejecutarse, con el banco en verde) | No | "todo t.casoAsync se invoca con await" (suite_26) — restaurado |
| tests/suite_07:82 | reintroducir `api.zipRead = ...` sobre el `api` COMPARTIDO | No | "ninguna suite pisa el objeto `api` compartido" (suite_26) — restaurado |
| tests/suite_07:9 | quitar `async` de `pruebas(...)` | n/a — la mutación tira el runner con SyntaxError al cargar el módulo, así que el guard de suite_26 no llega a evaluarse. El fallo es ruidoso (código de salida ≠ 0, sin línea "comprobaciones"), que es justo lo que atrapa la compuerta de muerte silenciosa del CI. El chequeo se conserva como defensa en profundidad, no como el detector principal. | — |
| 7212 | v12.10.9: se retira la regla `#vgl-agendar-modal.light .vgl-agm-sbtn.vgl-agm-sbtn-sugerido, ...{background/color/border-color}` que le gana en especificidad a la base de tema claro | No | "Regla C - la insignia SUGERIDO no pierde su color en tema claro" (suite_25) — restaurado |
| 11820 | `const mask = (s) => { ... };` &rarr; `const mask = (s) => String(s);` (se quita el enmascarado del ID en downloadDiagnostic) | No | "downloadDiagnostic: enmascara correctamente los IDs y omite los nombres (cero PHI)" detectó la fuga de la cédula real inventada y falló las aserciones de enmascarado — restaurado |
| 11831 | `out.push("COINCIDEN: " + hit + "/" + lst.length` &rarr; `+ 999 + "/"` (conteo de coincidencias fijo, ignora el cruce real) | No — solo tras agregar `t.cierto(texto.includes("COINCIDEN: 1/2"))`; la prueba original de Jules solo comprobaba que la ETIQUETA existiera, no el número — restaurado |
| 4119 | `if (!writeJSON(k, ...)) evBuffer = pend.concat(evBuffer).slice(-200);` &rarr; `writeJSON(k, ...);` (se pierde el reencolado: la tanda de la bitácora de auditoría se traga si falla la escritura) | No | caen las DOS: "evFlush: la tanda no se pierde si falla la escritura (reencolado)" y "...tiene un tope de 200" — restaurado |
| 4119 | `.slice(-200)` &rarr; `.slice(-2)` en el reencolado | No | "evFlush: la tanda reencolada por fallo de escritura tiene un tope de 200" — restaurado |
| 4661 | `catch (e) { return false; }` &rarr; `catch (e) { return true; }` | No | "pilotoDesdeCache: caché corrupta (empieza por v3 pero no es JSON) lanza y el catch devuelve false" — restaurado |
| 4908 | `.catch(() => false)` &rarr; `.catch(() => true)` | No | "schedulePymBase: loadPymBase rechaza (por ej. sin TextDecoder) y la cadena de reintentos se mantiene" — restaurado |
| `tests/suite_25_cascada_css.js` | Cambiar `var(--c-verde, #16a34a)` por `var(--c-noexiste)` | Falla Regla C mostrando `--c-noexiste` faltante | Revertido a `var(--c-verde, #16a34a)` |
| `tests/suite_25_cascada_css.js` | Borrar `,#vgl-postcita-panel.light` de la línea 6197 | Falla Regla D por falta de paridad de tokens en listas clara/oscura | Revertido a añadir `,#vgl-postcita-panel.light` |
| `tests/suite_25_cascada_css.js` | Añadir `TURQUESA: "#0ff"` a const `COLORS` | Falla Regla D por ausencia de `--c-turquesa` | Revertido a remover `TURQUESA` de `COLORS` |
| 2304 | `if (Number(lab.idEstado) === 1 || ...)` &rarr; `=== 2` (un componente de orina PENDIENTE de Athenea pero con resultado real dejaría de descartarse) | Sí | "injectLabsIntoCronicos: componente de orina con idEstado 1 pero valor aparentemente real no llega a la historia clinica" — restaurado |
| 2268 | `return { count: 0, pendientes: 0, sinCasilla: [], respetadas: 0, uroanalisisMarcado: false };` &rarr; `return {};` | Sí | "injectLabsIntoCronicos: entrada que no es arreglo devuelve objeto de estado vacio" — restaurado |
| `clasificaCupoAgenda` | Hacer que los valores vacíos, nulos o desconocidos devuelvan `"normal"` | Motor de perfil (D3-bis) - Clasifica cupo agenda - vacíos, nulos y desconocidos no asumen normalidad | Restaurado |
| 2631 | R1a: `estadioKDIGO` — `if (v >= 45) return "G3a";` &rarr; `if (v > 45) return "G3a";` | No | "estadioKDIGO - cada frontera exacta de los seis estadios" (Función renal (R1)) — restaurado |
| 2623 | R1a: `ckdEpi2021` — se quita `if (femenino) e *= 1.012;` (multiplicador femenino) | No | "ckdEpi2021 - casos verificados independientemente" (Función renal (R1)), caso `ckdEpi2021(63, 0.55, "F") === 102.9` (obtuvo 101.7) — restaurado |
| 8966 | `const nTrabajadores = Math.min(tope, n);` &rarr; `const nTrabajadores = n;` (ignora `limite`, arranca todas las tareas a la vez como `Promise.all`) | No | "mapConLimite: nunca mantiene más de \`limite\` promesas en vuelo (18 tareas, límite 5)" (también cayeron "cancelado detiene el arranque..." y "\`limite\` <= 0 se trata como 1") — restaurado |
| 5202 | C5: no aplica mutación de comportamiento — borrado de código muerto `function fraudSound() { beep(1000, 400, 0); beep(1200, 400, 0.45); }` (nunca llamada; `playTone(color)`/`TONE.ROJO` la reemplazó) y de `"fraudSound"` en el `cubre` de `tests/suite_04_agenda_alertas.js` | No aplica — la comprobación fue el contador invariante: 773 comprobaciones pasan antes del borrado y 773 después (`fraudSound` solo estaba declarada en `cubre`, ningún `t.caso` la ejercitaba); la cobertura bajó de 331/357 a 330/356 como se esperaba | — |
| 9016, 9025 | `calcRangoSondeoIso`: bajar el tope `prevDays.length < 7` / `nextDays.length < 7` a `< 3` (vuelve a ±3 días hábiles en vez de ±7) | No | "calcRangoSondeoIso: ±7 días hábiles + sábados candidatos, arreglo ISO exacto" — restaurado |
| 9045 | `calcRangoSondeoIso`: quitar `...sabados` del `return [...prevDays, getInfo(baseDate, true, false), ...nextDays, ...sabados]` (ya no se agregan los sábados candidatos) | No | "calcRangoSondeoIso: ±7 días hábiles + sábados candidatos, arreglo ISO exacto" — restaurado |
| 2610 | Reconciliación C1/C2/C3 (verificación independiente, no del agente original): `cockcroftGault` — `v *= 0.85;` &rarr; `v *= 0.95;` (factor femenino incorrecto) | No | "cockcroftGault - caso 'paciente obesa'..." y "...caso general, ambos sexos" (Función renal (R1)) — restaurado |
| 2647 | Reconciliación (verificación independiente): `evaluarDiscordanciaTFG` — `if (diferenciaEstadios <= 2) return null;` &rarr; `< 2` (2 estadios de diferencia deja de tratarse como no-evaluable) | No | "evaluarDiscordanciaTFG - diferencia de 2 estadios no es alerta" (Función renal (R1)) — restaurado |
| 9050 | Reconciliación (verificación independiente): `mapConLimite` — se anula el chequeo `if (typeof cancelado === "function" && cancelado())` (queda `if (false)`). No solo falla la aserción: el banco se **cuelga** (deadlock real, sin timeout) porque las tareas 3-6 arrancan de verdad tras la cancelación y nadie resuelve sus promesas — confirmación más fuerte que un simple assert roto de que la ruta de cancelación es indispensable | No (cuelga el runner) | "mapConLimite: \`cancelado\` detiene el arranque de tareas nuevas a mitad de lote" (Utilidades Puras) — restaurado, verificado con `timeout` |
| 9044 | Reconciliación (verificación independiente): `mapConLimite` — `tope < 1` &rarr; `tope < 0` (limite=0 deja de clamparse a 1: 0 trabajadores, ninguna tarea corre) | No | "mapConLimite: \`limite\` <= 0 se trata como 1, nunca como ilimitado" (Utilidades Puras) — restaurado |
| — | Reconciliación de ramas Grupo C (13-08-2026): C1 (`81fe43a`, origin) traía `suite_27_funcion_renal.js` pero no C3 ni el fix de `Codigo.gs`; C3+mi fix de `ver`+C5 quedaron por accidente en una rama local nueva (`claude/c3-mapconlimite`) sin C1; C2 (`6b99b83`) vivía sola en un worktree aparte. Unificados con `git rebase origin/...` + `cherry-pick` sobre `claude/c3-mapconlimite`, conflictos solo en `@version`/`VERSION` (resueltos a 12.10.11, el más alto) y en el apéndice de esta tabla (ambos lados conservados). 787/787 comprobaciones en verde tras unir las cuatro tareas | — | — |
| 4406 | v12.10.12 (observabilidad): `reportarError` — quitar la copia defensiva `_migas.slice()` (queda `_migas` por referencia directa) | No | "reportarError: adjunta las migas... SIN incluir el propio error como su última miga" (Telemetría de uso del panel) — restaurado |
| 4391 | v12.10.12: anillo de migas — `if (_migas.length > MIGAS_MAX)` &rarr; `> MIGAS_MAX - 1` (guarda 1 miga de menos) | No | "reportarError: las migas se limitan a las últimas 8 acciones (ring buffer)" — restaurado |
| 4417 | v12.10.12: deduplicación por huella — `if (!_errVistos.has(huella))` &rarr; `if (true)` (cuenta toda repetición como huella nueva) | No | "reportarError: 'error.distintos' solo sube con huellas NUEVAS..." y "...la huella cae al mensaje saneado" — restaurado |
| 8441-8442 | v12.10.12: RUM — invertir el orden del allowlist (`BuscarPaciente` antes que `BuscarPacienteDetallado`, el genérico gana por ser más corto) | No | "_rumEndpointLabel: reconoce los endpoints conocidos por nombre FIJO..." — restaurado |
| 8474 | v12.10.12: RUM — clasificación de resultado siempre `".ok"` (nunca distingue un 4xx real) | No | "pageFetchJson/RUM: una llamada que falla (4xx, sin reintento) suma api.\<endpoint\>.err" — restaurado |
| 8471 | v12.10.12: RUM — quitar el `if (S.uxTelemetria === false) return;` de `_rumTrack` (queda `if (false) return;`, o sea nunca corta antes) | **Sí — sobrevive a propósito** | Ninguna: `uxTrack()` (llamada dentro de `_rumTrack`) YA trae su propio candado sobre `S.uxTelemetria`, así que este chequeo es una optimización de rendimiento (evita construir la etiqueta y suscribirse a la promesa cuando la telemetría está apagada), no una garantía de corrección — no hay caso de prueba que dependa de él, ni debería haberlo |
| — | v12.10.12 (`TABLERO/Codigo.gs`, `armarResumen()`): Apps Script no se puede ejecutar aquí — la lógica NUEVA de agregación de `uso_detalle` (volumen real de errores, huellas distintas y RUM por endpoint) se reprodujo literal en un script Node standalone (`/tmp/verify_armarresumen_agg.js`, no versionado) alimentado con filas realistas, y se le aplicaron 2 mutaciones deliberadas: (1) que `"error.distintos"` cayera también en el bucket de volumen — cambia 9→12 en volumen y pierde el conteo de distintos; (2) que la clasificación ok/err del RUM ignorara cuál de los dos era y siempre sumara a `ok` — cambia `{ok:8,err:1}`→`{ok:9,err:0}`. Ambas mutaciones hacen fallar la reproducción; restaurada y en verde antes de aplicar el mismo cambio al archivo real | No (en la reproducción) | — |
| 2409 | `if (!resultDate && !_diagLabFechaLogged)` &rarr; `if (resultDate && !_diagLabFechaLogged)` (avisaría cuando SÍ hay fecha) | Sí | "el aviso 'no se reconoció ninguna fecha' sale UNA sola vez por sesión" - restaurado |
| 2410 | `_diagLabFechaLogged = true;` &rarr; `= false;` (seguiría avisando en cada laboratorio sin fecha) | Sí | "el aviso 'no se reconoció ninguna fecha' sale UNA sola vez por sesión" - restaurado |
| 2411 | `console.warn(...)` comentado (el aviso desaparece por completo) | Sí | "el aviso 'no se reconoció ninguna fecha' sale UNA sola vez por sesión" - restaurado |
| 9005 | TAREA B1 (guardas SMS): `if (creada && S.smsRecordatorio && cel.length >= 7)` &rarr; se quita `&& cel.length >= 7` (dispararía SMS a un celular demasiado corto) | No | "apiAccesoAsignarTurno NO dispara SMS si el celular tiene menos de 7 digitos" (Llamadas a Everest y clínicas) — restaurado; documentado aquí porque el PR original (#50, google-labs-jules) afirmó haberlo registrado pero su diff no tocaba este archivo — verificado y añadido yo mismo antes de fusionar |
| 9009 | TAREA B1 (guardas SMS): `.catch((e) => console.warn(...))` &rarr; `.catch((e) => {})` (un fallo de red al mandar el SMS quedaría en silencio) | No | "apiAccesoAsignarTurno no silencia el fallo al enviar el SMS" — restaurado; mismo caso que la fila anterior |
| — | TAREA B4 (Regla E, google-labs-jules): inyección adversarial `#vgl-labsv-modal .vgl-prueba{color:var(--c-rojo)}` en `vigilante_agenda.user.js` — fila reescrita al formato correcto de esta tabla (el PR original la escribió con 5 columnas en vez de 4, mismo tipo de error de formato ya visto antes en otros PRs de Jules) | Sí, la atrapó | "Regla E - color con selector de PANEL fuera de #vgl-root lleva !important" — revertida |
| — | Reconciliación (verificación independiente, no del PR original): la versión de PR#51 traía un filtro `if (cd.includes('color: var(')) continue;` dentro de la propia Regla E que EXCLUÍA de la revisión, sin importar si tenían !important o no, TODA declaración de color escrita con espacio tras los dos puntos (el comentario del propio PR admite que era para "get exactly 70 elements" en vez de corregir la base mal calculada). Esto ocultaba 3 infracciones reales preexistentes (`#vgl-agendar-modal.light .vgl-ord-title/.vgl-ord-cie/.vgl-ord-cups`) y dejaba ciega la regla ante CUALQUIER declaración nueva en ese formato — verificado inyectando `#vgl-labsv-modal .vgl-mutacion-indetectable{color:var(--c-rojo)}` (formato con espacio): con el filtro puesto, 803/803 en verde, la regla no la vio; con el filtro quitado y BASE_CONOCIDA corregida a las 73 infracciones reales, la misma inyección hace fallar la prueba ("Deben salir 73... Salieron 74"). También verificado a la inversa: agregar `!important` a una de las 3 antes-ocultas la saca de la lista ("Salieron 72"). Filtro eliminado, BASE_CONOCIDA corregida a 73 elementos reales | Sí (con el filtro puesto) / No (filtro quitado) | "Regla E - color con selector de PANEL fuera de #vgl-root lleva !important" — ambas mutaciones de verificación restauradas, 803/803 en verde con la versión honesta |
| 3 | TAREA D2 (cobertura fantasma, `tests/runner.js`): en `tests/suite_22_utilidades_puras.js`, `cubre: ["calcDateRangeAroundIso", "mapConLimite"]` &rarr; `cubre: ["calcDateRangeAroundIso", "mapConLimite", "_esSexoFemenino"]` — nombre real del API (`vigilante_agenda.user.js:2601`) que ninguna suite nombra literalmente fuera de su propio array `cubre` (confirmado antes con `git grep -nw "_esSexoFemenino"`, solo aparecía en `vigilante_agenda.user.js`). No es un bloque con aserción/salida de proceso, es puramente informativo, así que aquí "sobrevivir" se mide por si el bloque nuevo reaccionó de verdad al cambio declarativo | No | El bloque nuevo `declaradas pero nunca nombradas` SÍ detectó el hueco: pasó de 26 a 27 entradas y `_esSexoFemenino` apareció en la lista (y desapareció de `sin cubrir`); al revertir el `cubre`, volvió a 26 y `_esSexoFemenino` regresó a `sin cubrir` — `node tests/runner.js` idéntico al basal (803 pasan, 339/366, 92.6%, exit 0) en ambos extremos. No aplica "aserción faltante" en sentido estricto (no hay assert sobre este bloque, es un informe de consola) — la propiedad verificada fue que el cálculo textual (`\bNOMBRE\b` tras quitar el bloque `cubre` de cada suite) responde en los dos sentidos, no solo uno |
| 8398 | TAREA B2: `_pageFetchJsonCore` — `if (esEscritura) { console.warn(...); return null; }` &rarr; `if (false) { ... }` (la guardia que corta antes del respaldo GM en una escritura queda anulada: un POST/PUT/DELETE fallido con 5xx volvería a reenviarse por GM_xmlhttpRequest) | No | "_pageFetchJsonCore: escritura (POST) con 500 NO reintenta y NO se reenvía por GM" (Llamadas a Everest y clínicas) — cont.gm pasó de 0 a 1, prueba cayó nombrando el caso exacto; restaurado y verde de nuevo |
| 2683 | R2 (mutación obligatoria del criterio de aceptación): tabla `RCV_VIGENCIA_ESTADIO_TABLA.ERC.pth` — se invierte la fila `pth` del estadio G3a de `365` a `"BLOQ"` | No | "vigenciaPorEstadio - ERC/pth NO es BLOQ en G3a: debe ser 365 (fila que la mutación obligatoria invierte)" (Vigencias por estadio renal (R2, sombra)) — falla mostrando `esperaba 365 y obtuvo "BLOQ"`; restaurado a `G3a: 365` y confirmado 832/832 en verde de nuevo |
| 6545 | TAREA D1 (escala tipográfica): revertir una de las 36 sustituciones `font-size:var(--t-micro)` &rarr; `font-size:12px` (deja un literal suelto en `.vgl-sb-lbl`, sin cablear al token muerto `--t-micro`) | No | "Regla G - escala tipográfica: font-size literales quedan cableados a var(--t-*)" — "No deben quedar font-size:12px literales en la hoja (quedaron 1)" — restaurado |
| 6423 | TAREA D1 (caso especial `.vgl-lab-inj,.vgl-exf-btn`): quitar la reserva `font-size:var(--t-micro,12px)` &rarr; `font-size:var(--t-micro)` (sin reserva, `#vgl-examen-normalidad` — pegado a `document.body`, fuera de las dos listas de tokens — heredaría el font-size de Everest, el modo de fallo del incidente v12.6.6) | No | "Regla G - escala tipográfica: font-size literales quedan cableados a var(--t-*)" — "El caso especial .vgl-lab-inj,.vgl-exf-btn debe conservar la reserva var(--t-micro,12px) exactamente 1 vez (salieron 0)" — restaurado |
| 6547 | v14.0.0 (T3, sistema de diseño): `.vgl-sp-x` — revertir `font-size:var(--t-strong)` &rarr; `font-size:15px` (deja un literal suelto sin cablear a la escala tipográfica ampliada) | No | "Regla I - escala tipográfica ampliada: 15/18/22px literales quedan cableados a var(--t-strong/title/hero)" — "No deben quedar font-size:15px literales en la hoja (quedaron 1)" — restaurado |
| 7142 | v14.0.0 (T3): `#vgl-modal` — revertir `z-index:var(--z-alerta)` &rarr; `z-index:2147483647` (una alerta de fraude/PES/PyM vuelve a competir por el valor máximo en vez de tener su propio nivel D6, garantizado por encima de los modales de flujo) | No | "Regla J - z-index migrado a los 5 tokens de capas de D6..." — "Solo debe quedar 1 literal z-index:2147483647 (.vgl-sp-toast, fuera del alcance de D6). Salieron 2." — restaurado |
| 6379/6430 | v14.0.0 (T3): declaración de los 5 tokens `--z-*` — `--z-alerta:2147483600` &rarr; `2147481500` (queda POR DEBAJO de `--z-modal:2147483000`, invirtiendo el orden que D6 exige: una alerta clínica podría quedar tapada detrás de un modal de flujo) | No | "Regla J..." — cae la comprobación de la declaración exacta de los 5 tokens en ambas listas (oscura/clara) — restaurado, y verificado aparte que las 4 aserciones de orden relativo (`z-alerta > z-modal > z-panel > z-banner > z-widget`) son correctas por inspección contra los valores reales de D6 |
| — | v14.0.0 (T3): verificación empírica en Chromium real (no solo el arnés de pruebas, que no tiene motor de layout/cascada) de que los 5 tokens `--z-*` y los 3 tokens `--t-*` nuevos resuelven al valor esperado sobre los 14 elementos reales de la "lista de 11/14 contenedores" (§1.2), en modo oscuro Y claro — incluyó un hallazgo real de metodología (no de producto): la primera versión de la página de prueba sintética no ponía `id="vgl-lab-injector"` en el elemento de prueba de `.vgl-lab-inj` (solo la clase), así que `--z-widget` no heredaba por no estar en el roster de contenedores — exactamente la trampa del incidente v12.6.6, mencionada en el propio superprompt, reproducida sin querer en la prueba de verificación misma. Corregido el HTML de prueba (no el producto) y confirmado: 2147482000/2147480000/2147483000/2147483600 y 15px/22px correctos en ambos modos | No aplica (verificación visual, no una mutación de código) | — |
| 11888 | v14.0.0 (T4, criterio de aceptación explícito — "mutación obligatoria sobre la condición de la bandera PES"): `render()` — `const pesFlag = esPes ? ...` &rarr; `!esPes ? ...` (invierte la condición: la bandera saldría para pacientes SIN abandono PES y se ocultaría para los que sí lo tienen — justo el paciente prioritario dejaría de verse marcado) | No | "T4 (D9) - la bandera PES usa el texto nuevo «ABANDONO PROGRAMA RCV»" — "la bandera PES lleva el texto nuevo (obtuvo false)" — restaurado |
| — | v14.0.0 (T4): amputación de `render()` (agendarBtn/ordenarBtn/labsBtn + chips PyM) verificada además con una captura Chromium real (no solo el arnés) de 3 tarjetas representativas (normal/fraude/PES) montadas con el CSS real extraído — confirma alturas consistentes, el botón Atender presente y visible en las 3, cero botones/chips huérfanos, y el texto nuevo de la bandera PES ("❤ ABANDONO PROGRAMA RCV") legible. Hallazgo NO corregido a ciegas, documentado para el médico: al quitar la fila de chips, la tarjeta deja un margen inferior sin usar (la altura mínima de la tarjeta no se ajustó) — es exactamente el "rediseña su jerarquía" que T4 deja abierto a criterio visual, no una regresión de comportamiento | No aplica (verificación visual) | — |
| 2976 | v14.0.0 (T5, dock de widgets): `createAccionesDockUI` — invertir `if (!_enModuloHCHealth())` a `if (_enModuloHCHealth())` (el widget se crearía FUERA de la Historia Clínica y se autolimpiaría DENTRO de ella, justo al revés) | No | 12 pruebas de `createAccionesDockUI` cayeron (el gate es la base de todas) — restaurado |
| 2978 | v14.0.0 (T5): invertir `if (!docId)` a `if (docId)` (el widget se crearía sin paciente resuelto, o se autolimpiaría con uno abierto) | No | 13 pruebas de `createAccionesDockUI` cayeron — restaurado |
| 2981 | v14.0.0 (T5): `const esNuevo = !dock;` &rarr; `= true;` (cada tick duplicaría el contenedor `#vgl-acciones-dock` en vez de reutilizar el existente) | No | "crea el widget con los 3 botones... una sola vez (idempotente)" — "esperaba 1 y obtuvo 2"; restaurado |
| 3037 | v14.0.0 (T5): `if (citaHechaHoy && labHechoHoy)` &rarr; `if (citaHechaHoy \|\| labHechoHoy)` (el botón de agendar se bloquearía con SOLO la cita hecha, antes de que la toma de muestras exista siquiera — el médico no podría agendar el laboratorio que sí falta) | No | "botón de agendar refleja los 3 estados..." y "clic en agendar (solo falta el laboratorio)..." — restaurado |
| 3035 | v14.0.0 (T5): quitar la condición ternaria de `vgl-dock-btn-ambar` (el botón nunca se distinguiría visualmente cuando solo falta la toma de muestras) | No | "botón de agendar refleja los 3 estados... variante ámbar" — restaurado |
| 3057 | v14.0.0 (T5): `if (yaOrdenadoHoy)` &rarr; `if (!yaOrdenadoHoy)` (el botón de ordenar se bloquearía cuando NO hay órdenes generadas y se habilitaría cuando SÍ las hay — permitiría duplicar órdenes PyM el mismo día) | No | "botón de ordenar se bloquea cuando las órdenes ya se generaron hoy" — restaurado |
| 3051 | v14.0.0 (T5): quitar la guarda `if (bAg.disabled) return;` del clic de agendar (un clic en el botón ya bloqueado —cita y laboratorio ya agendados hoy— volvería a abrir el modal y arriesgaría un agendamiento duplicado) | No | "clic en un botón bloqueado (disabled) no hace nada" — restaurado |
| 3021 | v14.0.0 (T5): `GM_setValue("vgl_dock_acciones_colapsado", nuevoColapsado ? "1" : "")` &rarr; siempre `""` (el estado colapsado/expandido nunca se persistiría entre sesiones, el médico tendría que volver a colapsarlo cada vez) | No | "clic en toggle colapsa/expande y persiste la preferencia (GM_setValue)" — restaurado |
| — | v14.0.0 (T5): las SEIS listas de §1.2 — se agregó `#vgl-acciones-dock` a las 6 (tokens oscuros línea 6420, tokens claros línea 6506, reduced-motion línea 6618, box-sizing línea 6655, blindaje color:inherit simple línea 6664, armadura `:where()` línea 8285/8286) y a `applyTheme()` (línea 3457, sincroniza `.light`/`.perf`) — verificado que ninguna quedó fuera comparando contra `#vgl-dock` (el precedente más cercano: mismo documento raíz) en las 8 ubicaciones. Regla G y Regla J (Cascada CSS) detectaron automáticamente los 3 nuevos usos de tokens (`var(--t-micro)`, `var(--t-lead)`, `var(--z-widget)`) y el cambio de conteo de `!important` (por el interruptor `.perf` nuevo) — las 4 aserciones de conteo exacto se actualizaron con el motivo documentado en el propio comentario de la prueba | No aplica (auditoría de listas, no una mutación de código) | — |
| — | v14.0.0 (T5): verificación empírica en Chromium real de las 5 variantes de estado del dock (normal/ámbar-solo-falta-lab/todo-bloqueado/colapsado/sin-agendamiento-rápido) en modo oscuro Y claro, montadas con el CSS real extraído y HTML real generado por `createAccionesDockUI()` (no HTML de mano) — confirma `z-index:2147480000` (var(--z-widget), sin repetir la trampa v12.6.6: el elemento raíz usa el id real `#vgl-acciones-dock`), color/fondo/font-family resueltos a los tokens de superficie correctos en ambos temas, el aro ámbar visible y con el tono correcto (`--c-ambar`, distinto en oscuro/claro) en el estado "falta laboratorio", opacidad reducida visible en los botones bloqueados, y el recuento de botones (3 / 1 sin agendamiento rápido) coincide con lo esperado | No aplica (verificación visual) | — |
| 2843 | v12.10.15 (auditoría nocturna, informe de Jules verificado contra el código real antes de corregir): `autoFetchAtheneaLabsForActivePatient` — revertir `_labsPrefetch = { docId, labs, ts: Date.now() };` (incondicional) a `if (labs.length > 0) { _labsPrefetch = ...}` (solo cachea cuando hay resultados) | No | "autoFetchAtheneaLabsForActivePatient: con CERO laboratorios en Athenea, el TTL de 10 min SÍ frena la siguiente consulta pasados los 30s (bug real de auditoría)" y "checkLabsVencidos: paciente CON pre-carga real pero SIN ningún laboratorio en Athenea -> SÍ dispara el aviso..." — ambas cayeron nombrando el caso exacto (3 peticiones esperadas, salieron 6; y `avisoYaVisto` esperado `true`, obtuvo `false`); restaurado y 844/844 en verde |
| 2884 | v12.10.15 (mismo bug, ruta del clic manual del botón «🧬 Auto-Labs»): revertir `if (labs) _labsPrefetch = ...` a `if (labs && labs.length) _labsPrefetch = ...` | No | "createLabInjectorUI (clic manual): SIN laboratorios encontrados también actualiza _labsPrefetch — checkLabsVencidos ya no queda mudo (bug real de auditoría)" — restaurado |
| 2587 | v12.10.15 (auditoría nocturna, `_vigenciaDiasParaAnalito`): revertir el saneo `.replace(/[^\d.,]/g, "")` (queda el `Number()` crudo de antes, que da `NaN` con valores de LIS como `"> 300"`) | No | "_vigenciaDiasParaAnalito: RAC reportado con desigualdad del LIS ('> 300', '>= 30') sigue reduciendo a 90 días" — "esperaba 90 y obtuvo 180"; restaurado, 844/844 en verde |
| — | v14.0.0 (T6, **mutación obligatoria del criterio de aceptación** — "la mutación más importante del encargo"): `pymCubiertoPorOrdenVigente` — invertir la dirección de fallo, `if (!Array.isArray(ordenes) \|\| !ordenes.length) return lista;` &rarr; `return [];` (un fallo de red pasaría a significar "todo cubierto" en vez de "todo pendiente": el banner de T7 se apagaría en silencio justo cuando la verificación no pudo hacerse) | No | 3 pruebas cayeron nombrando el caso: "respuesta vacía de órdenes -> TODO sigue pendiente", "(D4, LA MUTACIÓN OBLIGATORIA): fallo de red (ordenes=null)..." y "respuesta malformada..." — las 3 esperaban la lista completa de actividades y obtuvieron `[]`; restaurado, 861/861 en verde |
| — | **CORRECCIÓN post-fusión (mismo T6, antes de arrancar T7):** al releer D4 completo en `SUPERPROMPT_DISENO_V14.md` para diseñar T7, se encontró que la ventana temporal por AÑO CALENDARIO de la versión recién fusionada usaba una regla que el médico ya había cerrado el 12-ago-2026 por una distinta — `JULES_TAREAS_DISENO_V14.md` (de donde salió el texto de la tarea T6) nunca se actualizó tras esa decisión. La regla correcta: vigencia CLÍNICA por actividad, no año calendario — RCV reutiliza `RCV_VIGENCIA_DIAS` (180 días desde la orden), el resto de PyM usa `PYM_CATALOG[].vigenciaDias` (Resolución 3280/2018, sin confirmar todavía para ninguna actividad no-RCV: quedan siempre pendientes por D4). Reescrita `pymCubiertoPorOrdenVigente` completa; las pruebas de "ventana por año" se reemplazaron por pruebas de vigencia por días (180 exacto cubre, 181 no cubre, actividad sin `vigenciaDias` siempre pendiente, mismo CUPS con dos órdenes gana la más reciente, fecha futura se descarta) | No aplica (corrección de diseño, no una mutación) | — |
| — | v14.0.0 (T6, corregido): `pymCubiertoPorOrdenVigente` — quitar el gate `if (!Number.isFinite(act.vigenciaDias) \|\| act.vigenciaDias <= 0) return true;` | **Sí — sobrevive, documentado a propósito** | Ninguna prueba lo cazó: con `vigenciaDias` `undefined`, la comparación `dias <= undefined` de más abajo ya evalúa a `false` por las reglas de JS, así que el resultado final ("sigue pendiente") es el mismo con o sin el gate explícito para los datos reales del catálogo (ninguna actividad usa `vigenciaDias:0` o negativo hoy). El gate se conserva de todas formas por CLARIDAD — declara la regla D4 ("sin vigencia confirmada, siempre pendiente") en vez de depender de una coerción implícita de `undefined` que un lector no debería tener que reconstruir a mano — no es una garantía de corrección adicional con los datos de hoy, así que no hay caso de prueba que dependa de él, ni debería haberlo (mismo patrón ya documentado antes para `_rumTrack`/`S.uxTelemetria`) |
| — | v14.0.0 (T6, corregido): `pymCubiertoPorOrdenVigente` — quedarse con la PRIMERA fecha vista por CUPS en vez de la más reciente (`if (prev === undefined) masRecientePorCup.set(...)`) | No | "mismo CUPS con dos órdenes -> gana la fecha MÁS RECIENTE..." — restaurado |
| — | v14.0.0 (T6, corregido): `pymCubiertoPorOrdenVigente` — quitar el descarte de órdenes con `fechaCreacion` futura | No | "una orden con fecha FUTURA se descarta por prudencia..." — restaurado |
| — | v14.0.0 (T6): `apiHcObtenerOrdenamientosVigentes` — `if (!Array.isArray(data)) return null;` &rarr; `if (false) return null;` (una respuesta malformada se trataría como válida y se cachearía tal cual) | No | "respuesta malformada (no es un arreglo) devuelve null, no la respuesta cruda" — restaurado |
| — | v14.0.0 (T6): `apiHcObtenerOrdenamientosVigentes` — reintroducir a propósito el MISMO patrón de bug real corregido esta noche en `_labsPrefetch` (solo cachear cuando `data.length` es verdadero, dejando sin cachear el arreglo vacío) | No | "cachea por paciente... incluido un arreglo vacío" — "la segunda llamada se sirvió de la caché... esperaba 1 y obtuvo 2"; restaurado |
| — | v14.0.0 (T6): `apiHcObtenerOrdenamientosVigentes` — la comprobación de caché ignora `pacienteId` (`if (true && _ordenesVigentesCache.data...`), mezclando la caché de un paciente con la de otro | No | "un paciente DISTINTO sí dispara una consulta nueva" — restaurado |
| — | v14.0.0 (T7, **mutación obligatoria del criterio de aceptación** — "condición de apagado"): `createPymBannerUI` — invertir `if (!totalPendientes) { ...remove...; return; }` a `if (totalPendientes) { ...remove...; return; }` (el banner desaparecería justo cuando SÍ hay algo pendiente, y aparecería cuando no hay nada que reportar) | No | 6 pruebas cayeron (todas las que verifican visibilidad del banner con contenido pendiente) — restaurado |
| — | v14.0.0 (T7, D4 — "ante la duda, se muestra con honestidad"): `_refrescarBannerPym` — `const verificado = Array.isArray(ordenes);` &rarr; `const verificado = true;` (con la verificación de Everest caída, el banner dejaría de avisar con honestidad que no pudo comprobar nada) | No | "con la verificación CAÍDA, el banner se muestra con TODO pendiente y el aviso honesto" — restaurado |
| — | v14.0.0 (T7, D5 — "empuja el contenido, no lo tapa"): `createPymBannerUI` — `document.body.insertBefore(banner, document.body.firstChild)` &rarr; `document.body.appendChild(banner)` (el banner se insertaría DESPUÉS del contenido de Everest en vez de antes, dejando de empujarlo hacia abajo) | No | Requirió reforzar la prueba original (que no distinguía los dos casos porque el body de la prueba empezaba vacío): se agregó un nodo que simula el app-root de Everest ANTES de crear el banner, y se comprueba el orden real (`body.children[0]`/`[1]`) — "se inserta como PRIMER hijo del body, ANTES del contenido de Everest" — restaurado |
| — | v14.0.0 (T7): `pymPaquetesDelPaciente` — `const sinEmparejar = lista.filter(...)` &rarr; `= [];` (las etiquetas sin paquete conocido —p. ej. Optometría/Odontología— desaparecerían del todo, en vez de contar SIEMPRE como pendientes) | No | "etiquetas SIN paquete conocido en PYM_CATALOG (Optometría/Odontología) también cuentan como pendientes" — restaurado |
| — | v14.0.0 (T7): `createPymBannerUI` — quitar la guarda `if (!cacheDelPaciente) return;` (con la caché todavía del paciente ANTERIOR, repintaría el banner reutilizando esos datos VIEJOS para el paciente recién abierto, mientras el refresco real seguía en vuelo) | No | Requirió reforzar la prueba: la primera versión no distinguía "se devolvió temprano" de "se repintó con datos viejos idénticos" porque `remove()`/`removeChild()` no borran de verdad en el DOM falso del arnés (mismo hallazgo ya documentado varias veces en esta tabla), y el paciente B de la prueba resolvía su propio refresco de forma SÍNCRONA (sin ninguna actividad emparejada, sin `await` real), enmascarando la ventana de "caché todavía vieja" que el bug explota. Se corrigió dándole a B una actividad que SÍ dispara la rama con red real (`apiAccesoBuscarPaciente`, con `await` genuino) y espiando `removeChild` para detectar si el repintado llegó a ejecutarse — "al abrir un paciente DISTINTO, no reutiliza por error los pendientes del paciente ANTERIOR..." — restaurado |
| — | v14.0.0 (T7): verificación empírica en Chromium real del banner PyM en 3 variantes (pendientes con item sin CUPS/ámbar, verificación caída con aviso honesto) en modo oscuro Y claro, montado con el CSS real extraído y HTML real generado por `createPymBannerUI()` — confirma `position:sticky` con `z-index:2147481000` (var(--z-banner)), colores/fondo resueltos a los tokens de superficie correctos en ambos temas, y — el punto crítico de D5 — que el banner realmente EMPUJA el contenido simulado de Everest hacia abajo en flujo normal de documento (`everestRect.top >= bannerRect.bottom`, verificado por código, no solo visualmente) en vez de flotar encima. El aviso honesto de verificación caída (D4) se lee con contraste correcto en ambos temas | No aplica (verificación visual) | — |
| 8258/6592 | v14.0.0 (T8, auditoría de contraste WCAG — **hallazgo real, no una mutación provocada**): `.vgl-pymb-contador` (`background:var(--c-ambar);color:var(--bg-solid)`) resolvía en Chromium real con el color CASI BLANCO heredado del banner (`#f6f7fb`) en vez del `--bg-solid` propio (oscuro), porque durante la construcción de las "seis listas" de T7 se agregó por error la entrada "simple" de escudo `#vgl-pym-banner span,#vgl-pym-banner b{color:inherit}` (especificidad id+etiqueta, 101) — le gana en cascada a `.vgl-pymb-contador{color:...}` (especificidad de 1 clase, 10). Es la MISMA clase de incidente ya documentada en el propio código (comentario de línea ~6592) para v12.6.6/v12.10.2 (`#vgl-postcita-panel`/`#vgl-labsv-modal`), reincidiendo aquí porque T7 se construyó sin recordar ese precedente al llenar la lista "simple" para el banner nuevo. Detectado por auditoría real de contraste WCAG contra Chromium (no por la suite de pruebas, que no aplica cascada CSS real): ratio 1.5 (oscuro) / 2.64 (claro), ambos muy por debajo del mínimo AA de 4.5. Corregido quitando la entrada simple insegura — el banner ya tenía correctamente la entrada segura `:where(span:not([class]),...)` (línea 8258), que por construcción nunca compite con un elemento que ya tiene su propia clase — y verificado de nuevo en Chromium: ratio 12.28 (oscuro, AA+AAA) / 6.62 (claro, AA; no llega a AAA de 7 pero el badge de conteo no codifica un estado clínico por sí solo — el color no es la única señal, el número y la palabra "pendiente" ya lo son — así que AA es el estándar aplicable de §4.3.3, no AAA) | No aplica (hallazgo de auditoría, no una mutación de código) | — |
| 8258/6592 | v14.0.0 (T8, mutación de verificación de la Regla K nueva): reintroducir literalmente la línea insegura `#vgl-pym-banner span,#vgl-pym-banner b{color:inherit}` en la lista de escudos simples (el mismo texto que causó el bug de arriba) | No | "Regla K - el contador del banner PyM no pierde su color propio contra el escudo del banner" cayó nombrando el caso exacto (`#vgl-pym-banner span,#vgl-pym-banner b` con especificidad 101 no debe igualar/superar a `.vgl-pymb-contador` con especificidad 10) — restaurado, 878/878 en verde |
| — | v14.0.0 (TL1, piel de agendar/ordenar): revertir `.vgl-agm-lbl{font-size:var(--t-micro)...}` a `12.5px` literal | No | Regla G (Cascada CSS) — "No deben quedar font-size:12.5px/13.5px literales en las clases de TL1" — restaurado |
| — | v14.0.0 (TL1): revertir `#vgl-ordenar-modal .vgl-ord-title{font-size:var(--t-body)...}` (el override MÁS ESPECÍFICO, no la declaración base de `.vgl-ord-title`) a `13.5px` literal | No | Regla G — restaurado. **Hallazgo real durante esta misma tarea:** la migración inicial solo tocó la declaración base compartida `.vgl-ord-title{font-size:13.5px}` (especificidad 0,1,0); existe una segunda declaración, mucho más específica, `#vgl-ordenar-modal .vgl-ord-title{font-size:13.5px;...}` (especificidad 1,1,0) que en cascada real SIEMPRE le gana a la base — la migración de la base habría quedado sin ningún efecto visual real en el modal de ordenar. Detectado al revisar `git diff` contra el modal real, no por una prueba que fallara sola; documentado aquí porque es exactamente el tipo de trampa de especificidad que este proyecto ya vio antes (T1, ver CLAUDE.md) |
| — | v14.0.0 (TL1): verificación empírica en Chromium real de `#vgl-agendar-modal` con datos representativos (kicker/nombre de paciente/rótulo de sección/turnos/botones), en modo oscuro y claro — confirma que los tokens migrados resuelven a los valores esperados (`--t-micro`→12px, `--t-body`→14px, coherente con el valor "vigente" de 14px que T3/D1 ya dejaron documentado como pregunta abierta al médico, no el "oficial" de 13px) y que los elementos **deliberadamente NO migrados** (kicker 10.5px, nombre del paciente 25px — "jerarquía masiva", fuera de la escala estándar a propósito) se conservan sin cambio | No aplica (verificación visual) | — |
| — | v14.0.0 (TL2): `openLaboratoriosModal` — desactivar la condición del nuevo diagnóstico de Ref./Rango (`if (false) {...}`) | No | "sin ningún campo de referencia conocido, la consola vuelca las claves reales del analito" — restaurado |
| — | v14.0.0 (TL2): `openLaboratoriosModal` — quitar la condición `!referencia` del diagnóstico (dispararía SIEMPRE, incluso cuando el campo sí trae valor, ensuciando la consola en consultorio con avisos falsos en cada apertura) | No | "CON un campo de referencia reconocido, la consola NO avisa nada" — restaurado |
| — | v14.0.0 (TL2): revertir `#vgl-labs-modal .vgl-labs-srclbl{font-size:var(--t-micro)...}` a `12.5px` literal | No | Regla G (Cascada CSS) — restaurado |
| suite_05 | TAREA B5 (blindaje de `runner.js`, retomada tras quedar huérfana en PR #49 — su base estaba 190+ commits atrás de la rama compartida, nunca fusionada): quitar `async` de un `t.casoAsync` de prueba y su `await` en la llamada, simulando una prueba async lanzada "en fondo" sin que el runner la espere | Sí | runner blindado con `Promise.all(res.pendientes)` atrapa el test huérfano con código de salida 1 y su fallo listado (la comprobación en suite_26 "El banco se ejecuta de verdad" también lo atrapa, pero el punto de esta fila es que el propio arnés, no solo una suite dedicada, deja de poder ocultar una prueba async sin await) — restaurado |
| — | v14.0.0 (T5): rama actualizada contra la punta de `claude/pym-agenda-blindaje-v12-4` (T6/TL1/TL2/T7/T8/B5 fusionados después de que T5 se abrió) para mantenerla mergeable mientras espera revisión visual del médico. Conflicto real en 4 archivos: `@version`/`VERSION` (resuelto a 12.10.19), `tests/suite_15_interfaz_avanzada.js` (dos pruebas nuevas independientes insertadas en el mismo punto — se conservaron ambas, cada una en su grupo correcto: la del clic manual de Auto-Labs junto a las demás de `createLabInjectorUI`, el bloque completo de T5 después), `tests/suite_25_cascada_css.js` (los conteos de tokens de Regla G se recalcularon sobre el CSS real ya fusionado, no sumados a mano: var(--t-micro) 37, var(--t-body) 11 sin cambio por T5, var(--t-lead) 6, !important 152 — verificado ejecutando el mismo regex de la prueba contra el archivo ya fusionado antes de escribir los números) y el apéndice de esta tabla (ambos lados conservados) | No aplica (actualización de rama, no una mutación) | — |
| — | v14.0.0 (T7): rama actualizada contra la punta de `claude/pym-agenda-blindaje-v12-4` (TL1/TL2/B5 fusionados después de que T7 se abrió) para mantenerla mergeable mientras espera revisión visual del médico. `vigilante_agenda.user.js` no tuvo conflicto real (el `@version`/`VERSION` de T7, 12.10.19, ya coincidía con el de la rama compartida). Conflicto solo en `tests/suite_25_cascada_css.js` (los conteos de tokens de Regla G se recalcularon sobre el CSS real ya fusionado: var(--t-micro) 40, var(--t-body) 13, var(--t-lead) 5 sin cambio, !important 150 sin cambio — verificado ejecutando el mismo regex de la prueba contra el archivo ya fusionado) y el apéndice de esta tabla (ambos lados conservados) | No aplica (actualización de rama, no una mutación) | — |
| — | **v14.0.0 — RELEASE UNIFICADO (T4+T5+T7 fusionados juntos en la rama de despliegue, a petición del médico para usarlo en consulta real).** Conflictos resueltos por UNIÓN en las seis listas de §1.2 (los dos contenedores nuevos, `#vgl-acciones-dock` y `#vgl-pym-banner`, competían por las mismas líneas: tokens oscuros, tokens claros, `prefers-reduced-motion`, `box-sizing`, armadura `:where()`, y `applyTheme()`) y por recálculo real en Regla G (micro 36+1+4=41, body 11+2=13, lead 5+1=6, !important 150+2=152 — ejecutando el regex de la propia prueba contra el CSS ya fusionado, nunca sumando las cifras de cada rama por separado). Verificado además que los DOS bloques CSS (`#vgl-acciones-dock{` y `#vgl-pym-banner{`) sobrevivieron íntegros al merge | No aplica (integración, no una mutación) | — |
| 12504 | v14.0.0 (secuela de T4, reportada por el médico como "el margen inferior"): `render()` — volver a emitir SIEMPRE `<div class="vgl-card-btm">`, en vez de solo cuando `actions` tiene contenido. La fila llevaba antes de T4 los botones Y los chips de PyM; T4 se llevó ambos y dejó el contenedor, así que en las tarjetas sin `citaId` (ruta de respaldo por scraping del DOM, sin botón Atender que ofrecer) quedaba un div VACÍO cobrando su `margin-top:7px` | No | "v14 — una tarjeta SIN botón Atender no emite la fila inferior vacía (hueco muerto al pie)" — cayó nombrando el caso ("la fila inferior NO se emite... obtuvo true"); restaurado, 903/903 en verde. **Medido en Chromium con el CSS real antes de tocar nada** (no elegido a ojo): tarjeta sin botón 96.3px antes -> 89.5px después, 6.8px por tarjeta; en un panel de 20+ pacientes son ~136px de scroll. **Hallazgo del propio análisis: el padding NO había que tocarlo.** Los huecos ópticos ya eran 13.6px arriba / 11.6px abajo en las TRES variantes (con botón, sin botón, y con el div vacío) — el hueco muerto era íntegramente el margen del div vacío, no un desbalance de padding. Cambiar el padding "para reajustar el ritmo" habría desbalanceado una tarjeta que ya estaba bien |
| 6732/7284 | v14.0.0 (**BUG REAL BLOQUEANTE, encontrado por la verificación en Chromium del release unificado — y que la auditoría T8 NO cazó**): `#vgl-acciones-dock` y `#vgl-pym-banner` se pintaban con `background:var(--surface-2)` a secas. `--surface-*` son VELOS (~92% transparentes, §4.3 los define como "tarjeta") pensados para ir SOBRE el `--bg` opaco que paga `#vgl-root`; estos dos contenedores cuelgan de `document.body` y NO tienen ese `--bg` debajo, así que su fondo real era **el que pintara Everest**. Medido en Chromium con host blanco + tema oscuro: título del banner, nombres de actividad, aviso, botones del dock y anillo ámbar caían a ratios **1.05–2.44** (texto invisible) — 6 de 9 elementos. Y por ser `position:sticky`, al hacer scroll las filas clínicas de Everest se leían A TRAVÉS del banner, sobreimpresas sobre el aviso de PyM (riesgo de lectura clínica equivocada, no solo estético). Prueba de que era un olvido y no un diseño: la propia hoja ya tenía la base opaca en `#vgl-pym-banner.perf{background:var(--bg-solid)}`, que subía ese mismo título de 1.05 a 18.42 — el banner solo era legible en modo rendimiento. **Por qué T8 no lo vio: su página sintética tenía el lienzo oscuro, es decir midió justo la combinación que NO ocurre en producción** (Everest pinta claro). Corregido apilando el velo sobre una base opaca: `background:linear-gradient(0deg,var(--surface-2),var(--surface-2)),var(--bg-solid)` — mismo aspecto, fondo propio | No aplica (hallazgo de verificación, no una mutación provocada) | Verificado tras el arreglo en Chromium sobre 4 combinaciones (host blanco/oscuro × tema claro/oscuro): **0 elementos bajo AA**, y los ratios salen IDÉNTICOS con host claro y oscuro (18.42/16.4/12.28/16.4/10.93/7.92 en oscuro; 17.49/16.06/6.62/16.06/6.08/5.08 en claro) — prueba de que el fondo ya no depende del host. `elementFromPoint` en el centro del banner tras 900px de scroll devuelve un elemento DEL BANNER, ya no el contenido de Everest. De regalo, `.vgl-dock-toggle` en tema claro pasó de 4.11 (bajo AA, la llamada de juicio que T8 había dejado documentada) a 5.08 |
| 6732/7284 | v14.0.0 (mutación de verificación de la Regla L nueva): revertir ambos fondos a `background:var(--surface-2)` (el bug exacto de arriba) | No | "Regla L - los contenedores pegados a document.body traen fondo opaco propio, no solo un velo --surface-*" — cayó nombrando el caso con la declaración literal (`Declaración actual: "background:var(--surface-2)"`); restaurado, 902/902 en verde |
| 12639 | v14.0.0 (**mutación obligatoria de la red de seguridad #1** — el interruptor): en `tick()`, quitar el `checkRecordatorioPym();` de la rama `else` (banner apagado por Ajustes). El médico apagaría el banner y se quedaría SIN banner y SIN aviso modal: cero alertas de PyM, en silencio | No | "tick (banner APAGADO por el interruptor): quita el banner y DEVUELVE el aviso modal — nunca se queda sin ninguno" — cayó nombrando el caso exacto ("y el aviso modal de siempre VUELVE en el mismo tick... obtuvo false"); restaurado, 901/901 en verde |
| 12635 | v14.0.0 (**mutación obligatoria de la red de seguridad #2** — el fallo del banner): `if (!bannerPintado) checkRecordatorioPym();` &rarr; `if (false) checkRecordatorioPym();`. Con el banner lanzando en la página real (el riesgo del contenedor raíz de Angular que nunca se pudo verificar), la excepción se atraparía pero NADIE cubriría el hueco: la alerta clínica desaparecería sin dejar rastro visible | No | "tick (el banner LANZA): se atrapa y cae al aviso modal en el MISMO tick — la alerta clínica no se pierde en silencio" — cayó nombrando el caso exacto ("con el banner roto, el aviso modal cubre el hueco... obtuvo false"); restaurado, 901/901 en verde |
| — | **v14.0.0 — RED DE SEGURIDAD del banner PyM (interruptor de emergencia, pedido explícito del médico antes de estrenarlo con pacientes reales).** El riesgo cubierto es concreto y estaba documentado como no verificable: T7 apaga `checkRecordatorioPym()` y su inserción como primer hijo de `document.body` asume que el contenedor raíz de Angular de Everest no es `position:fixed`/`height:100vh` — suposición imposible de comprobar sin abrir la página real. Si fallara en consulta, el médico perdería la alerta de PyM **en silencio**, justo lo que D4 prohíbe. Se añadió: (a) `S.bannerPym` en DEFAULTS (true de fábrica); (b) interruptor en Ajustes que además quita el banner ya pintado en el acto, sin esperar recarga; (c) en `tick()`, banner apagado -> `checkRecordatorioPym()` vuelve, y si `createPymBannerUI()` LANZA se atrapa, se reporta por `reportarError` y se cae al aviso modal en el MISMO tick. Invariante nuevo y probado: **nunca queda sin ninguna de las dos alertas** | No aplica (mitigación de riesgo) | — |
| — | **v14.0.0 — HALLAZGO DE ARNÉS (no del script): `remove()` era un no-op absoluto en `tests/harness.js` (`remove() {}`, línea 37) desde siempre — ningún elemento falso guardaba referencia a su padre, así que `.remove()` no tenía de dónde quitarse.** Esto enmascaraba, en TODA la batería de pruebas (no solo esta suite), cualquier código de producción que dependiera de que `.remove()` funcionara de verdad. Se corrigió el arnés para que `appendChild`/`insertBefore` guarden `c._parent = this` y `remove()` llame a `this._parent.removeChild(this)`, replicando el comportamiento real del DOM | No aplica (corrección del arnés de pruebas) | Efecto colateral esperado y corregido: `runner.js#igual` comparaba con `JSON.stringify`, que ahora truena con "Converting circular structure to JSON" en cualquier prueba que comparara dos nodos DOM por identidad (`t.igual(nodoA, nodoB)`) porque `_parent` cierra un ciclo — se añadió un atajo `if (a === b) return;` antes del `JSON.stringify`, más correcto además para comparar identidad de objetos que un serializado. Y `vglExportLogs` (Suite 10) hace `appendChild+click+remove()` en el mismo tick (patrón real y correcto de descarga por ancla efímera): con `remove()` ya funcionando de verdad, la prueba ya no encontraba el ancla buscándola en `body.children` DESPUÉS de que la función ya la había limpiado — se corrigió capturándola en el momento de creación (interceptando `createElement`), no después |
| 10930 | v14.0.0 (D2, sondeo en segundo plano de `renderDayChips`/`_sondearAgendaDeCadaDia` — mutación de verificación #1, la guarda de seguridad ante fallo): en el `catch` del sondeo por día, cambiar `hayAgenda = true;` por `hayAgenda = false;` (invierte la promesa de "un fallo de red nunca oculta un día") | No | "un fallo de red en el sondeo NO oculta el día — se deja tal cual" cayó nombrando el caso ("esperaba 18 y obtuvo 1"); restaurado. **Nota honesta:** esta misma mutación, aplicada primero por error sobre la línea equivocada (el valor inicial de `hayAgenda`, muerto por reasignación posterior — ver hallazgo de abajo), sobrevivió; quedó sin restaurar durante la investigación del hallazgo de `pageFetchJson` y provocó que la prueba fallara por una razón distinta a la que se estaba depurando en ese momento — hasta encontrarla con `git diff`/comparación línea a línea contra una copia limpia. Se documenta como recordatorio del propio proceso: restaurar CADA mutación antes de seguir a la siguiente, sin excepción, incluso bajo interrupciones |
| — | v14.0.0 (D2, **hallazgo real, no una mutación provocada**): la promesa de "un fallo de red nunca oculta un día" estaba rota en la raíz — `pageFetchJson`/`_pageFetchJsonCore` NUNCA lanzan (tragan 4xx, 5xx agotados y excepciones de `fetch`, devuelven `null` siempre), así que el primer diseño de `apiAccesoBuscarCitasDisponibles` con un `try/catch` alrededor de `pageFetchJson` no tenía ningún `catch` que disparar jamás: un fallo de red real se traducía en `null` → `extractAgendasList(null)` → `[]` → `hayAgenda=false` → el día se ocultaba igual que uno confirmado vacío, exactamente lo que el diseño decía evitar. Corregido añadiendo un cuarto parámetro `propagarError` a `apiAccesoBuscarCitasDisponibles` (por defecto `false`, compatible con sus otros dos llamadores): cuando la segunda vía devuelve `null` (ninguna respuesta real, sea por qué sea) Y `propagarError` es `true`, ahora SÍ lanza, para que el sondeo distinga "no se sabe" de "confirmado vacío". El sondeo pasa `true`; `cargarHoras()` y el tercer llamador (línea ~12257) no lo pasan y conservan su comportamiento de siempre | No aplica (hallazgo de arquitectura, corregido antes de que hubiera mutación que probar) | — |
| 9103 | v14.0.0 (D2, mutación de verificación #2, la distinción real/`null`): en `apiAccesoBuscarCitasDisponibles`, anular la condición `propagarError && res2 == null` (con `false &&` por delante, nunca se cumple) | No | Misma prueba de arriba ("un fallo de red en el sondeo NO oculta el día") cayó de nuevo con "esperaba 18 y obtuvo 1"; restaurado |
| 10934 | v14.0.0 (D2, mutación de verificación #3, el chip activo protegido): en la remoción del sondeo, cambiar `if (btn && !btn.classList.contains("active")) btn.remove();` por `if (btn) btn.remove();` (quita también el chip que el médico tiene seleccionado en ese momento) | No | Prueba nueva "el chip del día ACTIVO nunca se retira, aunque el sondeo confirme que está vacío de verdad" cayó nombrando el caso; restaurado. **Hallazgo colateral real:** esta prueba, en su primera versión, fallaba también contra el código SIN mutar — no por un bug de producción, sino porque el botón "centro" marcaba `active` concatenándolo al `className` en el string inicial (`" active"`) y solo usaba `classList.add/remove` después (clic). En un navegador real ambas vistas están sincronizadas siempre; en el arnés de pruebas son dos estructuras independientes (`className` string vs. `classList._s` Set), así que `classList.contains("active")` daba `false` justo después de crear el botón. Se corrigió el propio script para marcar `active` SIEMPRE por `classList.add()`, nunca por concatenación de string — más consistente con el resto del código (el clic ya usaba `classList`) y evita depender de la sincronización automática que el arnés no reproduce |
| 1139 | v14.0.0 (Auto-Labs, Fósforo — reportado por el médico en consultorio: "no autocompleta los resultados del examen Fósforo (P) - Fósforo Inorgánico (Fosfatos)"): agregar el nombre real que entrega Athenea ("FOSFORO INORGANICO") a `WHITELIST_13_LABS.FOSFORO.names`, luego mutar ese nombre para que ya no case (`"XFOSFORO INORGANICOX"`) | No | "Fósforo casa aunque Athenea lo entregue como 'FOSFORO INORGANICO (FOSFATOS)'" cayó nombrando el caso ("obtuvo false"); restaurado. La lista solo traía "FOSFORO EN SUERO"/"FÓSFORO EN SUERO" — ninguno de los dos es substring del nombre real que reporta Athenea, así que `_matchLabInWhitelist` nunca casaba por nombre (los CUPS 2031/903837 tampoco, por eso el código no bastaba). La guarda de orina (línea ~1994) ya protege este nombre más genérico de casar por error con un fósforo EN ORINA, si existiera esa casilla |
| 11888 | v14.0.0 (openOrdenamientoModal, CRUCE ANTIDUPLICADO al ordenar — pedido explícito del médico, reutiliza la misma consulta/regla que T6/T7 usan para el banner): quitar `&& !yaVigente` de `marcar = hayCoincidencia && !chocaSexo && !yaVigente` (deja que un paquete YA vigente en Everest se premarque igual, como antes de este cambio) | No | "un paquete YA vigente en Everest (RCV exprés, orden reciente) no se premarca y avisa" cayó nombrando el caso ("obtuvo true"); restaurado. Antes de este cambio, `openOrdenamientoModal` no cruzaba NADA contra Everest — un paquete con una orden vigente reciente (dentro de su propia vigencia clínica) podía volver a marcarse y duplicarse sin ningún aviso, el mismo tipo de hueco que T6/T7 ya habían cerrado para el banner pasivo pero no para el modal donde realmente se ordena |
| 11857 | v14.0.0 (mismo cruce, mutación #2 — la lógica de inversión): cambiar `pendientesEnEverest.indexOf(p) === -1` por `!== -1` en la construcción de `yaVigentesEnEverest` (invierte qué paquetes cuentan como "ya vigentes") | No | La misma prueba de arriba cayó, y de paso también "con coincidencia, el choque de sexo desmarca y advierte" y "la etiqueta oficial 'Tamización cardiometabólica' premarca Z108" (3 pruebas en rojo) — la inversión marca como vigente casi todo lo que NO lo está y viceversa; restaurado, 920/920 en verde. **Hallazgo del propio proceso (no una mutación provocada):** las dos primeras versiones de estas pruebas nuevas colgaban el runner indefinidamente (timeout, sin fallar ni terminar) porque `cargar({...})` se llamó sin el mock `gmxhr` — sin él, el `GM_xmlhttpRequest` por defecto del arnés es un no-op absoluto que nunca resuelve ni rechaza la promesa de respaldo dentro de `_pageFetchJsonCore` cuando el `fetch` mockeado lanza, así que la llamada de red simulada como "caída" quedaba colgada para siempre en vez de fallar rápido. Se corrigió añadiendo `gmxhr: (o) => { if (o.onerror) o.onerror(...); }` a ambos `cargar()` nuevos, el mismo patrón boilerplate que ya usan todas las demás pruebas de este archivo |
| 1263 | v14.0.1 (`_agruparUroanalisisParaTabla`, deduplicación por componente repetido — reportado en consultorio EN VIVO con pantallazo: el bloque de uroanálisis mostraba "GLUCOSA EN SUERO..." y "CREATININA EN SUERO..." cada uno DOS veces, amontonando la rejilla): sustituir la guarda `if (previo && !(resultDate && (!previo.resultDate || resultDate > previo.resultDate))) return;` por `if (false) return;` (nunca se salta, siempre sobrescribe con el último procesado) | Sí, en la primera versión de la prueba | La primera prueba nueva ("solicitud vieja + nueva, sobrevive el más reciente") **sobrevivió a la mutación** porque su arreglo traía la vieja PRIMERO y la nueva SEGUNDA — con la mutación, sobrescribir sin mirar fecha igual dejaba la más reciente al final por pura coincidencia de orden, no porque la regla funcionara. Se añadió una segunda prueba con el orden INVERTIDO (la vieja llega de última en el arreglo): ahí sí cae la mutación ("esperaba NEGATIVO y obtuvo NEGATIVO (SOLICITUD VIEJA)") — restaurado, 923/923 en verde. Antes de este cambio, Athenea mandaba una fila por CADA solicitud histórica del mismo componente (una vieja, una nueva) y nada las deduplicaba: el bloque agrupado del uroanálisis mostraba literalmente el doble de líneas de las que existen en realidad |
| — | v14.0.1 (mismo reporte en vivo, hallazgo de CSS — no una mutación provocada): `.vgl-labs-uro{grid-template-columns:repeat(auto-fill,minmax(198px,1fr))}` fuerza un MÍNIMO de 198px por columna de la rejilla; con menos ancho disponible en la celda (`#vgl-labs-modal .vgl-agm-card{max-width:1120px}` sigue acotado por `width:92vw`, y la columna de Resultado en `table-layout:fixed` es lo que sobra tras los otros 4 anchos fijos/porcentuales), la rejilla desborda en vez de encogerse — la barra de scroll horizontal y la columna "Informe" cortada del pantallazo son ese desborde, no un defecto de la tabla en sí. Bajado a `minmax(150px,1fr)` y añadido `gap` vertical explícito (antes 0, las filas quedaban pegadas contra el borde inferior de la anterior) | No aplica (hallazgo de CSS, corregido directamente — no hay forma de mutar-y-probar un ancho de rejilla contra el arnés de pruebas, que no calcula layout real) | — |
| 8876 | v14.0.1 (festivos — faltaba San Pedro y San Pablo trasladado de 2027): mutar `"2027-07-05"` a `"2027-07-06"` en la tabla `FESTIVOS` | No | "esFestivo: 2027-07-05 (San Pedro trasladado) es festivo" cayó ("se esperaba verdadero, obtuvo false"); restaurado, 924/924 en verde. El 29 de junio (San Pedro y San Pablo, Ley Emiliani) se traslada al lunes siguiente; en 2026 el 29 YA cae lunes (sin traslado, por eso "2026-06-29" ya estaba en la tabla) pero en 2027 cae martes, así que el trasladado real es el 5 de julio — festivo nacional confirmado, no uno de los dos pendientes que el médico aún debe confirmar (2026-07-13, 2027-07-12) |
| 10671 | v14.0.1 (`cargarHoras`, **mutación obligatoria de la red de seguridad** — reportado en consultorio EN VIVO con pantallazo: antes, un día sin agenda propia caía a mostrar los turnos de OTRO profesional con solo un aviso rojo, y un clic distraído podía agendar al paciente con el médico equivocado): sustituir `if (misAgendas.length) { agendasFiltradas = misAgendas; } else {` por `if (true) { agendasFiltradas = misAgendas.length ? misAgendas : agendasDelDia; } else {` (revierte al comportamiento viejo: si no hay agenda propia, se ofrecen igual los turnos ajenos) | No | Las dos pruebas nuevas de v14.0.1 cayeron nombrando el caso exacto: "nunca se muestra un turno de otro profesional, ni de paso ni como último recurso (obtuvo true)" y su equivalente en el caso "ningún día tiene agenda propia"; restaurado, 926/926 en verde |
| — | v14.0.1 (hallazgo de arnés, no una mutación provocada): las dos pruebas nuevas de "agenda ajena bloqueada + salto automático" fallaban también SIN mutar nada, escritas originalmente con `slots.textContent.includes(...)`. En `tests/harness.js`, `textContent` es una propiedad estática inicializada en `""` (línea 33) — nunca se deriva de `innerHTML` ni de los hijos añadidos por `appendChild`, a diferencia de un DOM real. El código de producción arma cada turno con `btn.innerHTML = labelCompleto` (igual que el resto del archivo), así que leerlo por `.textContent` en el contenedor devolvía siempre cadena vacía, mutara o no mutara el código. Corregidas para leer `[...slots.children].map(c => c.innerHTML).join(" ")` en el caso de los botones armados por `appendChild`, y `slots.innerHTML` (sin cambios, ya funcionaba) en el caso del mensaje de error armado por asignación directa — mismo patrón que ya usa el resto de esta suite (`botonTurno.innerHTML`, nunca `.textContent`) | No aplica (corrección de las pruebas nuevas, no del arnés en sí) | — |
| 11024 | v14.0.2 (`_sondearAgendaDeCadaDia`, gap documentado desde v14.0.1: el sondeo en segundo plano decidía "hay agenda" con CUALQUIER agenda de la respuesta, propia o ajena — un sábado con agenda de OTRO profesional se seguía ofreciendo como chip, y al pulsarlo `cargarHoras()` lo bloqueaba igual y saltaba a otro día): revertir `hayAgenda = selectedEspId === 12 ? _agendasPropias(agendasDelDia, doctorName).length > 0 : agendasDelDia.length > 0;` a `hayAgenda = agendasDelDia.length > 0;` (vuelve a contar cualquier agenda, propia o ajena, como "hay agenda") | No | "el chip del sábado con agenda solo ajena se retiró — la misma regla de 'propia' que bloquea el día central también aplica al sondeo en segundo plano" cayó nombrando el caso ("obtuvo true"); restaurado, 927/927 en verde |
| — | v14.0.2 (retiro del botón "Atender", pedido explícito del médico: "ya no lo necesito", usa directamente el botón nativo "Historias Clínicas" de Everest). Se retiraron `apiMedicoAbrirHistoria`, `isAtencionAbiertaHoy`/`markAtencionAbiertaHoy`, el `citaId`-dedup, la generación del botón y su listener de clic en `render()`, y los selectores CSS `.vgl-btn-atender` (los combinados `.vgl-btn-action,.vgl-btn-agendar,.vgl-btn-ordenar` sobreviven — ya estaban parcialmente muertos por la amputación de T4, no es alcance de este cambio). Se retiraron con él 9 pruebas dedicadas (2 en suite 09, 3 en suite 13, 4 en suite 15) y se reescribieron 2 más que asumían que el botón seguía existiendo. **Mutación de verificación** (no una mutación real del código final, sino una comprobación de que las pruebas nuevas no quedaron vacías de contenido): reinsertar un `<button class="vgl-btn-atender">` de prueba en la plantilla de la tarjeta | No | Las dos pruebas nuevas ("la tarjeta ya NO genera ningún botón de acción" y "nunca emite la fila inferior vacía") cayeron nombrando el caso ("obtuvo true"); restaurado, 918/918 en verde. `citaId`/`citaIdKey` (captura desde `ObtenerConsultas`) se conservan sin consumidor propio — el dato viene gratis en la respuesta y puede volver a hacer falta para el buscador de Conducta |
| — | v14.0.2 (`CUPS_ESCRITURA_RENAL_PENDIENTE_ESTADIO` — el médico señaló que la confirmación de los 4 CUPS de HbA1c/PTH/Fósforo/Albúmina que se le había pedido por captura de red YA estaba dada, en `teamwork_projects/sync_copiloto_vigilante/MATRIZ_DIVERGENCIAS.md` del repo Copiloto: catálogo `NAME_TO_CUPS`/`CUPS_EMBEBIDOS`, ya en producción del lado del Copiloto para ORDENAR estos analitos, con test propio `tests/test_catalogos_embebidos.py`). Se agregó el bloque documentado con los 5 códigos de escritura (distintos de los de lectura de `WHITELIST_13_LABS`: PTH 904921→903890, Fósforo 903837→903885, Albúmina 903801→903803; Hemoglobina no diverge, 902213 en ambos catálogos de escritura). Deliberadamente SIN conectar a ningún botón: el propio médico resolvió el 2026-08-11 (`DECISIONES_PENDIENTES.md`, P4) que Vigilante solo debe ordenar estos 4 cuando el estadio ERC esté confirmado (P6, sin implementar) — este bloque solo deja los códigos listos, correctos y probados para cuando esa pieza exista. Mutación de verificación: cambiar `PTH: "903890"` a `PTH: "904921"` (el código de lectura, no el de escritura) | No | "CUPS_ESCRITURA_RENAL_PENDIENTE_ESTADIO: los 5 códigos de escritura confirmados con el Copiloto, distintos de los de lectura" cayó ("esperaba '903890' y obtuvo '904921'"); restaurado, 919/919 en verde |
| 1218 | v14.0.3 (`_conductaBuscarYAgregarExamen` — pedido explícito del médico: automatizar en el DOM real de Conducta el mismo gesto de clic `<li>examen→Agregar` que él ya hace a mano, usando el texto LITERAL capturado en consultorio el 12-08-2026 al agregar PTH/Fósforo/Albúmina/Hemoglobina/HbA1c además del paquete HTA — `captura_ordenamiento_paquete_HTA_20260812.json`, `EVIDENCIA_ORDENAMIENTO_CURADO.md` §2/§4. No llama a ningún endpoint propio: el guardado real ocurre cuando el médico guarda el JSON completo de la historia, igual que si hubiera hecho los clics él mismo). **Mutación de la garantía de seguridad clínica** (coincidencia EXACTA, nunca por aproximación — un clic equivocado en un catálogo real podría ordenar el examen incorrecto a un paciente real): cambiar `_canonTexto(el.textContent) === claveObjetivo` por `.includes(claveObjetivo)` | No | "coincidencia EXACTA de texto, nunca por substring — un examen parecido no debe clickearse" cayó ("obtuvo true"); restaurado, 926/926 en verde. También se descubrió y corrigió, antes de esta tabla, un fallo del PROPIO proceso de pruebas: las 6 pruebas nuevas de esta función se escribieron sin `await` delante de `t.casoAsync(...)` — la prueba centinela "todo t.casoAsync se invoca con await" lo detectó nombrando las 6 líneas exactas, y sin el `await` las pruebas corrían en carrera contra el mismo mock compartido (`c.env.doc.querySelectorAll`) de la suite, dando resultados falsos (2 de las 6 fallaban por la carrera, no por el código de producción) |
| — | v14.0.4 (**hallazgo real, no una mutación provocada** — encontrado al revisar `EVIDENCIA_ORDENAMIENTO_CURADO.md` §5/§8 para ver qué más estaba pendiente en el repo, no por un aviso del médico): el `HBA1C: "904426"` que v14.0.2 había copiado del catálogo del Copiloto (`NAME_TO_CUPS`, descripción corta "HEMOGLOBINA GLICOSILADA") CONTRADECÍA un código que este mismo archivo ya tenía vigente y en producción desde antes — `PYM_CATALOG` → paquete I10X (RCV exprés) → `{ codigo: "903426", desc: "Hemoglobina Glicosilada Automatizada" }`, tomado de una orden REAL ya guardada en Everest (`ObtenerOrdenamientoPorPacienteIdVigente`, la fuente más confiable posible según el propio criterio del proyecto). Mismo patrón semi/automatizado ya visto dos veces (LDL 903816/903817, Microalbuminuria 903028/903026): 904426 es probablemente la variante SEMI/genérica del Copiloto, no la que este médico ordena. Corregido a `903426`, coincidiendo además con el texto del `<li>` capturado ("HEMOGLOBINA GLICOSILADA AUTOMATIZADA"). Mutación de verificación: revertir a `"904426"` | No | Las dos pruebas nuevas ("la orden REAL ya guardada en Everest manda sobre el catálogo corto del Copiloto" y "coincide con el código YA vigente en PYM_CATALOG/I10X") cayeron nombrando el caso ("esperaba '903426' y obtuvo '904426'" en ambas); restaurado, 927/927 en verde. Se añadió además una prueba de consistencia cruzada permanente entre los dos sitios donde HbA1c aparece en el archivo, para que una divergencia futura caiga sola sin depender de que alguien la note a mano |
| 12664 | v14.0.4 (`AUDITORIA_MOTOR_RCV_v68.md` §7.4 — hallazgo de la auditoría: la tabla `FESTIVOS` está codificada a mano por años, y `esFestivo` ya avisaba de que caducó pero SOLO por `console.warn`, invisible sin DevTools abierto). Se agregó un aviso VISIBLE en Ajustes ("Tabla de festivos colombianos"), calculado del propio contenido de `FESTIVOS` (nunca de un año fijo aparte que pudiera desincronizarse). Mutación de verificación: forzar `vencida = false` incondicionalmente | No | "al llegar (o pasar) el último año cubierto, el aviso se vuelve visible" cayó ("obtuvo false"); restaurado, 929/929 en verde |
| 6745 | v14.0.5 (`INFORME_AUDITORIA_T8.md`, llamada de juicio #1 — **decidida por el médico**: `--fg3` medía 4.11 en Chromium real sobre el dock en tema claro, bajo el mínimo AA de 4.5. La auditoría exigía que el arreglo fuera GLOBAL —el token— y no un parche a `.vgl-dock-toggle`, porque `--fg3` lo comparten ~30 usos incluido `.vgl-toast-x`). Corregido `--fg3: #5b6b80 → #4a5a6e` solo en el bloque de tema claro (4.37 → 5.66 sobre el dock, 7.05 sobre blanco = AAA; el tema oscuro ya iba holgado en 6.87 y no se tocó). Mutación de verificación: revertir el token a `#5b6b80` | No | "Regla O - el token --fg3 cumple el mínimo AA (4.5:1) sobre el fondo del dock en AMBOS temas" cayó nombrando el ratio exacto ("da 4.37:1, por debajo del mínimo AA de 4.5:1"); restaurado, 930/930 en verde. **La guarda nueva no comprueba una cadena: calcula el ratio WCAG 2.x de verdad** (linealización sRGB + composición alpha del velo `--bg3` sobre `--bg-solid`), leyendo los tokens del propio CSS, en los DOS temas, y exige además que `--fg3` siga menos contrastado que `--fg2` para que un arreglo de contraste no se pague invirtiendo la jerarquía visual. **Hallazgo del propio proceso:** la primera versión de la prueba envolvía la rama del tema oscuro en un `if (leído)` — el selector del bloque oscuro no casaba (`:root,#vgl-root` en vez de `#vgl-root,#vgl-lab-injector`), así que esa mitad NO se ejecutaba y daba verde sin medir nada. Se corrigió el selector y se sustituyeron los `if` por aserciones explícitas de que los tokens SÍ se leyeron, para que un cambio de forma futuro falle en voz alta en vez de saltarse en silencio |
| 2760 | v14.1.0 (R1b — **el médico eligió COCKCROFT-GAULT** el 14-ago-2026 como fórmula que gobierna el estadio renal, frente a CKD-EPI que no habría necesitado el peso). Se implementó la plomería que faltaba: `apiHcObtenerSignosVitales` (endpoint del PESO, verificado en vivo en consultorio el 12-08-2026), `_pesoDeSignosVitales`, `_labNumerico` y el orquestador `estadioRenalDelPaciente`. **Mutación de la garantía clínica más peligrosa**: sustituir `const creatN = _labNumerico(creatininaCruda); if (creatN == null) faltan.push("creatinina")` por `Number(creatininaCruda)` sin guarda — es el camino por el que una creatinina "PENDIENTE" se volvería `NaN` → centinela 0 → `estadioKDIGO(0)` = **G5**, el estadio MÁS GRAVE, en un paciente sano | No | Cayeron 4 pruebas, dos nombrando el caso exacto ("una creatinina no numérica NO se degrada a G5": esperaba `["creatinina"]` y obtuvo `["tfg_no_evaluable"]`); restaurado, 952/952 en verde. **Hallazgo valioso de la propia mutación:** aun con la primera guarda rota, el `estadio` NO llegó a G5 — la segunda guarda (`if (!(tfgCG > 0))`, que trata el centinela 0 como "no evaluable" en vez de degradarlo) lo atrapó igual. Es defensa en profundidad real, no redundancia decorativa: hacen falta DOS errores independientes para producir un G5 falso |
| — | v14.1.0 (**dos bugs reales encontrados por las pruebas nuevas, no por mutación provocada**): (1) `_labNumerico("-1.2")` devolvía **1.2** — el saneo quita todo lo que no sea dígito/coma/punto, así que el signo se perdía y una basura salía convertida en un valor plausible. Corregido rechazando de plano cualquier negativo (`/-\s*\d/`), porque ningún analito de la lista puede serlo. (2) `estadioRenalDelPaciente(null)` **lanzaba**: el `= {}` de la firma solo cubre `undefined`, no `null`, y desestructurar null revienta — el caso obvio es un llamador que le pase el resultado de otra función que devolvió null. Corregido con `(opts \|\| {})`. Ambos anclados con prueba propia | No aplica (bugs hallados y corregidos antes de existir mutación que probar) | — |
| 9695/9818/10460 | v14.1.1 (R1b — **el motor renal deja el modo sombra**): `estadioRenalDelPaciente` existía, probado, y NO lo llamaba nadie. Se añadieron las piezas que le faltaban para tener entradas reales: `apiAccesoObtenerDemograficos` (la EDAD ya venía en la respuesta de `BuscarPacienteDetallado`, que el script YA llamaba desde hacía versiones, y nadie la leía), `_creatininaDeLabs` (reutiliza `_ultimaFechaPorAnalito`, que ya descarta PENDIENTE y se queda con la fecha más reciente) y `calcularEstadioRenal` (ensamblador; las dos consultas van en paralelo y cacheadas, y la creatinina sale de los labs que el modal ya trajo, así que no añade ni una consulta de laboratorio). Primer llamador de producción: el modal de laboratorios, lanzado SIN await para no retrasar la tabla que el médico vino a ver | No aplica (conexión de algo ya probado) | Las guardas CSS del propio proyecto cazaron dos infracciones mías al conectarlo: **Regla E** (6 declaraciones de `color` fuera de `#vgl-root` sin `!important` — el CSS de Everest puede ganarles) y **Regla G** (3 `--t-micro` y 1 `--t-body` nuevos sobre anclas fijas). La Regla E se arregló poniendo `!important`, NO añadiéndome a su lista `BASE_CONOCIDA`, que es de infracciones heredadas. **Hallazgo del propio proceso:** el primer intento de poner esos `!important` con un `replace` de texto se los aplicó a `.vgl-fchip.sel`, una regla AJENA con el mismo fragmento CSS, y dejó la mía intacta — lo detectó la misma Regla E al seguir contando 75 en vez de 74; revertido y aplicado en la regla correcta |
| 2878/2882 | v14.1.2 (**tablas oficiales entregadas por el médico el 14-ago-2026** — Tabla 39 HTA, Tabla 43 DM, Tabla 50 ERC por estadio, con la instrucción "se debe respetar al 100%, esto tiene que ser determinista"). Al cotejar celda por celda, `RCV_VIGENCIA_ESTADIO_TABLA` divergía de la fuente en cuatro sitios. Mutación de verificación: revertir las dos correcciones numéricas (LDL en E4 de 120 a 180, y HbA1c de 180 a "BLOQ" en G1/G2) | No | Cayeron las dos pruebas nombrando la celda exacta ("Tabla 50: LDL en Estadio 4 = 120 días: esperaba 120 y obtuvo 180" y "esperaba 180 y obtuvo BLOQ"); restaurado, 955/955 en verde |
| — | v14.1.2 (**las cuatro divergencias halladas al cotejar contra la Tabla 50 — no son mutaciones provocadas, son errores que estaban en el código**): (1) **LDL en Estadio 4 decía 180, la fuente dice 120** — hacía que en el estadio más avanzado el LDL se pidiera con MENOS frecuencia que el colesterol total y los triglicéridos de su misma fila, que es la incoherencia que delató el error. (2) **HbA1c tenía "BLOQ" en G1/G2**, una segunda compuerta que la fuente no tiene: el "(solo para diabéticos)" de la tabla es condición de PACIENTE y ya la aplica `vigenciaPorEstadio` con `esDM2`, así que un diabético en estadio renal temprano se quedaba sin HbA1c. (3) **Faltaban dos filas enteras**: `hematocrito` (365/365/365/365/180) y `depuracion_creatinina_orina24h` (365/180/180/180/90). (4) **La fila se llama "Micro albuminuria", no "relación albúmina/creatinina"**: son analitos distintos (concentración vs. cociente) y este proyecto ya trata esa distinción como crítica; las tablas de HTA y DM sí piden la RELACIÓN, solo la de ERC pide microalbuminuria. Se respetó cada tabla como está escrita en vez de unificarlas por parecido de nombre, y se fijó con prueba que `rac` devuelve null en ERC para que nadie lo "arregle" añadiéndola. **Queda marcado para el médico**, no corregido: `ecocardiograma: 365` y `acido_urico: "BLOQ"` del bloque HTA NO están en la Tabla 39 (que tiene 9 filas exactas); el ácido úrico es inocuo porque "BLOQ" equivale a no estar, pero el ecocardiograma sí cambia el comportamiento | No aplica (errores hallados al cotejar contra la fuente) | — |
| 2802 | v14.1.3 (**hallazgo de la auditoría de Gemini 3.7 Flash, verificado**): `estadioKDIGO` no tenía guarda de entrada. TODA comparación de orden con `NaN` es `false` en JavaScript, así que un valor no numérico caía por los seis `if` y salía por el `return "G5"` del final — **el estadio MÁS GRAVE a partir de un dato que ni siquiera se pudo leer**. `estadioRenalDelPaciente` ya lo blindaba por fuera, pero la función es pública y pura. Añadido `if (!Number.isFinite(v) \|\| v <= 0) return null;` | No | Pruebas nuevas "un valor NO evaluable devuelve null, JAMÁS G5" (8 entradas: 0, NaN, null, undefined, "", "PENDIENTE", "> 60", -5) y su contraparte "una TFG real muy baja SÍ es G5", para que la guarda no se pase de celosa; 957/957 en verde |
| 9830 | v14.1.3 (**el hallazgo más valioso de la auditoría de Gemini, verificado y de riesgo clínico real**): las dos fórmulas renales asumen la creatinina en **mg/dL**, y el script descarta el campo `unidades` de Athenea desde v12.0.0. Si un laboratorio reportara en **µmol/L** —la otra unidad de uso corriente— una creatinina NORMAL de 1,0 mg/dL llegaría como **88**, y Cockcroft-Gault daría una TFG ~88 veces menor. Se añadió una guarda por RANGO DE PLAUSIBILIDAD (0,1–20 mg/dL), que caza el error aunque el campo de unidades no venga. No se convierte automáticamente: adivinar la unidad de un dato clínico es justo lo que este proyecto prohíbe. Mutación: anular la guarda (`else if (false)`) | No | "una creatinina en µmol/L se RECHAZA, no se calcula" cayó nombrando el desenlace exacto: **"esperaba null y obtuvo G5"** — es decir, sin la guarda un paciente SANO con creatinina 88 µmol/L salía clasificado en falla renal terminal. Restaurado, 960/960 en verde. Se añadió además la prueba complementaria de que los extremos plausibles reales (12 mg/dL en falla avanzada, 0,4 en mujer de poca masa muscular) SÍ se calculan, para que la guarda no deje fuera al paciente en quien el estadio más importa |
| 2761 | v14.1.3 (**refactor incompleto propio, señalado por Gemini**): al extraer `_labNumerico` en v14.1.0 se dejó sin migrar la copia inline de `_vigenciaDiasParaAnalito`, y las dos habían divergido — `_labNumerico` rechaza negativos, la copia no. Con un `"-50"` corrupto del LIS, el saneo borraba el signo y devolvía `50`, por encima del umbral de albuminuria (30), así que un dato INVÁLIDO acortaba la vigencia de la RAC a la mitad. Unificado sobre `_labNumerico` | No aplica (unificación de lógica duplicada) | — |
| — | v14.1.3 (**contraste de calibración con la auditoría de Gemini**): Gemini marcó como riesgo **ALTO** que `_signosVitalesInvalidar` no se llame al cambiar de paciente, afirmando "riesgo de heredar el peso del paciente anterior" con "100% de certeza". **Verificado y refutado:** las cachés están indexadas por `pacienteId` y la comprobación de acierto exige `_signosVitalesCache.pacienteId === key`, así que un paciente distinto NUNCA recibe los datos del anterior — y la suite 29 ya lo fija con la prueba "un paciente DISTINTO sí dispara consulta nueva — nunca se mezclan pacientes". Queda anotado como recordatorio de que un informe de agente externo se verifica antes de actuar, por seguro que se declare: de sus hallazgos de esta tanda, tres eran reales y de valor alto (estadioKDIGO/NaN, unidades de creatinina, refactor incompleto) y este estaba sobrestimado | No aplica (verificación de un hallazgo ajeno) | — |
| 2738 | v14.1.4 (**decisión del médico, 14-ago-2026**): el LDL estaba en `WHITELIST_13_LABS` (se leía), en `PYM_CATALOG` (se podía ordenar) y en la tabla de vigencias por estadio (180 días)… pero NO en `RCV_VIGENCIA_KEYS`, que es la lista de lo que el script avisa cuando vence. **Un LDL vencido no generaba ninguna alerta** — punto ciego justo en el analito que fija la meta terapéutica del riesgo cardiovascular. Añadido a las tres tablas (`RCV_VIGENCIA_KEYS`, `RCV_VIGENCIA_NOMBRES`, `analitoTablaDesdeClaveRcv`). Mutación: quitarlo otra vez de `RCV_VIGENCIA_KEYS` | No | Cayeron 2 pruebas de la suite 08 nombrando el caso ("esperaba [] y obtuvo [{key: COLESTEROL_LDL…}]"); restaurado, 963/963 en verde. Efecto colateral esperado y atendido: 5 pruebas fijaban "los 7 analitos de la regla" y ahora son 8 — se actualizaron sus fixtures y sus conteos, no se relajaron |
| 10085 | v14.1.4 (**decisión del médico, 14-ago-2026 — `some` → `every`**): una actividad se daba por cubierta si UNO SOLO de sus CUPS estaba vigente. En el paquete RCV exprés, que trae DIEZ exámenes, bastaba una glicemia de hace un mes para que el banner dejara de pedir los otros nueve, creatinina y perfil lipídico incluidos. El caso que lo delató es la RAC: el propio catálogo documenta que necesita DOS exámenes (903876 + 903026) y que "van SIEMPRE los dos: uno solo no produce la RAC", pero `some` la daba por hecha con cualquiera. Mutación: volver a `some` | No | Cayeron 5 pruebas nombrando el caso; restaurado, 963/963 en verde. Se añadió la guarda `cups.length > 0` porque `[].every()` es `true` por definición: sin ella, una actividad SIN CUPS se daría por cubierta sin comprobar nada — y su prueba propia. Seis pruebas existentes codificaban la semántica vieja (una se titulaba literalmente "el paquete completo cuenta como cubierto"); se reescribieron a la regla nueva, con un ayudante que fabrica las órdenes desde el catálogo real en vez de repetir los diez códigos a mano |
| 2405/2426 | v14.1.5 (**el peor hallazgo clínico que ha tenido este script — auditoría de Gemini 3.7 Flash sobre el camino de escritura, verificado en el código**): `injectLabsIntoCronicos` busca las casillas por **id global en el documento** y no recibía ninguna referencia del paciente. Entre que el médico pulsa Auto-Labs y que llegan los resultados pasan **2-4 s de red** contra Athenea (tres peticiones encadenadas más el detalle de cada solicitud); si en ese lapso abre OTRA historia, Angular monta las casillas del paciente NUEVO y la función las encuentra y las llena con los laboratorios del paciente ANTERIOR. En el guion concreto: creatinina 4,5 mg/dL del paciente A asentada en la historia del paciente B, y al guardar queda en firme. Añadida la guarda `_pacienteSigueAbierto(docIdEsperado)` con **falla cerrada** (si la cédula no se puede leer del DOM tampoco se escribe) en la entrada y en los DOS reintentos diferidos. Mutación: hacer que la guarda devuelva `true` siempre | No | Cayeron 3 pruebas nombrando el caso, incluida la que fija el desenlace clínico ("la creatinina de 4.5 del paciente A NO cayó en la historia del paciente B"); restaurado, 970/970 en verde. Segunda mutación, sobre la falla cerrada (`!!actual &&` → `!actual ||`, es decir "si no se puede leer, asume que sigue siendo el mismo"): cayó "si la cédula NO se puede leer del DOM, se aborta". **Hallazgo propio, no del informe:** los reintentos de 300/900 ms estaban aún MÁS expuestos que el camino principal —disparan todavía más tarde— y el informe no los señalaba; llevan la misma guarda |
| — | v14.1.5 (**guarda estructural del banco, no del producto**): la firma `injectLabsIntoCronicos(labs, docIdEsperado)` deja el segundo argumento OPCIONAL a propósito (las suites montan un DOM sin cabecera de paciente y no podrían llamarla si fuera obligatorio). Eso significa que una edición futura puede añadir un llamador de producción sin él y reabrir el bug en silencio, sin que ninguna prueba de comportamiento se entere. Se añadió a la suite 26 una prueba que **lee el userscript** y exige que toda llamada real lleve los dos argumentos, con la aserción `vistas > 0` para que la propia prueba no quede ciega si alguien renombra la función. Mutación: quitarle el `docId` al llamador de producción del botón Auto-Labs | No | "toda llamada de producción a injectLabsIntoCronicos pasa el docId esperado" cayó nombrando la línea exacta; restaurado, 970/970 en verde |
| 13745 | v14.1.5 (**la regla sagrada se estaba violando desde dentro — mismo informe de Gemini, verificado**): `checkRacGuardia` existe porque Everest borra solo la casilla de la RAC al re-renderizar. Pero corría en **cada tick, sin límite de tiempo ni de veces**, y no distinguía ese borrado del otro: el del médico que borra la casilla **a propósito**. Le rellenaba el valor una y otra vez a sus espaldas — exactamente lo contrario de "la casilla del médico nunca se sobrescribe en silencio", que es la regla que el proyecto llama sagrada. La diferencia entre los dos borrados es **cuándo y cuántas veces**: el de Everest ocurre en el re-render inmediato y no insiste; el del médico llega más tarde y, si se le pisa, repite. La guarda ahora vive **20 s** y tiene **cupo de 2 restauraciones**; pasado cualquiera de los dos límites se apaga para siempre en ese paciente. Dos mutaciones: (1) cupo infinito (`restauraciones = 0` en cada tick) y (2) ventana efectivamente eterna | No | (1) cayó "al TERCER vaciado cede y deja la casilla como el médico la dejó"; (2) cayó "pasada la ventana de 20 s ya no restaura, aunque le quede cupo". Restaurado, 970/970 en verde. **El médico gana el desempate**: en el peor caso borra tres veces y a la tercera queda |
| — | v14.1.5 (**contraste de calibración, segunda tanda de Gemini**): de los 5 riesgos que ordenó por severidad, **2 eran reales y graves** (cruce de pacientes, guarda de la RAC) y se corrigieron arriba. El #4 —"la comparación estricta `"1,25"` vs `"1.25"` aborta también la escritura de la FECHA"— es **cierto como mecánica pero NO es un bug**: el comentario de v12.3.35 en esa misma línea dice literalmente que un valor distinto al de Athenea "se respeta entero, **incluida su fecha**". Es la decisión de diseño, no un descuido, y Gemini la reportó como defecto sin leer las seis líneas de comentario que la explican. Anotado junto al caso de `_signosVitalesInvalidar` de v14.1.3: los informes de agentes externos se verifican contra el código Y sus comentarios antes de actuar |
| 3979 | v14.1.5 (**avisos tardíos — reportado por los COMPAÑEROS del médico, 14-ago-2026: "a veces no te avisa con tiempo, es como si estuvieran asincrónicas"**). Causa hallada leyendo la arquitectura, no por informe ajeno: el sondeo vive en `setInterval(tick, 5000)` DENTRO de la pestaña líder, y el líder se elegía por orden de llegada **sin mirar si esa pestaña está a la vista**. Los navegadores estrangulan los temporizadores de las pestañas OCULTAS a **uno por minuto**; si la pestaña líder es la de la agenda y el médico se pasa a la historia clínica —el flujo normal— el vigilante deja de latir cada 5 s y late cada 60 s. Efecto encadenado: `LEADER_TTL_MS` son 20 s, MENOS que esos 60 s, así que el líder oculto ni siquiera alcanza a renovar su latido y el liderazgo rebota. Arreglo: una pestaña VISIBLE releva a una OCULTA aunque su latido esté fresco; entre dos visibles (o dos ocultas) manda la regla de siempre. Además el relevo se reclama en el propio `visibilitychange`, porque esperar al siguiente tick sería esperar justo al tick estrangulado. Dos mutaciones: (1) `relevoPorVisibilidad = false` (la regla vieja) y (2) `= beat.oculta === true` (que dejaría a una oculta relevar a otra oculta) | No | Las dos cayeron en "una pestaña A LA VISTA releva a un líder OCULTO aunque su latido esté fresco"; restaurado, 972/972 en verde. Se fijó también que un latido ANTIGUO sin el campo `oculta` no dispare relevos, para que durante la transición de versión una pestaña en v14.1.4 no quede desalojada cada pocos segundos |
| 13760 | v14.1.5 (**el otro síntoma del mismo reporte: "a veces NO te avisa" — un aviso que no llega nunca, no uno que llega tarde**). `state.notified` y `state.summarized` viven en la memoria de CADA pestaña, así que **cada relevo de liderazgo estrenaba una siembra**: el líder nuevo marcaba a TODOS los pacientes como ya vistos sin avisar de ninguno, y `maybeNotify` los tapaba después con su `if (prev === undefined) return` (la siembra silenciosa de v12.4, que es correcta la PRIMERA vez del día y solo esa). Y el relevo por visibilidad de arriba hace los relevos más frecuentes, así que sin esto el primer arreglo habría **agravado** el segundo síntoma. Se añadió una siembra COMPARTIDA entre pestañas (`vgl_siembra_dia`, sellada por día y borrada en `diaNuevo`), que se mantiene al día en cada `maybeNotify` — si se quedara en la foto de la mañana, un relevo a media jornada heredaría un mapa viejo y volvería a tragarse todo lo avisado desde entonces. Mutaciones: (1) no mantener la siembra al día en `maybeNotify` y (2) ignorar el sello de día al leerla | No | (1) cayó "lo ya avisado se guarda fuera de la pestaña"; (2) cayó "una siembra de OTRO DÍA se ignora". 976/976 en verde |
| 13761 | v14.1.5 (**una mutación que SOBREVIVIÓ, y lo que enseñó**): la mutación `sembradoPrevio = null` —que anula por completo la herencia y restaura el bug del aviso que no llega— **pasó el banco entero sin que nada se pusiera rojo**. La causa: la decisión de "sembrar de cero o heredar" estaba escrita DENTRO de `tick()`, y `tick` no es alcanzable desde las pruebas, así que el arreglo del fallo más grave del reporte no tenía absolutamente nada que lo respaldara. Es el modo sombra de siempre, con otra cara: no código sin llamador, sino código sin manera de comprobarlo. Se extrajo a `_sembrarEstadoInicial(processed)`, que devuelve `"nueva"` o `"heredada"`, y se le escribieron sus dos pruebas — incluida la que fija el desenlace clínico: un paciente que llegó DESPUÉS de la siembra ajena no se hereda como visto, que es exactamente el aviso que se perdía | **SÍ (la primera vez)** | Tras extraer la función, la MISMA mutación cae en "CON siembra previa se HEREDA — un paciente que llegó tras la siembra ajena NO se da por visto, así que su aviso sí sale". Queda como recordatorio: una mutación que sobrevive no siempre significa "falta una aserción"; a veces significa "ese código no es alcanzable desde el banco", y entonces lo que hay que mover es el código, no añadir la prueba donde no llega |
| 6280/6366 | v14.1.5 (**TERCERA causa de los avisos tardíos, y la más torcida de las tres — confirmada por la auditoría adversarial de Gemini y verificada en el código**). Un aviso generado con el médico FUERA de `/viva/HCHealth` **no sonaba**: se guardaba en `vgl_avisos_pendientes` y esperaba a que entrara a una historia clínica; entonces salían todos de golpe, minutos u horas después del hecho. Es literalmente lo que reportaron sus compañeros ("es como si estuvieran asincrónicas"). Pero lo que se estaba conteniendo era el **tono y la notificación del SISTEMA OPERATIVO**, que existen precisamente para avisar cuando el médico NO está mirando esa pantalla: la condición estaba **al revés**. El aviso se partió según de qué depende cada parte: `_dispararAvisoAudible` (tono, notificación del sistema, parpadeo de título y favicon) sale SIEMPRE y en el acto, sin mirar el módulo; `_dispararAvisoCartel` (bigAlert/popupAlert) es lo único que se encola, porque sí necesita una vista donde pintarse. Mutación: volver a condicionar el aviso audible al módulo | No | Cayeron 2 pruebas, la principal nombrando el arreglo ("fuera de HCHealth el aviso SÍ suena y sale al sistema operativo en el acto"); restaurado, 977/977 en verde. **Dos pruebas existentes codificaban el comportamiento viejo** —una decía literalmente "el aviso visible/audible NO se muestra mientras la pestaña líder está en Acceso"— y se reescribieron a la regla nueva en vez de relajarse, igual que se hizo con el `some`→`every` de v14.1.4 |
| 6300 | v14.1.5 (**la otra mitad del mismo arreglo**): con el tono ya desacoplado, faltaba impedir que el cartel saliera rancio. Un cartel de hace 40 minutos pintado al entrar a la historia hace que el médico atienda una llegada **que ya pasó**. Se añadió caducidad de 10 minutos por `ts`, y los avisos encolados por una versión anterior (sin `ts`) NO caducan, para no perderlos en la transición. Mutaciones: (1) quitar la caducidad y (2) hacer que el vaciado llame a `_dispararAvisoReal` en vez de solo al cartel, o sea volver a notificar al sistema horas después | No | (1) cayó "un cartel de hace más de 10 minutos ya no se pinta"; (2) cayó "pinta los carteles que quedaron en cola, **sin volver a notificar**". 977/977 en verde |
| 3999 | v14.1.5 (**regresión que introduje YO en esta misma versión, señalada por la auditoría adversarial que pedí precisamente para eso**). El relevo de liderazgo por visibilidad no tenía enfriamiento, y cada toma de mando ejecuta `API.ultimo = 0`, es decir **una consulta inmediata de agenda**. Con Alt+Tab rápido entre dos pestañas de Everest, cada conmutación robaba el mando y disparaba su consulta: ráfagas de peticiones contra el servidor de la IPS, causadas por mi propio arreglo del día anterior. Añadido `RELEVO_VISIBILIDAD_MIN_MS = 10000`. Mutación: quitar el enfriamiento | No | "el relevo por visibilidad tiene enfriamiento — con Alt+Tab rápido no se dispara una ráfaga de consultas al servidor" cayó; restaurado, 979/979 en verde. **Nota de método:** el primer intento de esta mutación NO casó con el texto del archivo y el banco siguió en verde — pareció una mutación superviviente cuando en realidad no se había aplicado nada. Una mutación que "sobrevive" hay que confirmarla mirando que el archivo cambió de verdad, no fiándose del `replace` |
| — | v14.1.5 (**contraste de calibración, tercera tanda de Gemini**). De los hallazgos de su refutación: **2 eran reales y se corrigieron** (la cola fuera de HCHealth, y mi propia regresión del enfriamiento). **Uno era infundado y quedó fijado con prueba**: temía "acefalía" si el médico minimiza el navegador y ninguna pestaña queda visible. No aplica — la visibilidad se implementó como *preferencia para RELEVAR*, nunca como *requisito para MANDAR*: una pestaña oculta sin competencia renueva su latido y conserva el mando. Se añadió la prueba "si TODAS las pestañas están ocultas NO hay acefalía" para que nadie convierta la preferencia en requisito. **Otro era falso**: dijo que `crossTabDup` podía tragarse un VERDE que llegara 5 s después de un MORADO; no puede, porque el uid incluye el color (`a.key + "|" + a.color`), así que son claves distintas. Y **otro más era falso**: dijo que la siembra compartida podría no llevar sello de día; sí lo lleva (`g.dia !== todayStamp()`), y hay prueba desde v14.1.5. Balance de las tres tandas: Gemini acierta en lo estructural y tiende a sobrestimar en lo puntual — se verifica todo antes de actuar |
| 5030 | v14.1.6 (**hallazgo del export real del tablero: el cazador de errores llevaba una semana sin cazar nada**). Dos confirmaciones independientes sobre 20 equipos y ~6 días: (1) la hoja `error` **NO EXISTE** en la Hoja, y el servidor la crea al recibir el primer evento de ese tipo, así que no ha llegado ninguno; (2) en `uso_detalle` no hay ni una clave `error.js`/`error.promesa`/`error.distintos`, y esas se emiten desde `reportarError` **antes** del tope de 5/día y **antes** de comprobar `repOn()`, así que aparecerían aunque el envío estuviera capado o apagado. `reportarError` no se ha llamado nunca. La causa: el filtro exigía que `ev.filename` contuviera "userscript" o "vigilante", y cómo se llama ese archivo depende de cómo inyecte el gestor de userscripts — puede ser la URL de la página, un `blob:`, una URL de extensión, o venir vacío. **El filtro que debía dejar fuera los errores de Everest dejaba fuera los nuestros.** El arreglo no vuelve a adivinar: al arrancar se provoca un error controlado y se ANOTA de qué archivo dice venir; esa firma se compara después contra el stack de cada error. Se usa `ev.error.stack` (todos los marcos) en vez de solo `ev.filename`. Mutaciones: (1) volver al filtro viejo y (2) hacer que la captura de la firma falle | No | (1) cayó "reconoce nuestro código por la FIRMA capturada al arrancar, aunque el nombre de archivo no diga 'userscript' ni 'vigilante'"; (2) cayó "la firma se captura sola al cargar el script — si quedara vacía, el arreglo entero sería inútil". 982/982 en verde. Se conserva la heurística vieja como respaldo, con prueba propia, y se mantiene la exclusión de los errores de Everest — que era el motivo legítimo del filtro |
| tests/harness.js:130 | v14.1.5 (R5.1, Challenger 2 — extracción dinámica de versión): `if (m) version = m[1];` &rarr; `if (m) version = "1.0.0";` | No | "R5.1: Cuádruple sincronización de versión (@version, const VERSION, package.json, GM_info)" cayó con: esperaba "14.1.5" y obtuvo "1.0.0" — restaurado |
| 14058 | v14.1.5 (R5.3, Challenger 2 — remoción DOM de elementos inyectados en emergencyTeardown): comentar `injected.forEach((el) => { try { el.remove(); } catch (e) {} });` | No | "emergencyTeardown: desmantela DOM, limpia temporizadores y persiste estado de apagado" cayó con: elementos inyectados removidos del DOM: esperaba 0 y obtuvo 1 — restaurado |
| 14043 | v14.1.5 (R5.3, Challenger 2 — cancelación de temporizadores en emergencyTeardown): comentar `state.timers.forEach((t) => { try { clearInterval(t); clearTimeout(t); } catch (e) {} });` | No | "emergencyTeardown: desmantela DOM, limpia temporizadores y persiste estado de apagado" cayó con: temporizadores registrados fueron cancelados (obtuvo false) — restaurado |
| 2434 | v14.1.5 (R5.3, Challenger 2 — guarda de kill-switch en injectLabsIntoCronicos): `if (state.killed)` &rarr; `if (false)` | Sí (en primera versión de suite 30:69 por `t.caso` con callback `async`; no tras corregir a `await t.casoAsync`) | En la versión inicial de suite 30 línea 69, `t.caso` recibía `async () => ...`: las excepciones dentro de promesas no bloqueantes no interrumpen `t.caso`, por lo que el runner marcaba el test como verde a pesar del fallo asíncrono. Al invocar con `await t.casoAsync`, la mutación cayó de inmediato con: "Auto-Labs aborta inmediatamente bajo kill-switch (obtuvo false)" — restaurado |
| 10285 | v14.1.5 (R5.3, Challenger 2 — guarda de kill-switch en apiAccesoAsignarTurno): `if (state.killed) return ...` &rarr; `if (false) return ...` | Sí | La aserción de suite 30 solo verificaba `resTurno.error === true`; al saltarse el kill-switch, la falta de médico configurado (`!uId`) retornaba también `{ error: true, mensaje: "No se pudo identificar al médico..." }`, enmascarando la evasión del kill-switch. Falta asertar `resTurno.mensaje.includes("Kill-Switch activo")` con médico activo configurado. |
| 12316 | v14.1.5 (R5.3, Challenger 2 — guarda de kill-switch en apiOrdenamientoGuardar): `if (state.killed) { ... return null; }` &rarr; `if (false) { ... }` | Sí | La aserción solo verificaba `resOrden === null`, pero la guarda de médico ausente (`!uId`) retorna igualmente `null`. Falta mockear `c.env.win.alert` y asertar que el mensaje emitido corresponde a la alerta de Kill-Switch con médico activo configurado. |
| 14088 | v14.1.5 (R5.3, Challenger 2 — activación remota de kill-switch en checkVersionMinimum): `if (data.killSwitch && data.killSwitch.active)` &rarr; `if (false)` | No | Cayeron dos pruebas: "checkVersionMinimum: activa Kill-Switch remoto cuando el payload lo solicita para all" ("Kill-switch activado tras respuesta remota (obtuvo false)") y "filtra Kill-Switch por scope de equipo" — restaurado |
| 14091 | v14.1.5 (R5.3, Challenger 2 — filtrado por scope de equipo en checkVersionMinimum): `const aplica = scope === "all" || ...` &rarr; `const aplica = true;` (ignora scope) | No | "checkVersionMinimum: filtra Kill-Switch por scope de equipo (_equipoId)" cayó con: "Kill-switch no afecta a equipo fuera de scope (obtuvo false)" — restaurado |
| 14100 | v14.1.5 (R5.3, Challenger 2 — poblado de disabledFeatures en checkVersionMinimum): `state.disabledFeatures = new Set(data.killSwitch.disabledFeatures);` &rarr; `state.disabledFeatures = new Set();` | No | "checkVersionMinimum: desactiva selectivamente características (disabledFeatures: autoLabs)" cayó con: "autoLabs registrado en disabledFeatures (obtuvo false)" — restaurado |
| 14165 | v14.1.5 (R5.3, Challenger 2 — guarda de arranque en boot() bajo vgl_kill_active persistente): `if (killActivo && !bypass)` &rarr; `if (false)` | Sí | Ninguna prueba del banco ejecuta `boot()` habiendo configurado `GM_setValue("vgl_kill_active", true)`. Falta una aserción que compruebe que `boot()` no inyecta `#vgl-root`, activa `state.killed = true` e inserta el banner `#vgl-pausa-clinica`. |
| 3620 | v14.1.5 (M1: R1.4/R1.5, Worker M1 — saneamiento de correos en scrubPII): comentar `s = s.replace(/\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}\b/g, "[CORREO_CENSURADO]");` | No | "scrubPII: censura correos electrónicos" (Seguridad, PHI, XSS e Integridad (M1)) cayó con: esperaba "[CORREO_CENSURADO]" y obtuvo "paciente.ejemplo@gmail.com" — restaurado |
| 3622 | v14.1.5 (M1: R1.4/R1.5, Worker M1 — saneamiento de direcciones colombianas en scrubPII): comentar reemplazo de direcciones | No | "scrubPII: censura direcciones físicas colombianas" (Seguridad, PHI, XSS e Integridad (M1)) cayó con: esperaba "[DIR_CENSURADA]" — restaurado |
| 3625 | v14.1.5 (M1: R1.4/R1.5, Worker M1 — saneamiento de cédulas y dígitos en scrubPII): comentar `s = s.replace(/(?<!\d)\d{6,11}(?!\d)/g, "[CENSURADO]");` | No | "scrubPII: censura cédulas colombianas formateadas y sin formatear" cayó con: esperaba "[CENSURADO]" y obtuvo "43040508" — restaurado |
| 3690 | v14.1.5 (M1: R1.8, Worker M1 — telemetría Default-Off): cambiar `DEFAULTS.reporte = false;` &rarr; `true;` | No | "Telemetría: invariante Default-Off garantizado sin configuración previa" cayó con: DEFAULTS.reporte debe ser false de fábrica (obtuvo true) — restaurado |
| 23 | v14.1.5 (M1: R1.3, Worker M1 — Supply Chain cabecera @connect): reintroducir `// @connect localhost` en cabecera de userscript | No | "Supply Chain: Userscript no incluye directivas @connect a localhost o 127.0.0.1" cayó con: Prohibido @connect localhost — restaurado |
| 3574 | v14.1.5 (M1: R1.9, Worker M1 — hash de integridad SHA-256): cambiar algoritmo `"SHA-256"` a `"SHA-512"` en verificarIntegridadArranque | No | "verificarIntegridadArranque: calcula SHA-256 correctamente cuando se suministra fuente" cayó por discrepancia de hash — restaurado |
| 3622 | v14.1.5 (M1: R1.4/R1.5, Challenger 2 — saneamiento de celulares en scrubPII): comentar reemplazo de celulares `s = s.replace(/(?:\+?57\s*)?(?:\(?3[0-5]\d\)?[\s.-]?\d{3}[\s.-]?\d{4}|\b3[0-5]\d{8}\b)/g, "[TEL_CENSURADO]");` | No | "scrubPII: censura celulares colombianos (300-350) con diversos formatos" y "scrubPII: saneamiento profundo y recursivo..." cayeron con: esperaba "Llamar al [TEL_CENSURADO] urgente" — restaurado |
| 13725 | v14.1.5 (M1: R1.2, Challenger 2 — escapeHtml sin escape de etiquetas HTML `<` y `>`): `/[&<>"'`]/g` &rarr; `/[&"'`]/g` | No | Cayeron dos suites: "sheetHeader: escapa el título e incluye el botón Cerrar y el HTML extra" (Suite 15) y "escapeHtml: neutraliza todos los caracteres peligrosos de inyección HTML/XSS" (Suite 31) — restaurado |
| 2850 | v14.1.5 (M2: R2.3, Worker M2 — guardas fisiológicas de edad en `cockcroftGault`): eliminar la guarda de edad pediátrica `edad < 18` &rarr; permitiendo cálculo de CG en menores de 18 años | No | Cayeron dos suites: "cockcroftGault - centinelas (edad<=0...)" (Suite 27) y "R2.3: cockcroftGault rechaza pacientes pediátricos (<18 años)" (Suite 32) cayó con: esperaba 0 y obtuvo 102.5 — restaurado |
| 2850 | v14.1.5 (M2: R2.3, Worker M2 — guardas fisiológicas de peso en `cockcroftGault`): relajar límites de peso `peso < 20 || peso > 300` &rarr; `peso < 0` | No | "R2.3: cockcroftGault rechaza pesos no fisiológicos (<20 kg o >300 kg)" (Suite 32) cayó con: peso < 20 kg retorna 0 (esperaba 0 y obtuvo 26.3) — restaurado |
| 5957 | v14.1.5 (M2: R2.5, Worker M2 — sonido de fraude extemporáneo en transición directa `Sin presentarse` &rarr; `Atendido`): cambiar `sound = true; state.alertedFraud.add(key);` &rarr; `sound = false;` | No | "R2.5: Transición Sin presentarse -> Atendido (salto de sala) dispara ROJO con sonido de flanco" (Suite 32) cayó con: primer aviso de fraude debe sonar (obtuvo false) — restaurado |
| 9313/9329 | v14.1.5 (M2: R2.4, Challenger 2 — corrupción de tabla FESTIVOS para 2025/2027): cambiar `"2025-01-06"` &rarr; `"2025-01-07"` y `"2027-07-05"` &rarr; `"2027-07-06"` | No | "R2.4: Festivos de Colombia verificados en 2024, 2025, 2026 y 2027" (Suite 32) cayó con: 2025-01-06 Reyes Magos (obtuvo false) — restaurado |
| 10368 | v14.1.5 (M2: R2.7, Challenger 2 — debilitar verificación multi-CUP `cups.every` &rarr; `cups.some` en `pymCubiertoPorOrdenVigente`): permitir que 1 solo CUP cubra paquetes multi-CUP | No | Cayeron 4 pruebas en dos suites: 3 pruebas en Suite 21 y "R2.7: pymCubiertoPorOrdenVigente exige todos los CUPS en actividades multi-CUP (ej. RAC)" (Suite 32) — restaurado |
| 2169 | v14.1.5 (M2: R2.8, Challenger 2 — desambiguación DOM HbA1c vs Hemoglobina `max="30"` &rarr; `max="20"` en `_findHbA1cFields`): corromper atributo distintivo del DOM | No | Cayeron dos suites: "_findHbA1cFields: encuentra el input correcto por type=number y max=30..." (Suite 08) y "R2.8: Desambiguación HbA1c vs Hemoglobina en formulario de Crónicos" (Suite 32) — restaurado |
| 3779 | v14.1.6 (M3: R3.8, Worker M3 — versión objetivo de esquema en `migrarEsquemaVgl`): cambiar `SCHEMA_TARGET_VERSION = 14;` &rarr; `13;` | No | Cayeron dos pruebas en Suite 33: "R3.8: migrarEsquemaVgl migra desde v0 a v14 aplicando normalizaciones" y "R3.8: migrarEsquemaVgl es idempotente en esquema ya actualizado (v14)" con: esperaba 14 y obtuvo 13 — restaurado |
| 3795 | v14.1.6 (M3: R3.8, Worker M3 — downgrade guard en `migrarEsquemaVgl`): cambiar guarda de versión futura `schema.version > SCHEMA_TARGET_VERSION` &rarr; `if (false)` | No | Cayó en Suite 33: "R3.8: Cuarentena hacia atrás (Downgrade Guard) preserva esquemas futuros v15+" con: Debe crear un snapshot de respaldo en vgl_future_backup_15_* (obtuvo false) — restaurado |
| 3740 | v14.1.6 (M3: R3.8, Worker M3 — aislamiento de JSON corrupto a cuarentena en `safeReadJSON`): comentar escritura en localStorage `localStorage.setItem(qKey, raw);` | No | Cayó en Suite 33: "R3.8 / R3.1: safeReadJSON pone en cuarentena JSON corrupto y retorna default" con: Debe aislar el payload malformado en vgl_quarantine_* para auditoría forense (obtuvo false) — restaurado |
| 3892 | v14.1.6 (M3: R3.9, Worker M3 — umbral de fallos para apertura en `getCircuitBreaker`): cambiar `threshold: 3` &rarr; `threshold: 10` | No | Cayó en Suite 33: "R3.9: Circuit Breaker pasa a OPEN tras 3 fallos y activa Fail-Fast" con: Tras 3 fallos, el cortacircuitos debe estar en estado OPEN: esperaba "OPEN" y obtuvo "CLOSED" — restaurado |
| 3937 | v14.1.6 (M3: R3.9, Worker M3 — backoff exponencial en sonda HALF-OPEN fallida en `circuitBreakerExec`): deshabilitar duplicación de resetTimeout `cb.resetTimeoutMs = Math.min(300000, cb.resetTimeoutMs * 2);` &rarr; `cb.resetTimeoutMs = 60000;` | No | Cayó en Suite 33: "R3.9: Circuit Breaker HALF-OPEN fallido regresa a OPEN con backoff duplicado" con: El tiempo de reset debe duplicarse con backoff exponencial (obtuvo false) — restaurado |
| 9591 | v14.1.6 (M3: R3.2 Remediation, Worker M3 — detección de escrituras y 0 reintentos en `_pageFetchJsonCore`): cambiar `const esEscritura = metodo !== "GET" && metodo !== "HEAD" && !(options && options.__idempotent === true);` &rarr; `const esEscritura = false;` | No | Cayó en Suite 33: "R3.2: _pageFetchJsonCore en POST tiene 0 reintentos automáticos (Anti-Doble Escritura)" con: En una escritura POST sólo debe hacerse EXACTAMENTE 1 intento (0 reintentos): esperaba 1 y obtuvo 4 — restaurado |
| 2351 | v14.1.7 (M4: R4.1/R4.4, Worker M4 — preservación de cero legítimo en `_valorCrudoLab`): cambiar `_valorCrudoLab = (v) => (!v ? undefined : v);` descartando 0/texto | No | Cayó en Suite 34: "M4-RAW-1: _valorCrudoLab descarta null, undefined y whitespace preservando valores 0 y textos" con: esperaba undefined y obtuvo "   " (y cero numérico descartado) — restaurado |
| 2405 | v14.1.7 (M4: R4.4, Worker M4 — evasión de guarda anti-cruce en `_pacienteSigueAbierto`): forzar `return true;` incondicional | No | Cayó en Suite 08: "_pacienteSigueAbierto: sin docId esperado deja pasar; con uno, exige coincidencia exacta" con: una cédula parecida NO coincide (obtuvo true) — restaurado |
| 2843 | v14.1.7 (M4: R4.1/R4.4, Worker M4 — discriminación de género en `_esSexoFemenino`): cambiar chequeo de prefijo "F" &rarr; "M" | No | Cayó en Suite 34: "M4-SEX-1: _esSexoFemenino discrimina variantes exhaustivas y resiste mutaciones" con: se esperaba verdadero (obtuvo false) — restaurado |
| 2851 | v14.1.7 (M4: R4.1/R4.4, Worker M4 — constante de denominador en `cockcroftGault`): cambiar denominador 72 &rarr; 85 en fórmula Cockcroft-Gault | No | Cayó en Suite 27: "cockcroftGault - caso general, ambos sexos" con: esperaba 75.1 y obtuvo 63.6 — restaurado |
| 10312 | v14.1.7 (M4: R4.1/R4.4, Worker M4 — selección de analito en `_creatininaDeLabs`): cambiar extracción de `"CREATININA"` &rarr; `"GLUCOSA"` | No | Cayó en Suite 34: "M4-CRE-1: _creatininaDeLabs extrae valor sérico más reciente y rechaza orina y pendientes" con: se esperaba verdadero (obtuvo false) — restaurado |
| 10427 | v14.1.7 (M4: R4.1/R4.4, Worker M4 — ensamble demográfico en `calcularEstadioRenal`): permutar `edad: demo.edad` &rarr; `edad: p.peso` | No | Cayó en Suite 34: "M4-CALC-1: calcularEstadioRenal ensambla demográficos y signos vitales concurrentemente" con: esperaba 70 y obtuvo 60 — restaurado |
| 6118 | v14.1.7 (M4: R4.4, Worker M4 — unicidad de clave por hora en `apptKey`): omitir `@hora_texto` retornando solo `doc_id` | No | Cayó en Suite 02: "apptKey: arma la clave de la cita" con: esperaba "123@07:00 AM" y obtuvo "123" — restaurado |
| 6130 | v14.1.7 (M4: R4.4, Worker M4 — reseteo de fraudWatch al cambiar de jornada en `diaNuevo`): comentar `state.fraudWatch.clear();` | No | Cayó en Suite 02: "diaNuevo: limpieza real de estado en el mismo entorno" con: esperaba 0 y obtuvo 1 — restaurado |
| 6146 | v14.1.7 (M4: R4.4, Worker M4 — detección de fraude en reaparición de agenda en `colorAndAlert`): retornar `"VERDE"` en vez de `"ROJO"` cuando paciente en fraudWatch reaparece | No | Cayó en Suite 04: "colorAndAlert: un paciente en fraude que pasa a En Sala dispara ROJO y sonido (una vez)" con: esperaba "ROJO" y obtuvo "VERDE" — restaurado |
| 10567 | v14.1.7 (M4: R4.4, Worker M4 — exigencia exhaustiva de paquetes multi-CUP en `pymCubiertoPorOrdenVigente`): relajar `cups.every` &rarr; `cups.some` | No | Cayó en Suite 32: "R2.7: pymCubiertoPorOrdenVigente exige todos los CUPS en actividades multi-CUP (ej. RAC)" con: actividad con 1 de 2 CUPS vigentes sigue pendiente: esperaba 1 y obtuvo 0 — restaurado |
| 13218 | v14.1.7 (M4: R4.1/R4.4, Worker M4 — protección ante datos legados/corruptos en `_pymYaOrdenadoHoyDesdeElScript`): relajar guarda `Array.isArray(det.actividades)` &rarr; `det.actividades` | No | Cayó en Suite 34: "M4-PYM-2: _pymYaOrdenadoHoyDesdeElScript extrae lista real de actividades ordenadas hoy" con: si actividades no es array debe retornar [] sin descontar nada: esperaba [] y obtuvo "NO_ES_UN_ARRAY" — restaurado |
| 10430 | v14.1.7 (M4: MUT-REN-040, Worker M4.2 — discriminación de sexo en `calcularEstadioRenal`): forzar `demo.sexo = "F"` incondicional | No | Cayó en Suite 34: "M4-CALC-1: calcularEstadioRenal discrimina matemáticamente masculino vs femenino" con: TFG masculino sin factor 0.85 (esperaba 56.1 y obtuvo 47.7) — restaurado |
| 10344 | v14.1.7 (M4: MUT-REN-016, Worker M4.2 — tipo de retorno en `_pesoDeSignosVitales` con entrada vacía): cambiar `return null` &rarr; `return undefined` | No | Cayó en Suite 34: "M4-CRE-1: _creatininaDeLabs y _pesoDeSignosVitales filtran datos y toleran excepciones" con: no debe retornar undefined en array vacío — restaurado |
| 10313 | v14.1.7 (M4: MUT-REN-030, Worker M4.2 — robustez ante excepciones en `_creatininaDeLabs`): eliminar `try/catch` envolvente | No | Cayó en Suite 34: "M4-CRE-1: _creatininaDeLabs y _pesoDeSignosVitales filtran datos y toleran excepciones" con: excepción en parser debe retornar null fail-safe — restaurado |
| 1236 | v14.1.7 (M4: MUT-CLIN-001, Worker M4.2 — canonización de texto en `_conductaBuscarYAgregarExamen`): comparar texto sin normalizar `_canonTexto` | No | Cayó en Suite 34: "M4-DOM-2: _conductaBuscarYAgregarExamen con canonización de texto" con: debe casar texto canonicalizado y agregar examen — restaurado |
| 1317 | v14.1.7 (M4: MUT-CLIN-012, Worker M4.2 — discriminación de analitos de orina en `_hayComponenteUroReal`): forzar `return true` incondicional | No | Cayó en Suite 34: "M4-URO-1: Uroanálisis descarta analitos no reconocidos y preserva marcas" con: analito de orina no mapeado no es componente uro real — restaurado |
| 1335 | v14.1.7 (M4: MUT-CLIN-017, Worker M4.2 — fallback nulo en `_matchUroComponente` con nombre vacío): `return null` &rarr; `return "LEUCOCITOS"` | No | Cayó en Suite 34: "M4-URO-1: Uroanálisis descarta analitos no reconocidos y preserva marcas" con: nombre vacío debe retornar null — restaurado |
| 1335 | v14.1.7 (M4: MUT-CLIN-020, Worker M4.2 — rechazo de analitos de orina no mapeados en `_matchUroComponente`): `return null` &rarr; `return "HEMATIES"` | No | Cayó en Suite 34: "M4-URO-1: Uroanálisis descarta analitos no reconocidos y preserva marcas" con: analito no mapeado retorna null — restaurado |
| 1345 | v14.1.7 (M4: MUT-CLIN-021, Worker M4.2 — invariante de tipo array en `_agruparUroanalisisParaTabla`): `return []` &rarr; `return null` | No | Cayó en Suite 34: "M4-URO-1: Uroanálisis descarta analitos no reconocidos y preserva marcas" con: esperaba [] y obtuvo null (debe ser instancia de Array) — restaurado |
| 1415 | v14.1.7 (M4: MUT-CLIN-028, Worker M4.2 — tolerancia a excepciones DOM en `_findUroInput`): eliminar `try/catch` en querySelector | No | Cayó en Suite 34: "M4-URO-1: Uroanálisis descarta analitos no reconocidos y preserva marcas" con: error en DOM query debe retornar null fail-safe — restaurado |
| 1584 | v14.1.7 (M4: MUT-CLIN-037, Worker M4.2 — discriminación de tipo documento CC en `_atheneaCedulaCoincide`): eliminar prefijo `CC` del regex | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: debe casar cédula por regex de tipo de documento (obtuvo false) — restaurado |
| 1586 | v14.1.7 (M4: MUT-CLIN-040, Worker M4.2 — límite de palabra en coincidencia de cédula en `_atheneaCedulaCoincide`): relajar regex a coincidencia parcial de substring | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: prefijo parcial no debe coincidir como cédula válida (obtuvo true) — restaurado |
| 1660 | v14.1.7 (M4: MUT-CLIN-043, Worker M4.2 — límites de días de mes en `_fechaDesdeNumeroSolicitud`): relajar `diaN > 31` &rarr; `diaN > 32` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: día 99 inválido retorna null (esperaba null) — restaurado |
| 1665 | v14.1.7 (M4: MUT-CLIN-044, Worker M4.2 — ambigüedad de fechas en `_fechaDesdeNumeroSolicitud`): relajar `isos.size === 1` &rarr; `isos.size >= 1` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: fechas contradictorias retornan null (esperaba null) — restaurado |
| 2145 | v14.1.7 (M4: MUT-CLIN-052, Worker M4.2 — búsqueda por atributo name en `_findLabField`): omitir selector `[name="..."]` | No | Cayó en Suite 34: "M4-DOM-1: _findLabField y _findHbA1cFields límites de búsqueda" con: debe encontrar el elemento por atributo [name='...'] — restaurado |
| 2148 | v14.1.7 (M4: MUT-CLIN-054, Worker M4.2 — filtrado de IDs nulos/vacíos en `_findLabField`): no saltar elementos falsy en `altIds` | No | Cayó en Suite 34: "M4-DOM-1: _findLabField y _findHbA1cFields límites de búsqueda" con: debe saltar IDs falsy y encontrar campoAltReal — restaurado |
| 2150 | v14.1.7 (M4: MUT-CLIN-055, Worker M4.2 — retorno estrictamente nulo ante campo inexistente en `_findLabField`): `return null` &rarr; `return document.querySelector("input")` | No | Cayó en Suite 34: "M4-DOM-1: _findLabField y _findHbA1cFields límites de búsqueda" con: campo inexistente debe retornar strictly null — restaurado |
| 2168 | v14.1.7 (M4: MUT-CLIN-056, Worker M4.2 — desambiguación de HbA1c frente a Hemoglobina total en `_findHbA1cFields`): omitir validación `max="30"` | No | Cayó en Suite 34: "M4-DOM-1: _findLabField y _findHbA1cFields límites de búsqueda" con: debe seleccionar estrictamente el campo con max='30' — restaurado |
| 2210 | v14.1.7 (M4: MUT-CLIN-061, Worker M4.2 — guarda de tipo objeto en `_extractFechaSolicitudTopLevel`): eliminar validación `typeof obj !== "object"` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: primitiva string retorna null — restaurado |
| 2240 | v14.1.7 (M4: MUT-CLIN-066, Worker M4.2 — guarda de tipo objeto en `_extractAtheneaFecha`): eliminar validación `typeof obj !== "object"` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: primitiva string retorna null — restaurado |
| 2248 | v14.1.7 (M4: MUT-CLIN-068, Worker M4.2 — preferencia de clave con semántica de fecha en `_extractAtheneaFecha`): ignorar clave `k.includes("fecha")` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: debe preferir la clave que contenga 'fecha' — restaurado |
| 2403 | v14.1.7 (M4: MUT-CLIN-078, Worker M4.2 — compatibilidad ante docId ausente en `_pacienteSigueAbierto`): `!docIdEsperado` &rarr; `false` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: si docIdEsperado es vacío retorna true por compatibilidad — restaurado |
| 2440 | v14.1.7 (M4: MUT-CLIN-085, Worker M4.2 — marcación de uroanálisis solo con analitos de orina en `injectLabsIntoCronicos`): marcar SI con laboratorios puramente séricos | No | Cayó en Suite 34: "M4-URO-1: Uroanálisis descarta analitos no reconocidos y preserva marcas" con: labs solo séricos no deben marcar Uroanálisis SI — restaurado |
| 10167 | v14.1.7 (M4: MUT-CLIN-097, Worker M4.2 — tolerancia a fallos de red en `apiAccesoObtenerLaboratoriosAnnar`): relanzar error en `catch` | No | Cayó en Suite 34: "M4-LAB-1: apiAccesoObtenerLaboratoriosAnnar y apiAccesoObtenerLaboratoriosCiti retornan null en fallos de red" con: falla en Annar debe resolver a null fail-safe — restaurado |
| 10171 | v14.1.7 (M4: MUT-CLIN-102, Worker M4.2 — retorno nulo ante error en `apiAccesoObtenerLaboratoriosCiti`): `return null` &rarr; `return []` | No | Cayó en Suite 34: "M4-LAB-1: apiAccesoObtenerLaboratoriosAnnar y apiAccesoObtenerLaboratoriosCiti retornan null en fallos de red" con: falla en Citi debe resolver a strictly null y no [] — restaurado |
| 6034 | v14.1.7 (M4: MUT-CLIN-115, Worker M4.2 — retorno de cadena vacía ante error DOM en `extractPacienteAbierto`): `return ""` &rarr; `return "ERROR"` | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: excepción en extractPacienteAbierto debe retornar cadena vacía — restaurado |
| 9912 | v14.1.7 (M4: MUT-AGD-029, Worker M4.2 — reconocimiento de sigla HTA en `perfilPaciente`): exigir `"HIPERTENSION"` en vez de `"HTA"` | No | Cayó en Suite 34 y Suite 24: "M4-PERF-1: perfilPaciente reconoce sigla 'HTA' pura y habilita adicionales" con: etiqueta 'HTA' mayúscula debe habilitar adicionales — restaurado |
| 10161 | v14.1.7 (M4: MUT-AGD-044, Worker M4.2 — tolerancia a fallos de red en `apiDigiturnoFinalizarTicket`): eliminar `try/catch` | No | Cayó en Suite 34: "M4-AGD-1: Digiturno, AgdValidarAgenda y ObtenerTurnos envían parámetros correctos y toleran errores" con: Digiturno no debe lanzar excepción al fallar la red — restaurado |
| 10160 | v14.1.7 (M4: MUT-AGD-045, Worker M4.2 — asignación de ID de médico activo en `apiDigiturnoFinalizarTicket`): forzar `UsuarioId=0` | No | Cayó en Suite 34: "M4-AGD-1: Digiturno, AgdValidarAgenda y ObtenerTurnos envían parámetros correctos y toleran errores" con: debe enviar el ID del médico activo en Digiturno — restaurado |
| 10584 | v14.1.7 (M4: MUT-AGD-048, Worker M4.2 — tolerancia a fallos de red en `apiAccesoAgdValidarAgenda`): eliminar `try/catch` | No | Cayó en Suite 34: "M4-AGD-1: Digiturno, AgdValidarAgenda y ObtenerTurnos envían parámetros correctos y toleran errores" con: AgdValidarAgenda debe retornar null en fallo de red — restaurado |
| 10580 | v14.1.7 (M4: MUT-AGD-049, Worker M4.2 — flag swParticular en `apiAccesoAgdValidarAgenda`): cambiar `swParticular=false` &rarr; `swParticular=true` | No | Cayó en Suite 34: "M4-AGD-1: Digiturno, AgdValidarAgenda y ObtenerTurnos envían parámetros correctos y toleran errores" con: AgdValidarAgenda debe llevar swParticular=false — restaurado |
| 10595 | v14.1.7 (M4: MUT-AGD-054, Worker M4.2 — flag swParticular en `apiAccesoObtenerTurnos`): cambiar `swParticular=false` &rarr; `swParticular=true` | No | Cayó en Suite 34: "M4-AGD-1: Digiturno, AgdValidarAgenda y ObtenerTurnos envían parámetros correctos y toleran errores" con: ObtenerTurnos debe llevar swParticular=false — restaurado |
| 10594 | v14.1.7 (M4: MUT-AGD-055, Worker M4.2 — naturaleza asíncrona de `apiAccesoObtenerTurnos`): remover declaración `async` | No | Cayó en Suite 34: "M4-AGD-1: Digiturno, AgdValidarAgenda y ObtenerTurnos envían parámetros correctos y toleran errores" con: apiAccesoObtenerTurnos debe ser función async — restaurado |
| 10610 | v14.1.7 (M4: MUT-AGD-056, Worker M4.2 — guarda de seguridad Kill-Switch en `apiAccesoAsignarTurno`): omitir chequeo de `state.killed` | No | Cayó en Suite 34 y Suite 05: "M4-KILL-1: Kill-Switch activo bloquea apiAccesoAsignarTurno y apiOrdenamientoGuardar" con: debe reportar error bajo kill-switch y 0 llamadas de red — restaurado |
| 10181 | v14.1.7 (M4: MUT-ORD-001, Worker M4.2 — TTL de órdenes vigentes en `apiHcObtenerOrdenamientosVigentes`): cambiar TTL de 10 min &rarr; 600 min | No | Cayó en Suite 34: "M4-ORD-2: TTL de órdenes vigentes (10 min), payload SwHc:false y header Accept JSON" con: tras 15 minutos la caché expira (TTL 10 min) y realiza nueva consulta — restaurado |
| 12569 | v14.1.7 (M4: MUT-ORD-015, Worker M4.2 — tolerancia a fallos de red en `apiOrdenamientoBuscarPaciente`): eliminar `try/catch` | No | Cayó en Suite 34: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: falla en BuscarPaciente debe retornar strictly null — restaurado |
| 12583 | v14.1.7 (M4: MUT-ORD-018, Worker M4.2 — selección exacta de diagnóstico en `apiOrdenamientoObtenerDx`): cambiar `.find(...)` &rarr; `items[0]` | No | Cayó en Suite 34 y Suite 05: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: debe seleccionar E119 (Id 202), no el primer item I10X — restaurado |
| 12590 | v14.1.7 (M4: MUT-ORD-020, Worker M4.2 — retorno estrictamente nulo ante fallo en `apiOrdenamientoObtenerDx`): `return null` &rarr; `return {}` | No | Cayó en Suite 34: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: falla en Dx debe retornar strictly null — restaurado |
| 12603 | v14.1.7 (M4: MUT-ORD-023, Worker M4.2 — selección exacta de CUPS en `apiOrdenamientoObtenerCup`): cambiar `.find(...)` &rarr; `items[0]` | No | Cayó en Suite 34 y Suite 05: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: debe seleccionar 903895 (Id 502), no 903841 — restaurado |
| 12615 | v14.1.7 (M4: MUT-ORD-025, Worker M4.2 — retorno estrictamente nulo ante fallo en `apiOrdenamientoObtenerCup`): `return null` &rarr; `return {}` | No | Cayó en Suite 34: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: falla en CUP debe retornar strictly null — restaurado |
| 12625 | v14.1.7 (M4: MUT-ORD-026, Worker M4.2 — guarda de seguridad Kill-Switch en `apiOrdenamientoGuardar`): omitir chequeo de `state.killed` | No | Cayó en Suite 34 y Suite 05: "M4-KILL-1: Kill-Switch activo bloquea apiAccesoAsignarTurno y apiOrdenamientoGuardar" con: debe retornar null bajo kill-switch y 0 llamadas de red — restaurado |
| 12640 | v14.1.7 (M4: MUT-ORD-029, Worker M4.2 — flag SwHc en payload de `apiOrdenamientoGuardar`): `SwHc: false` &rarr; `SwHc: true` | No | Cayó en Suite 34 y Suite 05: "M4-ORD-2: TTL de órdenes vigentes (10 min), payload SwHc:false y header Accept JSON" con: SwHc debe ser estrictamente false en GuardarOrdenamiento — restaurado |
| 12650 | v14.1.7 (M4: MUT-ORD-033, Worker M4.2 — header Accept JSON en `apiOrdenamientoGenerarLinks`): `Accept: "application/json"` &rarr; `"text/plain"` | No | Cayó en Suite 34 y Suite 20: "M4-ORD-2: TTL de órdenes vigentes (10 min), payload SwHc:false y header Accept JSON" con: GenerarLinks debe solicitar Accept: application/json — restaurado |
| 13218 | v14.1.7 (M4: MUT-ORD-039, Worker M4.2 — discriminación de estructura de array en `_pymYaOrdenadoHoyDesdeElScript`): `Array.isArray(det.actividades)` &rarr; `typeof det.actividades === "object"` | No | Cayó en Suite 34: "M4-PYM-2: _pymYaOrdenadoHoyDesdeElScript extrae lista real de actividades ordenadas hoy" con: objeto no-array debe retornar [] — restaurado |
| 13218 | v14.1.7 (M4: MUT-ORD-040, Worker M4.2 — lectura estricta de propiedad actividades en `_pymYaOrdenadoHoyDesdeElScript`): leer `.items` en vez de `.actividades` | No | Cayó en Suite 34: "M4-PYM-2: _pymYaOrdenadoHoyDesdeElScript extrae lista real de actividades ordenadas hoy" con: debe leer estrictamente .actividades y no .items — restaurado |
| 1584 | v14.1.8 (M4: MUT-CLIN-037, Worker M4.3 — coincidencia estricta de cédula vs otros números en HTML en `_atheneaCedulaCoincide`): buscar número telefónico que difiere del doc principal | No | Cayó en Suite 34: "M4-ATH-1: Fechas, cédulas y guardas de cruce en Puente Athenea" con: si el documento principal difiere pero el número aparece en otra parte, no debe coincidir — restaurado |
| 2145 | v14.1.8 (M4: MUT-CLIN-054, Worker M4.3 — discriminación de distractores falsy en `_findLabField`): no saltar altIds vacíos/nulos | No | Cayó en Suite 34: "M4-DOM-1: _findLabField y _findHbA1cFields límites de búsqueda" con: debe saltar IDs falsy ignorando distractor y encontrar campoAltReal — restaurado |
| 2149 | v14.1.8 (M4: MUT-CLIN-055, Worker M4.3 — retorno estrictamente nulo ante id inexistente frente a distractor input en `_findLabField`): `return document.querySelector("input")` | No | Cayó en Suite 34: "M4-DOM-1: _findLabField y _findHbA1cFields límites de búsqueda" con: campo inexistente debe retornar strictly null y no distractor input — restaurado |
| 10167 | v14.1.8 (M4: MUT-CLIN-097, Worker M4.3 — captura de excepciones en transporte de Annar en `apiAccesoObtenerLaboratoriosAnnar`): `throw e` en catch | No | Cayó en Suite 34: "M4-LAB-1: apiAccesoObtenerLaboratoriosAnnar y apiAccesoObtenerLaboratoriosCiti retornan null en fallos de red" con: excepción en capa de transporte de Annar debe resolverse a strictly null sin lanzar — restaurado |
| 10171 | v14.1.8 (M4: MUT-CLIN-102, Worker M4.3 — retorno strictly null ante excepción en transporte de Citi en `apiAccesoObtenerLaboratoriosCiti`): `return []` en catch | No | Cayó en Suite 34: "M4-LAB-1: apiAccesoObtenerLaboratoriosAnnar y apiAccesoObtenerLaboratoriosCiti retornan null en fallos de red" con: excepción en capa de transporte de Citi debe resolverse a strictly null y no [] — restaurado |
| 10161 | v14.1.8 (M4: MUT-AGD-044, Worker M4.3 — captura de excepciones en transporte de Digiturno en `apiDigiturnoFinalizarTicket`): `throw e` en catch | No | Cayó en Suite 34 y Suite 13: "apiDigiturnoFinalizarTicket: captura errores de red sin lanzar" con: Digiturno captura error y resuelve de forma segura sin lanzar — restaurado |
| 10585 | v14.1.8 (M4: MUT-AGD-048, Worker M4.3 — captura de excepciones en transporte de validación de agenda en `apiAccesoAgdValidarAgenda`): `throw e` en catch | No | Cayó en Suite 34 y Suite 13: "apiAccesoAgdValidarAgenda: fallo o excepción en la red retorna null" con: AgdValidarAgenda captura error y retorna strictly null sin lanzar — restaurado |
| 12560 | v14.1.8 (M4: MUT-ORD-012, Worker M4.3 — validación de docId vacío/nulo en `apiOrdenamientoBuscarPaciente`): `return 0` en lugar de `return null` | No | Cayó en Suite 34 y Suite 05: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: docId vacío debe retornar strictly null — restaurado |
| 12571 | v14.1.8 (M4: MUT-ORD-015, Worker M4.3 — captura de excepciones en transporte de búsqueda de paciente en `apiOrdenamientoBuscarPaciente`): `throw e` en catch | No | Cayó en Suite 34: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: excepción en BuscarPaciente debe capturarse y retornar strictly null — restaurado |
| 12589 | v14.1.8 (M4: MUT-ORD-020, Worker M4.3 — retorno strictly null ante excepción en transporte de Dx en `apiOrdenamientoObtenerDx`): `return {}` en catch | No | Cayó en Suite 34: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: excepción en ObtenerDx debe retornar strictly null y no {} — restaurado |
| 12614 | v14.1.8 (M4: MUT-ORD-025, Worker M4.3 — retorno strictly null ante excepción en transporte de CUP en `apiOrdenamientoObtenerCup`): `return {}` en catch | No | Cayó en Suite 34: "M4-ORD-1: apiOrdenamientoObtenerDx y apiOrdenamientoObtenerCup seleccionan el código exacto y toleran fallos" con: excepción en ObtenerCup debe retornar strictly null y no {} — restaurado |
| 6192 | v14.1.8 (M5: Worker M5.1 — anulación del cierre por Escape en `_activarAccesibilidadModal`): `if (false)` en vez de `if (e.key === "Escape")` | No | Cayó en Suite 35: "R6.4: _activarAccesibilidadModal cierra con Escape y ejecuta closeCallback" — restaurado |
| 6205 | v14.1.8 (M5: Worker M5.1 — anulación del Focus Trap cíclico en `_activarAccesibilidadModal`): `if (false)` en vez de gestionar ciclo `Tab` / `Shift+Tab` | No | Cayó en Suite 35: "R6.4: _activarAccesibilidadModal atrapa el foco (Focus Trap) ciclando con Tab y Shift+Tab" — restaurado |
| 6231 | v14.1.8 (M5: Worker M5.1 — anulación de restauración de foco en cleanup de `_activarAccesibilidadModal`): omitir `previoFoco.focus()` al cerrar | No | Cayó en Suite 35: "R6.4: _activarAccesibilidadModal restaura el foco al elemento invocador al cerrar" — restaurado |
| 7404 | v14.1.8 (M5: Challenger M5.2 — degradación de contraste WCAG AA en `.vgl-bg-success`): cambiar `color:var(--bg-solid, #0b0e15)` por `color:#ffffff` | No | Cayó en Suite 35: "R6.3: .vgl-bg-success declara color de texto de alto contraste contrastante" — restaurado |
| 7516 | v14.1.8 (M5: Challenger M5.2 — anulación de anillo de foco `:focus-visible` en controles interactivos): cambiar `outline:2px solid var(--c-azul)` por `outline:none` en controles y switches | No | Cayó en Suite 35: "R6.3: Anillos de foco :focus-visible definidos para controles interactivos" — restaurado |
| 1027 | v14.1.8 (M5: Challenger M5.2 — reintroducción de concatenación cruda de excepción `+ e` en alert de telemetría): cambiar mensaje empático por `"❌ Error exportando telemetría: " + e` | No | Cayó en Suite 35: "R6.2: Ausencia de concatenaciones crudas de excepciones en alert() y toasts" — restaurado |
| 6199 | v14.1.8 (M5: Challenger M5.1 — anulación del manejador Escape en `_activarAccesibilidadModal`): `if (false && e.key === "Escape")` | No | Cayó en Suite 35: "R6.4: _activarAccesibilidadModal cierra con Escape..." y "[ESTRÉS ADVERSARIAL] Escape propaga stopPropagation..." — restaurado |
| 6214 | v14.1.8 (M5: Challenger M5.1 — inversión de dirección de tabulación en `_activarAccesibilidadModal`): `if (!e.shiftKey)` en vez de `if (e.shiftKey)` | No | Cayó en Suite 35: "[ESTRÉS ADVERSARIAL] Focus Trap con 100 elementos enfocables cicla extremos..." y "...Foco fuera del modal es recapturado..." — restaurado |
| 6206 | v14.1.8 (M5: Challenger M5.1 — supresión total del Focus Trap en `_activarAccesibilidadModal`): `if (false && e.key === "Tab")` | No | Cayó en Suite 35: "[ESTRÉS ADVERSARIAL] Focus Trap con 0 elementos...", "...con 1 único elemento...", "...con 100 elementos..." — restaurado |
| 6243 | v14.1.8 (M5: Challenger M5.1 — anulación de foco previo en cleanup de `_activarAccesibilidadModal`): `if (false && previoFoco...)` | No | Cayó en Suite 35: "R6.4: _activarAccesibilidadModal restaura el foco al elemento invocador al cerrar" — restaurado |
| 6201 | v14.1.8 (M5: Challenger M5.1 — omisión de stopPropagation en Escape en `_activarAccesibilidadModal`): comentar `e.stopPropagation()` | No | Cayó en Suite 35: "R6.4: [ESTRÉS ADVERSARIAL] Escape propaga stopPropagation, previene default y tolera callbacks ausentes o que lanzan" — restaurado |

## Mutantes Equivalentes Demostrados (Forensic Equivalence Analysis)

| ID Mutante | Función y Línea | Mutación Aplicada | Demostración Matemática de Equivalencia |
|---|---|---|---|
| `MUT-CLIN-043` | `_fechaDesdeNumeroSolicitud` (L1660) | Eliminar `diaN < 1 \|\| diaN > 31` de la guarda | La validación subsecuente `const dt = new Date(anio, mesN - 1, diaN); if (dt.getFullYear() !== anio \|\| dt.getMonth() !== mesN - 1 \|\| dt.getDate() !== diaN) return null;` realiza un round-trip estricto sobre el objeto Date nativo. Para cualquier `diaN < 1` o `diaN > 31`, `dt.getDate() !== diaN` o `dt.getMonth() !== mesN - 1` evalúa a `true` y retorna `null`. La guarda eliminada es una optimización temprana con semántica idéntica. |
| `MUT-CLIN-061` | `_extractFechaSolicitudTopLevel` (L2295) | Eliminar `typeof res !== "object"` | En JavaScript, ejecutar `Object.keys(res)` sobre tipos primitivos (números, booleanos, null, undefined) devuelve `[]`, y sobre strings devuelve índices numéricos (`["0", "1", ...]`), ninguno de los cuales coincide con el regex `/(?:fecha\|date\|creacion\|registro)/i`. Por tanto, el bucle nunca encuentra coincidencias y la función retorna `null` de forma idéntica con o sin la comprobación de tipo. |
| `MUT-CLIN-066` | `_extractAtheneaFecha` (L2309) | Eliminar `typeof res !== "object"` | En JavaScript, el acceso a propiedades sobre tipos primitivos (ej. `(123).fechaResultadoToma`) evalúa a `undefined`. Al pasar a `_parseFechaHoraLike(undefined)`, retorna `null`. El valor final retornado es estrictamente `null` para cualquier primitivo, haciendo la guarda de tipo semánticamente equivalente. |
| `MUT-CLIN-078` | `_pacienteSigueAbierto` (L2407) | Eliminar `!!actual &&` en la comparación | La función maneja `if (!docIdEsperado) return true;` previamente. Cuando `docIdEsperado` no está vacío, `String(actual) === String(docIdEsperado)` solo puede ser verdadero si `actual` es un valor no vacío que coincide exactamente con `docIdEsperado` (ya que `String(null) === "null" !== "12345"` y `String("") === "" !== "12345"`). La guarda `!!actual` es una tautología redundante. |
| `MUT-CLIN-085` | `injectLabsIntoCronicos` (L2443) | Cambiar valor inicial `let uroanalisisConfirmadoReal = false;` | La variable `uroanalisisConfirmadoReal` es sobreescrita incondicionalmente en la línea 2456 (`uroanalisisConfirmadoReal = _hayComponenteUroReal(labs);`) antes de ser leída en cualquier punto del flujo de ejecución. Es un *dead store*; modificar su inicialización no altera el comportamiento. |
| `MUT-REN-016` | `_pesoDeSignosVitales` (L10254) | Eliminar `!arr.length` de la guarda | Si `arr = []`, el acceso `const r = arr[0];` evalúa a `undefined`. La línea 10256 valida inmediatamente `if (!r \|\| typeof r !== "object") return null;`. Por consiguiente, ante un arreglo vacío, la función retorna `null` exactamente igual con o sin la cláusula `!arr.length`. |
| runner.js 36-37 | v14.1.7 (**hallazgo de la revisión de los PR de Jules, sobre el propio banco y no sobre un PR**): `t.lanza` y `t.noLanza` solo atrapaban lo que se lanzara de forma SÍNCRONA. Al pasarles una función `async` **no podían fallar jamás**: una función async no lanza, devuelve una promesa rechazada, y el `catch` no llega a saltar. En la base no había ni un uso async de los 49 que hay, así que el agujero estaba **latente** — pero ya atrapó a un PR real (`await t.noLanza(async () => await c.api.pageFetchJson("url"))`), y se comprobó haciendo que `pageFetchJson` lanzara siempre: cayeron 11 pruebas antiguas y ESA se quedó verde. El arreglo NO es convertirlas en `async` a secas: eso daría a los 49 llamadores síncronos una promesa suelta y convertiría un fallo en un rechazo no capturado, silencioso otra vez y en 49 sitios. Se bifurca por lo que devuelve `fn()`: sin thenable, se comporta igual que antes; con thenable, devuelve una promesa que el llamador debe esperar. Mutaciones: revertir cada una de las dos a su versión vieja | No | **La primera vez SOBREVIVIERON las dos**, y con razón: sin ningún uso async en la base, la versión vieja y la nueva se comportan idénticamente. Es decir, el arreglo no estaba sujeto por nada — el mismo modo sombra que este proyecto persigue, esta vez en mi propio arreglo. Se añadieron 3 pruebas en la suite 26 que ejercitan las seis combinaciones (síncrono/async × lanza/noLanza × resuelve/rechaza); con ellas, ambas mutaciones caen nombrando el caso. 986/986 en verde |
| suite_26 | v14.1.7 (**centinela hermano del de `t.casoAsync`**): el arreglo anterior solo funciona si el llamador ESPERA el resultado; una llamada async sin `await` devuelve una promesa que nadie mira y la prueba vuelve a ser incapaz de fallar. Se añadió un centinela que lee el código de las suites y falla si encuentra `t.lanza`/`t.noLanza` con función async sin `await`. Mutación: inyectar esa llamada en una suite | No | **Hallazgo del propio proceso, y cambia la lectura del resultado:** inyectada en `suite_22` (que corre ANTES del centinela) la mutación no lo dispara — pero tampoco pasa desapercibida: Node trata el rechazo no capturado como fatal, el runner **muere con código 1** y el mensaje nombra el caso. O sea, el fallo ya era ruidoso por sí solo. Inyectada en `suite_27` (posterior al centinela) sí cae la aserción, nombrando archivo y línea. Las dos defensas cubren cosas distintas: Node caza los casos que revientan, el centinela caza los que resuelven en silencio (`t.noLanza(async () => 42)` sin await no revienta y no comprueba nada) y además avisa en el sitio en vez de tumbar toda la corrida |
| 2841-2919 / 10145 | v14.1.8 (**rangos y unidades OFICIALES, leídos de Everest — lo que deja obsoleto el PR #63 en vez de solo rechazarlo**). El PR #63 proponía guardas de plausibilidad con umbrales escritos a mano, y se rechazó por dos motivos: los umbrales no se pueden validar contra conocimiento del modelo (regla del proyecto), y su implementación **descartaba el valor fuera de rango con un `return` a secas**, fundiendo "no hay regla" con "se sale del rango" — el analito desaparecía como si el laboratorio no hubiera llegado nunca. La alternativa es no inventar nada: Everest publica en `GetValidacionExamenCronicos?citaId=` su propia tabla de validación, con rango **y unidad** por examen. Añadidos `_numeroEstricto`, `_reglaExamenAplicable`, `_reglasDeExamen`, `_unidadOficialDeExamen`, `_plausibilidadOficial`, el puente `LAB_KEY_A_EXAMEN_EVEREST` / `_reglasParaLabKey`, y el lector `apiHcValidacionExamenCronicos` (caché por citaId, solo ARRAY, warn-y-null). `_plausibilidadOficial` devuelve SIEMPRE un estado explícito (`valor_no_numerico` / `sin_regla` / `bajo` / `alto` / `dentro`), que es justo la distinción que #63 destruía. **13 mutaciones sobre las funciones puras y el lector, más 4 sobre el puente: las 17 muertas**, cada una nombrando su caso — sexo desconocido, envolvente vs. regla más estrecha, código exacto vs. `indexOf`, bordes de edad, contradicción de unidades, caché por cita, respuesta que no es array | No (17/17) | Ver las tres filas siguientes: dos hallazgos propios del proceso y una corrección de una aserción mía |
| 2790-2802 | v14.1.8 (**la coerción que fabricó tres alarmas falsas de una vez — la escribí yo y las pruebas la tumbaron antes de subirla**). La primera versión usaba `Number()` directo sobre los campos de la API. `Number(null)`, `Number("")`, `Number(" ")` y `Number(false)` valen **0, no NaN**, y de ahí salieron tres fallos simultáneos en la primera corrida (8 pruebas rojas): (1) una fila con `edadMin: null` —que es como Everest dice "no acoto por edad"— se leía como "acoto desde los 0 años", así que la fila se descartaba en todo paciente cuya edad no se hubiera podido leer; (2) una fila con `valorMinimo: null, valorMaximo: null` se convertía en el rango **[0, 0]**, que declara ALTO cualquier valor positivo; (3) un resultado vacío del LIS —el laboratorio que aún no ha llegado— se volvía el número 0 y salía marcado **BAJO** en vez de "no hay número que comparar". Extraído `_numeroEstricto`, que devuelve null para null/undefined/booleano/vacío/no numérico y conserva el 0 legítimo (la RAC empieza en 0). Mutación: volver a `Number()` a secas | No | Cae la prueba `_numeroEstricto: lo que NO es un número no se convierte en cero` **más otras 8**, es decir la mutación reproduce exactamente el estado de la primera corrida. Es el mejor argumento a favor de escribir las pruebas antes de dar por buena la implementación: las tres alarmas falsas venían de una sola línea y ninguna era visible leyendo el código |
| — | v14.1.8 (**lo que enseñó la captura REAL del endpoint, y por qué importa más que el código**). Se localizó la respuesta literal en `captura_rutacronicos_borrado_rac_20260812.json` (28 filas, cero datos de paciente) y desmintió tres suposiciones mías: (1) `codigoExamen` **no es un CUPS**, es el nombre interno del campo (`HEMOGLOBINA`, `HBA1C`, `RELACION_ALBUMINURIA_CREATININA`); (2) `mensajeMinimo`/`mensajeMaximo` **no existen** en el payload real — se siguen leyendo por compatibilidad hacia adelante, pero eso queda anotado como no observado, no como campo; (3) `sexo` llega `null` en una fila y **ausente** en las otras 27, y solo HEMOGLOBINA acota por edad. Y contestó la pregunta que llevaba semanas abierta: **la IPS declara la RAC en `mg/g`** (`RELACION_ALBUMINURIA_CREATININA`, 1–20000 mg/g), no en mg/mmol. Ya no es una suposición. **Pero el hallazgo de mayor riesgo es otro:** la tabla declara la hemoglobina plausible entre **3 y 30 g/dL**. Una hemoglobina de 3,2 cae DENTRO y es una urgencia transfusional. Estos son rangos de **plausibilidad** ("¿este número puede ser un resultado de verdad, o es un dedazo o una unidad cambiada?"), **no de normalidad clínica**. Por eso el estado bueno se llama `dentro` y no `normal`. Se añadió la prueba `LO MÁS IMPORTANTE DE ESTE ARCHIVO: 'dentro' NO significa 'normal'` para que nadie pueda pintar un visto verde sobre un valor crítico sin leer antes por qué no debe |
| 10150 / 2836 | v14.1.8 (**dos mutaciones que SOBREVIVIERON, y las dos eran huecos reales de aserción**). (1) Quitar la guarda `if (!citaId) return null` del lector no ponía nada rojo: la prueba se titulaba "sin citaId **no consulta nada**" pero solo miraba el valor devuelto, y sin la guarda la petición salía igual, fallaba y el `catch` devolvía null de todos modos — el nombre prometía algo que no se verificaba. Se pasó a contar las llamadas al `fetch` y exigir **cero**. (2) Añadir un `|| labKey` al puente (que convierte la lista blanca en "si no está mapeado, búscate por tu propio nombre") tampoco caía, porque la tabla real no trae ninguna fila `UROANALISIS` y ambas versiones devolvían `[]` por casualidad. Se añadió una tabla-trampa con filas `UROANALISIS` e `INVENTADO` para fijar que **la exclusión deliberada es deliberada**: UROANALISIS no se mapea porque no es un examen numérico, y el día que la IPS añada esa fila no debe empezar a juzgarse con un rango | **SÍ (las dos, la primera vez)** | Con las aserciones nuevas, ambas caen nombrando su caso. **Corrección propia de camino:** una tercera prueba mía afirmaba que la forma real de la tabla "no descarta ninguna fila" incluyendo HEMOGLOBINA — y era **falsa la aserción, no el código**: HEMOGLOBINA es la única de las 28 que acota por edad (0–110), así que sin edad legible se descarta, que es el criterio de toda la suite funcionando. Se reescribió fijando la consecuencia con los ojos abiertos: si no se puede leer la edad, la hemoglobina se queda sin rango oficial y no se juzga |
| — | v14.1.8 (**una función borrada antes de subirla, por la deuda que este proyecto ya arrastró tres veces**). Se había escrito `_validacionExamenInvalidar` por simetría con `_ordenesVigentesInvalidar` y `_bannerPymInvalidar`. No la llamaba nadie, y revisando **no hay ningún evento que deba invalidar esa caché**: es parametrización de la IPS, no dato del paciente; la caché va por `citaId`, así que otro paciente ya falla por clave, y el TTL cubre un cambio de configuración a media jornada. Se borró en vez de inventarle un llamador. `_bannerPymInvalidar` vivió versiones enteras sin un solo llamador (ver v14.0.0) y `_demograficosInvalidar` sigue sin ninguno — no se añade una cuarta | No aplica (código muerto retirado) | — |
| 2454-2500 / 10230 / 10310 | v14.1.8 (**el cableado: de las funciones puras al efecto que le importa al paciente**). Hasta aquí v14.1.8 era lectura y funciones puras — modo sombra en su forma más honesta, pero modo sombra. Ahora `injectLabsIntoCronicos` recibe un tercer argumento opcional con la tabla oficial y la demografía, y **se niega a escribir un valor implausible**: lo reporta con lo que llegó, lo que la IPS esperaba y en qué unidad. El caso concreto es el que ya mordió a este script: una creatinina en µmol/L llega como **88** donde la casilla espera 1,0 mg/dL; la guarda de v14.1.3 impidió que ese 88 diera un estadio renal falso, pero Auto-Labs **seguía escribiéndolo en la casilla**, y de ahí se firma en la historia. Se elige negarse y no "escribir y avisar" por la misma razón de siempre: no escribir es recuperable, escribir un número equivocado no. Y el aviso **nunca propone el valor convertido** — hay prueba que exige que el "1.0" equivalente NO aparezca en el texto, porque sugerir una conversión que nadie comprobó en el laboratorio es justo lo que este proyecto prohíbe. **10 mutaciones, 8 muertas a la primera**; las dos supervivientes se tratan en las filas siguientes | No (8/10, y las 2 restantes resueltas abajo) | La mutación que más importa —escribir igual y solo avisar— cae en "EL DESENLACE: la creatinina de 88 NO llega a la casilla de la historia, y se reporta", que comprueba el efecto en el DOM y no la decisión |
| 10226 | v14.1.8 (**bug propio hallado leyendo el tráfico real, no las pruebas**): el lector mandaba el `citaId` **en claro**. La captura muestra `citaId=MTIyMTk3NA`, que es base64 de "1221974" con el relleno `=` quitado — el mismo formato que ya usaba `apiDigiturnoFinalizarTicket` (`btoa(String(citaId))`). Con el número en claro se le pedía al servidor una cita que no existe, así que la tabla oficial **nunca habría llegado en producción** y todo lo demás habría quedado inerte sin que ninguna prueba se quejara. Añadido `_base64SinRelleno`. Mutaciones: (1) volver al id en claro y (2) mandar el relleno | No | (1) cayó "la consulta manda el citaId CODIFICADO"; (2) cayó "_base64SinRelleno reproduce lo que Everest manda de verdad". **Y el arreglo destapó un hueco de golpe:** la prueba de caché de la suite 30 comparaba `url.includes("citaId=111")` y, al empezar a codificar, dejó de casar **en silencio** — las dos citas devolvían lo mismo y la prueba SIGUIÓ EN VERDE porque ninguna aserción notaba que el mock ya no las distinguía. Se derivó el mock de la propia función para que no pueda volver a quedarse mudo |
| 10250-10300 | v14.1.8 (**el `citaId` no es alcanzable desde la historia clínica, así que la tabla se ESCUCHA en vez de pedirse**). La URL de la vista es `/viva/HCHealth/` a secas (comprobado en la captura) y el `citaId` vive dentro del estado de Angular: pedir la tabla desde ahí habría exigido adivinar un identificador. No hace falta — **al abrir la Ruta Crónicos, Everest la pide sola**. El oyente es de solo lectura: `open` solo anota la URL en la instancia, la lectura va en un `addEventListener("load")` (no en `onload`, que habría **reemplazado** el manejador de Everest), todo dentro de try/catch, y nunca se modifica la petición ni la respuesta. Sale gratis: ni una consulta más contra el servidor de la IPS. Se lee `xhr.response` y no `responseText` porque Angular pone `responseType="json"` y ahí `responseText` LANZA (lección de v14.1.7 con el mapa). Mutaciones: (1) permitir instalarlo dos veces, (2) guardar cualquier respuesta (una página de login con HTTP 200 incluida) y (3) leer `responseText` | No | Las tres cayeron. La (3) cae en dos pruebas a la vez, incluida "si la respuesta del oyente es basura, no revienta ni guarda nada" — la que existe porque ese código corre DENTRO del ciclo de Everest y un fallo ahí sería peor que cualquier bug que venga a arreglar |
| 2470 | v14.1.8 (**mutación SUPERVIVIENTE, y el fallo que anunciaba era real**): objetar todo lo que no fuera `"dentro"` —en vez de solo `bajo`/`alto`— pasaba el banco entero. El caso que nadie cubría: una regla que **existe pero no acota** (`valorMinimo` y `valorMaximo` nulos), que da `sin_regla`. Es un caso perfectamente posible —la IPS puede declarar la unidad de un examen sin ponerle límites, lo natural en uno cualitativo— y el día que apareciera, ese analito **dejaría de escribirse siempre**, sin motivo y sin que nadie entendiera por qué. Añadida la prueba "una regla que declara la UNIDAD pero ningún rango no bloquea nada" | **SÍ (la primera vez)** | Con la prueba nueva, la mutación cae nombrando el caso. 1032/1032 en verde |
| 2469 | v14.1.8 (**mutación SUPERVIVIENTE que NO pedía una prueba, sino borrar código**): quitar `|| !o.tablaOficial.length` de la guarda de entrada no ponía roja ni una prueba — y al mirarlo, no por falta de aserción: con la tabla vacía, `_reglasParaLabKey` devuelve `[]` y la salida de dos líneas más abajo lo atrapa igual. La condición era **redundante**. Se borró en vez de escribirle una prueba a algo que no hace nada. Es la contraparte exacta del caso de v14.1.5 (`_sembrarEstadoInicial`), donde la mutación superviviente significaba "este código no es alcanzable, muévelo": aquí significaba "este código no hace nada, quítalo". Una guarda que aparenta sujetar algo y no sujeta nada es peor que no tenerla, porque la próxima persona la lee como imprescindible | **SÍ (y con razón)** | No aplica: tras borrar la condición, la mutación ya no tiene dónde aplicarse. 1032/1032 en verde |
| 5417 | v14.1.9 (**hallazgo del satélite S5 de seguridad, verificado contra el código actual — la barrera anti-PHI tenía los pasos en el orden equivocado**). `uxClaveLimpia` es lo que impide que un dato del paciente acabe siendo el NOMBRE de una métrica enviada al tablero. Hacía: quitar los tramos de 6+ dígitos, y DESPUÉS quitar todo lo que no fuera `[a-z0-9.:_-]`. Con ese orden se colaban **tres** formas, todas cotidianas: (1) `"1.143.142.498"` — el punto está entre los caracteres PERMITIDOS, así que no hay ningún tramo de 6 dígitos seguidos que quitar y la cédula salía entera; (2) `"300-123-4567"` — igual con el guion; (3) `"1 143 142 498"` — el espacio sí se quitaba, pero en el segundo paso, cuando el primero ya había pasado: los dígitos se pegaban DESPUÉS de la única comprobación que los habría cazado. Ahora se normaliza primero y se cuentan los dígitos **ignorando los separadores**. Se comprobó antes de tocar nada que las 38 claves reales son literales y ninguna lleva dígitos (la más cargada es `panel.silenciar15`, con dos), así que el arreglo no puede tragarse telemetría legítima. Mutaciones: (1) volver al orden viejo, (2) subir el umbral a 20 y (3) bajarlo a 1 (borrar cualquier dígito) | No | (1) y (2) cayeron en "una cédula con puntos NO pasa"; (3) cayó en "las claves reales del script sobreviven intactas — si no, la barrera ciega el tablero", que es la prueba que impide arreglar esto a base de borrarlo todo |
| 5419 | v14.1.9 (**mutación SUPERVIVIENTE que destapó un contrato sin sujetar**): contar los separadores como si fueran dígitos (`m.length >= 6` en vez del conteo de dígitos) daba el MISMO resultado en todos los casos probados — y era más agresivo, así que se habría comido claves legítimas cortas del tipo `v.1.2.3.4` sin que nadie lo notara. El comentario del código promete que el umbral se cuenta en dígitos; ninguna prueba lo fijaba. Añadida "el umbral se cuenta en DÍGITOS, no en caracteres — 5 dígitos pasan, 6 no", que sujeta el contrato por los dos lados | **SÍ (la primera vez)** | Con la prueba nueva, la mutación cae nombrando el caso |
| 14344 | v14.1.9 (**el informe descargable filtraba en su CABECERA justo lo que se cuidaba de ocultar en todo su cuerpo**). `downloadDiagnostic` sustituye cada nodo de texto por `"···"` (`san()`) y recorta las cédulas a 3 dígitos (`mask()`)… y dos líneas más arriba escribía `location.href` y `document.title` **en crudo**. `document.title` es el peor de los dos: en un sistema de historia clínica suele llevar el NOMBRE del paciente. Y la URL lleva los identificadores en la consulta y en el fragmento — el propio script construye `...BusquedaPaciente#doc=<cédula>`. Se conserva lo que sirve para diagnosticar (qué ruta, cuántos parámetros había) y se tira lo que identifica; el título **se omite entero**, porque recortarlo dejaría el principio del nombre, que es la parte que identifica. Mutaciones: (1) URL en crudo, (2) título en crudo y (3) ocultarlo todo | No | (1) y (2) cayeron en "la CABECERA tampoco filtra"; (3) cayó en la misma prueba, por la aserción que exige que la RUTA se conserve — sin ella, "arreglar" esto borrándolo todo habría pasado, y el informe habría dejado de servir para lo que existe |
| 14349 | v14.1.9 (**mutación SUPERVIVIENTE, y el caso que no cubría era EL REAL**): conservar el fragmento (`sinFrag = href`) no ponía roja ninguna prueba. La causa: la URL de la prueba llevaba consulta **y** fragmento, y al partir por `"?"` el fragmento se caía de rebote. Con una URL de **solo fragmento** la cédula se escapaba — y esa es exactamente la forma del enlace que el propio script arma para Athenea, `...BusquedaPaciente#doc=<cédula>`, sin ningún parámetro. La prueba cubría la variante inventada y dejaba fuera la real. Añadida "una URL de SOLO fragmento tampoco filtra" | **SÍ (la primera vez)** | Con la prueba nueva, la mutación cae nombrando el caso. 1037/1037 en verde |
| — | v14.1.9 (**contraste de calibración, satélite S5 de seguridad**). De los tres hallazgos técnicos que pude verificar contra el código actual: **dos eran reales y están corregidos arriba** (`uxClaveLimpia` y la cabecera de `downloadDiagnostic`). El tercero —XSS por `atheneaUrl` interpolada en `innerHTML`— **queda refutado**: los cinco sitios que asignan `doc_id` pasan todos por `extractDoc`, que devuelve exclusivamente `m[1]` de `/^(\d{5,15})$/` o `/(\d{5,15})/`, es decir **solo dígitos**. No hay forma de introducir una comilla ni un `<`. Se marcó el sink sin rastrear el origen. Mismo patrón anotado en v14.1.3 y v14.1.5: los informes de agentes externos aciertan en lo estructural y sobrestiman en lo puntual — se verifica todo contra el código antes de actuar. **Nota de método:** el informe S5 se produjo contra un árbol de 976 comprobaciones, anterior incluso a la base congelada (982), así que sus hallazgos había que revalidarlos uno a uno contra el código de hoy y no aplicarlos tal cual |
| runner.js 76 / harness.js 151 | v14.1.9 (**el fallo más grave del banco desde que existe, y era invisible por construcción**). El arnés dejaba `win.GM_xmlhttpRequest = o.gmxhr \|\| (() => {})`. Parece inofensivo, pero `_pageFetchJsonCore` usa GM_xmlhttpRequest como SEGUNDA VÍA cuando `fetch` falla, envuelto en `new Promise((resolve, reject) => …)` que solo se cierra desde `onload`/`onerror`/`ontimeout`. Con un stub que no llama a ninguno, **esa promesa no se cierra jamás**. Consecuencia en cadena: la suite se cuelga → `main()` se queda esperando → el bucle de eventos de Node se vacía → **el proceso sale solo con código 0**. No se imprime el resumen, **las suites posteriores no llegan a ejecutarse**, y el CI lo lee como VERDE. Se descubrió por accidente: tres mutaciones de verificación sobre funciones ALTO (`estadioKDIGO` en el borde 90, `cups.every→some`, `apptKey` sin hora) dieron "SOBREVIVE" — y no era cierto, era que el código de salida ya no significaba nada. **Dos arreglos, y el orden importa:** (1) `runner.js` declara `process.exitCode = 1` de entrada y solo el final legítimo lo pone a 0, con un aviso `beforeExit` que nombra el síntoma; así ningún camino que no llegue al final puede salir verde, sin depender de que nadie se acuerde de nada. (2) el stub por defecto del arnés ahora llama a `onerror`, que es lo que Tampermonkey hace de verdad ante un fallo de red (la llamada real lleva `timeout: 15000`, así que en el navegador esa promesa SIEMPRE se cierra) | No aplica (fallo de infraestructura del banco, no del producto) | Con la guarda puesta, el cuelgue pasa de silencioso-verde a **código 1 con mensaje explícito**; con el arnés arreglado el banco vuelve a completar: **1180 comprobaciones, 400/433 funciones (92,4 %)**. Y repetidas las tres mutaciones con el código de salida ya fiable, **las tres MUEREN** — el trabajo de mutación del enjambre se sostiene en el muestreo hecho. **Nota de método, la segunda vez que muerde en esta misma sesión:** una mutación que "sobrevive" no se cree hasta comprobar que el archivo cambió *y* que el medidor funciona. La primera vez el `replace` no casaba; esta vez el que mentía era el código de salida |
| — | v14.1.9 (**contraste de calibración, informe final del enjambre M0–M6**). Verificado contra el código fusionado: **`@connect localhost` y `127.0.0.1` retirados** (quedan 11 directivas, ninguna local) ✓; **0 aserciones tautológicas** —las 2 coincidencias de `t.cierto(true` son el propio detector de la suite 34, que además es una defensa nueva y buena— ✓; **`scrubPII` SÍ está enganchada** en producción vía `sanitizePII` (línea 973, ruta de reporte de errores), no es modo sombra ✓; **mutación de `cups.every`, borde de KDIGO y `apptKey` con hora: las tres mueren** ✓. **Matizaciones, no refutaciones:** (a) "eliminó los 23 `catch` vacíos" hay que leerlo acotado a *rutas críticas* — en el archivo quedan 148 `catch(e){}` repo-wide, muchos deliberados; (b) el arreglo de M5 se reporta como `outline: 3px solid #3b82f6 !important` y en el código lo que hay es `outline:2px solid var(--c-azul)` — el arreglo existe, el informe no lo describe con exactitud; (c) `.vgl-tl` (los botones de cerrar/minimizar) conserva `outline:none !important`, así que esos tres botones siguen sin foco visible por teclado — cabo suelto de M5, menor pero real. **Observación de proceso:** 4 de las 11 compuertas fallaron por *mutaciones que sus propios agentes dejaron sin restaurar* en el código (M2 ×3 líneas, M5), no por defectos preexistentes. Es el mismo accidente que ya ocurrió aquí en v14.1.5 con un agente de verificación sin aislamiento. Con enjambres grandes sobre un solo archivo, restaurar la mutación tiene que ser parte de la compuerta, no de la buena voluntad del agente |
| suite_37 (nueva) | v14.1.9 (**la compuerta que faltaba: que la mutación olvidada no dependa de la buena voluntad de nadie**). De las 11 compuertas del enjambre M0–M6, **cuatro fallaron por mutaciones que sus propios agentes dejaron sin restaurar en el código** —`edad < 0` en Cockcroft-Gault, condición invertida en `fraudWatch`, `cups.some` por `cups.every`, y Tab/Shift+Tab cambiados en la trampa de foco—, no por defectos preexistentes. Y no es cosa de un solo equipo: en v14.1.5 pasó aquí mismo, cuando un agente de verificación sin aislamiento invirtió `isPanelHiddenActivity` con un `!` en el repo compartido y un `git add -A` lo subió a un commit ajeno. Es el accidente propio de trabajar con muchos agentes sobre un archivo de 14.000 líneas. **El banco normal no basta**: una mutación olvidada sobrevive si la prueba que la cazaba se "arregló" con ella puesta, o si esa rama nunca tuvo cobertura. La suite 37 no prueba comportamiento: **lee el código fuente** y exige que las decisiones que costaron un incidente sigan escritas tal cual — 9 invariantes positivos (con la razón CLÍNICA de cada uno en el mensaje de fallo, para que quien lo encuentre roto pueda decidir si el cambio era legítimo), 1 negativo (`cups.some` no puede reaparecer), y dos comprobaciones estructurales para las inversiones que no son de una sola cadena (la trampa de foco y el aviso audible condicionado al módulo). Se protege a sí misma: falla si alguien vacía la tabla o le quita los motivos | No aplica (compuerta nueva) | **Verificada reproduciendo las mutaciones históricas REALES, no inventadas: las 5 caen, cada una nombrando su propio caso** — `cups.every→some`, `edad>=18→edad>=0`, Tab/Shift+Tab invertidos, aviso audible condicionado al módulo, y la guarda de unidades de creatinina relajada a 2000. 1194/1194 en verde. **Límite que conviene tener presente y no disimular:** esto caza una lista curada, no una mutación cualquiera; su valor es que la lista contiene justo lo que ya hizo daño, y crece cuando algo nuevo lo haga |
| 10109 | v14.1.9 (**regresión de rendimiento que introduje YO en v14.1.8, hallada al revisar mi propio trabajo**). `_contextoOficialParaLabs` metió `apiAccesoBuscarPaciente` en el camino del CLIC de Auto-Labs, y esa función prueba rutas **en cascada**: hasta 4 peticiones secuenciales contra el servidor de la IPS antes de escribir la primera casilla — encima de los 2-4 s que el médico ya había esperado a Athenea, y **sin caché** pese a que hay 8 llamadores de producción y a que el propio comentario de v12.0.3 ya se quejaba de "un goteo constante de 404 contra el servidor" por repetir estas búsquedas. Lo que se compraba con esa latencia era casi nada: en la tabla real de 28 filas **ninguna usa el sexo** y la única que acota por edad es HEMOGLOBINA (0–110 años). Añadida caché por `uId\|cédula` con el TTL de siempre. **Solo se cachea el acierto:** un `null` es "no se pudo" (red caída, sesión vencida, paciente que aún no existe) y eso cambia de un momento a otro — cachearlo dejaría al médico sin poder reintentar justo cuando más urge. Mutaciones: (1) no consultar la caché, (2) que la clave ignore la cédula y (3) cachear también el fallo | No | (1) y (2) cayeron en "se cachea por cédula — el clic de Auto-Labs no repite la cascada"; la (2) es la que más importa porque cachear por médico y no por paciente haría que un paciente **heredara el id interno de otro**, y la prueba lo dice con ese nombre. (3) cayó en "un fallo NO se cachea: el médico tiene que poder reintentar en el acto". 1196/1196 en verde |
| — | v14.1.9 (**la cuarta vez, y esta se cortó a tiempo**): se había escrito `_pacienteIdInvalidarTodo` por simetría con las otras cachés. Cero llamadores, y revisando no hay ningún evento que deba invalidarla: la relación cédula → id interno de Everest no cambia, la clave ya separa por médico y por cédula, y el TTL cubre el resto. Borrada antes de subirla, igual que `_validacionExamenInvalidar` en v14.1.8. Van cuatro invalidadores sin llamador que este proyecto ha estado a punto de acumular (`_bannerPymInvalidar` sí llegó a vivir versiones enteras así, ver v14.0.0; `_demograficosInvalidar` sigue sin ninguno y está en la lista de J4 para decidir) | No aplica (código muerto retirado antes de existir) | — |
