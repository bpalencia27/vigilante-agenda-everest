# Vigilante de Agenda — dependencias de ejecución/desarrollo (Windows)
# Instalar en el venv:  pip install -r requirements.txt
# Luego, para Playwright (driver): python -m playwright install
# (No hace falta instalar el navegador de Playwright: se usa el Chrome del sistema.)

playwright==1.62.0
openpyxl==3.1.5
pywin32==312
cryptography==50.0.0
requests==2.34.2

# Empaquetado (opcional, solo para generar el .exe)
pyinstaller==6.21.0
